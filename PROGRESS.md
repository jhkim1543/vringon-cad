# VRINGON CAD — 진행 상태 (v14 빌드 시트 + 편집기)

## v14 — 사양서를 CAD 피처 트리가 아니라 빌드 시트로
화장품 용기 사양서가 파트 8개로 나왔는데 그중 5개(상부 필렛 R1.5, 캡 측면 필렛 R2.0, 외부 테이퍼 5도,
바닥 두께 2.4mm, 나사산 피치 1.5mm)가 **파트가 아니라 속성**이었다. 우리 빌더는 프리미티브 조립 엔진이라
"필렛"이라는 파트를 만들 수 없다. 원인은 프롬프트의 "최소 7개, 3~4개면 덜 만든 것"이라는 지시였고,
모델이 개수를 채우려고 속성을 파트로 올렸다.

- **파트의 정의를 좁혔다**: 따로 떼어낼 수 있는 솔리드만 파트. 필렛·챔퍼·테이퍼·두께·나사산은 features로
  옮기고 수치는 그 파트의 프로파일 좌표에 반영한다. 개수 하한 폐지.
- **스키마에 `build{prim,spec,repeat}` 추가**: 각 파트가 어떤 프리미티브인지와 빌더가 그대로 쓸 수치를 담는다.
  lathe는 "[반경,높이]" 12점 이상, loft는 4단 이상.
- **판정 기준을 개수에서 빌드 가능성으로**: `notBuildable`(프리미티브 없음/수치 부족/이름이 속성)과
  `thinLathe`(프로파일 10점 미만)로 재시도를 건다. 재시도 수락도 "파트가 더 많으면"에서
  "빌드 가능한 것이 더 많으면"으로 바꿨다(개수 보상이 패딩의 원인이었다).
- **정체성은 프롬프트가 기준**: "500ml 스테인리스 텀블러" 요청의 시안이 흰 화장품 용기로 나오자 사양서가
  이미지를 읽어 "300ml 화장품 용기"로 이름 붙인 사고가 있었다. 이름·제품군·재질은 요청을 따르고,
  형상 수치만 실측·이미지에서 가져오며, 불일치는 confidence.inferred에 적는다.
- **검증(사양서 단계)**: 같은 제품이 파트 8개(5개 빌드 불가) → **파트 3개 전부 lathe + 조밀 프로파일**로 바뀌었다.
  본체 `[0,0][28,0][30,2][31.5,5][32,10][32,100]...` 처럼 필렛이 파트가 아니라 프로파일 곡선으로 들어갔다.
- **빌드 시트 편집기 (신규)**: 생성된 프로그램의 프리미티브 수치를 패널에서 직접 고친다. 프로파일 점 추가·삭제,
  단면·치수·위치 편집, 260ms 디바운스 후 즉시 재빌드하고 실루엣 일치도를 다시 잰다. 모델 호출이 없어 즉각적이다.
  편집값은 고정 수치로 저장되고, 구조 컴파일 이후에 편집하면 프로그램 모드로 되돌아가 재컴파일을 요구한다.

# (이전) v13 실측 우선 파이프라인

## v13 — 사양서를 추정에서 계측으로
이미지→코드의 격차는 네 단계에서 각각 정보가 빠져서 생긴다. ① 투영(3D→2D) ② 어휘(픽셀→프리미티브)
③ 전사(이미지→**언어**→좌표) ④ 밀도(마이크로 디테일·PBR 채널). 참조 갤러리는 ③을 실루엣 추적으로,
①을 다시점으로, ④를 기하·채널 예산으로 막았다("alpha trace ... agree to 1.6 px", "12-view reference set",
"separate authored channels, none derived from the albedo").

우리는 이미 image-to-3D를 갖고 있으므로 다시점 시안을 흉내내는 대신 **형상을 먼저 만들고 그것을 잰다**.
①과 ③이 동시에 사라지고, 실루엣 추적은 정사영 렌더로 공짜로 따라온다.

- **js/measure.js (신규)**: 결정론 계측. 높이별 단면 프로파일(p90 반경·반폭 hx/hz), 단면 급변점
  (넥·확대·축소 = 파트 경계 후보), 주축·세장비·원형도·형태 분류(회전체/각형/판형), 부피·무게중심,
  그리고 **정사영 실루엣 마스크(정면·측면·평면)** 와 마스크 IoU.
- **파이프라인 재배치**: 시안 → **image-to-3D → 실측** → 사양서(실측값 기반) → 코드 저작 → 구조 컴파일 → 실루엣 IoU.
  유기 곡면은 실측 직후 재구성 그대로 유지하고 코드 저작을 건너뛴다.
- **사양서가 계측 리포트가 됨**: `/api/blueprint`에 실측 텍스트를 넘기면 "이 수치는 확정값이므로 다시 추정하지 말라"고
  지시하고, 이미지는 용도·재질·마감을 읽는 데만 쓴다. overall은 실측값으로 덮어쓴다.
- **프로파일을 DSL에 직접 투입**: `/api/plan`에 실측 텍스트를 함께 넘겨 높이별 반경을 lathe profile,
  반폭을 loft sections에 그대로 옮기게 한다. 언어를 거쳐 좌표를 만드는 단계가 빠진다.
- **검증이 의견에서 수치로**: 코드 결과와 참조 형상을 같은 정사영 프레임으로 렌더해 실루엣 IoU를 낸다.
  비전 모델의 "40/100" 대신 정면·측면·평면 각각의 일치율. 55% 미만이면 프리미티브로 도달 불가라고 알린다.
- 준비도(Asset Readiness)와 형상 일치도를 별도 행으로 분리. 서로 다른 지표다.

# (이전) v12 형상 클래스 분기 + 코드 품질

## v12 — img2threejs 코드 경로 복귀와 품질 기준
v8에서 로봇 자산 컴파일러로 전환하며 주 경로를 메시 재구성으로 바꿨는데, 그 결과 프롬프트로 만든 로봇 암이
37만 삼각형 단일 셸로 나와 구동부를 만들 수 없었다. 표면 군집은 구역일 뿐 부품이 아니다.

- **형상 클래스 분기**: 기계·하우징·용기·기구류는 **코드 경로**(기하 DSL 저작), 유기 곡면(마우스·신발·
  자동차·헬멧·피규어 등 `MESH_FIRST_RX`)은 **메시 경로**. 고품질 메시 버튼은 두 경로 모두 유지.
  메시 서비스가 죽으면 코드 경로로 자동 전환.
- **코드 결과도 구조 컴파일에 태운다**: 코드로 지은 파트는 처음부터 분리돼 있어 조인트·충돌체·질량·
  USD/URDF/MJCF가 전부 의미 있게 나온다. 메시 경로에서 불가능했던 구동부가 여기서 살아난다.
- **레퍼런스 갤러리 품질 요인 분석 → DSL 명세에 반영**: 참조 데모(BMX·글록·나이프·이어버드·샷건)의
  캡션에서 뽑은 실제 품질 동인은 다음과 같았고, 이를 명세의 "품질 기준" 7항으로 옮겼다.
  · 단면이 위치에 따라 변한다 ("no part is a constant-thickness extrusion", 더스트커버가 리시버보다 얇다)
  · 디테일이 텍스처가 아니라 실제 기하다 ("real geometry at the measured coordinates": 나사·보어·비드·퀼트)
  · 속이 찬 솔리드다 ("watertight solid rather than a plate with a picture on it", 관통 탱이 손잡이를 지난다)
  · 반복 요소는 개수를 센다 (스포크 5, 기어 잇수, 파베 스톤 수)
  · 파트당 mesh를 아끼지 않는다 (frame 4.3k / welds 3.2k / handlebar 1.1k / pegs 240 삼각형)
  · PBR 채널을 알베도에서 유도하지 않고 따로 저작한다
  · 움직임이 있다 (동기화된 드라이브트레인, 발사 VFX, 뚜껑 개폐)
  명세의 파트 수 상한을 2~10 → 5~14로 올리고 "파트당 mesh 1개는 덜 만든 것"을 못박았다.
- **설계 사양서를 기하 저작의 계약으로 전달**: `/api/plan`에 blueprint 텍스트를 함께 넘겨 치수·파트 수·
  개수·특징을 그대로 구현하게 했다. 사양서가 있으면 임의 축소를 막는다.
- **이미지 입력 시 시안 생성 생략**: 레퍼런스나 재현 이미지가 들어오면 `/api/design`을 건너뛰고 그 이미지를
  바로 설계 근거로 쓴다(원래 그렇게 되어 있었고, 이번에 경로를 명시적으로 확인했다).
- **CAD 입력 상세 복원**: STEP/IGES는 솔리드가 이미 분리돼 오므로 테셀레이션을 더 곱게 하고
  (linearDeflection 0.002 → 0.0008, angular 0.4 → 0.22) 솔리드별 **색상을 읽어 재질로 매핑**,
  면 수(brep_faces)를 파트 메타에 보존한다.
- **설계 사양서가 사라지던 버그**: 구조 컴파일이 `state.spec`을 CIR JSON으로 덮어쓰고 있었다.
  사양서와 CIR을 별도 패널로 분리.
- **표면 군집일 때 조인트 판정 금지**: 구역 경계는 실제 부품 경계가 아니므로 조인트를 만들지 않고
  그 사실을 명시한다. 로봇 암이 "직동 슬라이딩 암"으로 둔갑하던 원인.

# (이전) v11 파트 분리·관계 표현·레퍼런스 60종

## v11 — 분리되지 않던 형상, 읽히지 않던 관계
- **융합 메시 파트 분리**: 목(neck)을 주축 하나에서만 찾던 것을 세 주축 전부로 확대하고,
  잘록한 곳이 아예 없는 형상(함체 등)은 **위치+법선 k-means 군집 분할**로 나눈다.
  면적 가중·최원점 시딩이라 실행마다 같은 결과가 나온다. 실측: 융합 함체 메시 1파트 → 4파트, 준비도 86.
- **조인트 오판 수정**: 원통이면 전부 회전체로 보던 규칙이 "펌프 하우징이 베이스 위에서 회전한다"는
  틀린 관계를 만들었다. 회전 판정에 **감싸임(containment) 또는 좁은 환형 접촉**을 요구하고,
  넓은 평면에 안착한 원통은 체결면으로 보고 고정 판정한다.
- **레퍼런스 자산은 제품 정의가 조인트를 확정**: 기하만으로는 볼트 체결 원통과 저널을 구분할 수 없으므로
  데이터셋에 `mates: {spin, slide}`를 선언하고(실제 CAD 어셈블리의 mate와 같은 역할),
  축·원점·가동범위는 그대로 실측한다. 판정 근거에 "제품 정의(assembly mate)에서 확정"이라고 명시.
- **부모 선택 보정**: 케이블 덕트가 상완의 부모가 되던 문제를 부피비 가중으로 해결.
  자식보다 작은 파트는 구조적 부모가 될 수 없다.
- **관계 정보 재작성**: "고정된다 캐비닛 → 인터록 센서 (55%)" 대신 역할별로 묶어 문장으로 쓴다.
  "임펠러가 하우징 위에서 회전합니다. Y축 중심, 가동 -17°~17°." + 판정 근거 + (낮을 때만) 확인 필요.
  한국어 조사(이/가, 을/를, 와/과)를 받침에 맞춰 처리하고 절차 동사를 한국어로 옮겼다.
- **AI 티 제거**: 파랑·초록 계열 상태 색을 중립 회색으로, em 대시를 마침표·중점으로,
  준비도 막대와 상태 칩을 무채색으로. 검증 콘솔은 웹에서 숨기고(점수 계산에는 계속 사용) 자산 화면에서도 제외.
- **레퍼런스 자산 60종**: 20개 기본 제품에 볼트 서클·냉각 핀·리브·기어 이·포트 보스 같은 실제 형상 특징을 넣고,
  제품마다 대형(치수 1.22배)·고온내식(재질 상향) 파생 2종을 생성. 파생본은 호환·대체·파생 관계와
  단종 BOM 라인을 갖고 있어 카탈로그 검색이 실제 답을 돌려준다.
- **설계 사양 대비 실측 비교**: 컴파일 후 사양서의 치수·파트 수와 실제 결과를 비교해 속성 패널에 표시.
- **전체 버튼 QA**: 워크스페이스 도구·모드·내보내기, 라이브러리 탭·카테고리·검색, 자산 화면 도구·절차·파일 링크를
  모두 클릭 검증. 콘솔 오류 0건, 파생 파일 6종 전부 200 응답.

# (이전) v10 설계 사양서 + 레퍼런스 20종

## v10 — 설계 사양서(blueprint)와 산업 레퍼런스 자산
- **설계 사양서 단계 신설(/api/blueprint)**: 형상 생성 **전에** 설계 AI가 제조용 사양서를 작성한다.
  전체 치수(mm), 파트 분해(최소 7개, 반복 요소 개수), 파트별 형상 파생(스윕/테이퍼/필렛 반경/챔퍼),
  표면 특징(널링·각인·세팅), baseColor/roughness/metalness 채널 분리, 조립 관계(체결 방식), 운동,
  측정/추론 구분. **얕은 응답(파트<7, 치수 누락, 영어 파트명)은 지적 사항을 붙여 1회 재작성 강제.**
  실측: 향수병 사양서 8파트, 나사산 깊이 2mm·구간별 roughness까지 한국어로 출력(96초, 2라운드).
- 사양서는 좌측 프롬프트 패널(#bpBlock)에 실시간 표시되어 사용자가 분해 품질을 직접 판단할 수 있고,
  전체 치수는 메시 스케일 기준으로, 파트명은 /api/semantics의 명명 우선순위로 쓰인다.
- **스케일 안전망**: 생성 메시가 1mm 스펙(작게 나오는 이슈)이 되지 않게 사양서 치수→최장축 스케일,
  범위 밖(5mm 미만/3m 초과)이면 ensurePlausibleScale이 150mm로 복원.
- **자산 상세 뷰어 버그 수정**: GLTFExporter가 어셈블리를 한 겹 감싸 root.children이 1개가 되고
  링크 10개 중 1개만 노드에 연결됐다(분해도·시뮬레이션·파트 선택 전부 무동작의 원인).
  래퍼 해제 + 이름 매칭(공백↔언더스코어 정규화) + 순서 폴백으로 수정. 로봇암 8링크 전부 연결 확인.
- **산업 레퍼런스 데이터셋 20종(seed.html + js/seed-datasets.js)**: 원심펌프·6축 로봇암·자동차 도어·
  컨베이어 셀·EV 배터리 팩·CNC 스핀들·그리퍼 라이브러리·AMR·드론·풍력 기어박스·태양광 트래커·
  공조기·엘리베이터·서버 랙·주입펌프·산업용 CT·세탁기·노트북·유압 실린더·전동공구.
  각각 실치수 mm 형상 → **실제 구조 컴파일러 통과**(링크·조인트·충돌체·질량 실측) → BOM(공급사·단가·
  재고·수명·토크)·구조화 정비 절차(sequence/action/tool/torque/direction/safety)·고장 증상→파트 매핑·
  검사 이력과 함께 등록. 등록 후 상세 화면에서 절차 클릭→파트 강조+방향 화살표 동작.
- 문구 정리: UI·AI 출력에서 em 대시 제거(클라이언트 clean() + 프롬프트 문체 지시), 마케팅 상투구 금지.

# (이전) v9 자산 카탈로그

## v9 — 라이브러리 "자산" 탭 = 3D 제품 카탈로그
생성물(작업)과 **보관할 3D 자산**을 분리했다. 자산은 파일 목록이 아니라 **공간형 제품 카탈로그**다.
- **4계층 분리(asset-store.mjs)** — 한 대에서 돌지만 역할은 나눠 뒀고, 나중에 S3/Postgres/Neo4j/벡터DB로
  바꿔도 스키마가 아니라 어댑터만 교체된다.
  · Object Storage `data/assets/files/<id>/` — STEP·GLB·USD·URDF·MJCF·CIR JSON·썸네일 (`/assets-data/` 로만 노출)
  · Catalog DB `data/assets/<id>.json` — 자산ID·제품/부품번호·BOM·버전·상태·담당·권한
  · Knowledge Graph — 레코드의 `relations[]` (contains/fixed_to/revolute_to/prismatic_to/compatible_with/
    replaces/derived_from/used_in/documented_by) + 자산 간 스캔
  · Search — 텍스트 인덱스 + **형상 서명 코사인 유사도**(컴파일러 실측값 15차원: 스케일·비례·링크수·
    조인트수·질량·조인트 타입 히스토그램·충돌체 히스토그램)
- **검색 3종 동시**: `/api/asset-search`는 자연어 질문을 LLM이 **필터로만 번역**하고(답은 만들지 않음)
  실행은 결정론 코드가 한다. 지원 의도: 이 부품이 쓰인 제품 / 재질 / 정비·교체 필요 / 호환 / 단종 대체 /
  원본 파생 파일 / 일반 텍스트 / 형상 유사.
- **등록(app.html "자산으로 보관")**: 컴파일된 CIR → `buildAssetRecord`가 기본정보·3D데이터·제품구조·
  관계정보·조립정비·비즈니스(BOM)·운영(변경이력/검수)로 변환하고 GLB·USD·URDF·MJCF·STEP·CIR을 함께 업로드.
- **조립법은 영상이 아니라 구조화 데이터**: 측정된 조인트에서 분해/조립 절차를 자동 시드
  (sequence·action·targetPart·tool·torque·direction·instruction·safetyNotice·minutes).
  단계를 누르면 3D에서 **대상 파트 강조 + 분리 방향 화살표 + 공구·토크·주의사항**이 함께 표시된다.
- **asset.html 공간형 상세**: 중앙 3D(파트 선택·분해도·동작 시뮬레이션) / 왼쪽 제품구조·BOM·버전 /
  오른쪽 기본정보·부품정보·관계·**형상 유사 자산** / 하단 절차·파생파일·변경이력·검수.

### v9 검증 (프로덕션 실측)
Pump PX-300을 컴파일→보관: 준비도 90/100, 링크 4, 조인트 3, 6개 포맷(GLB·USD·URDF·MJCF·STEP·CIR) 저장.
AI 명명이 housing/motor/shaft/impeller를 **펌프 베이스·모터 하우징·구동축·임펠러**로 정확히 매핑,
BOM PX300-001~004 자동 채번. 파트 선택 시 치수 실측 일치(모터 Ø64×70mm), 질량 근거 "닫힌 메시 실측".
PX-500 Variant를 얹어 관계·검색 실증:
| 질문 | 해석 | 결과 |
|---|---|---|
| 베어링 교체가 필요한 제품 | maintenance | PX-500 (정비 절차 보유: 프론트 베어링 교체) |
| 스테인리스가 사용된 파트가 있는 제품 | material | PX-500 (stainless 파트: 임펠러) |
| 모터가 들어간 제품 전부 | uses_part | PX-300, PX-500 (모터 하우징 / PX300-002·PX500-002) |
| 단종된 구동축을 대체할 수 있는 자산 | replacement | PX-500 (PX500-003→PX500-003R) |
| 형상 유사 (PX-300 기준) | shape | PX-500 99.3% |
절차 단계 클릭 → 3D에서 대상 파트 강조 + 분리 방향 화살표 + 공구·토크·주의사항 표시 확인.

**v9에서 잡은 버그 4건**:
1. `asset-store.mjs` 내부에서 `assetSummary()`를 호출했는데 모듈 바인딩명은 `summary`라 ReferenceError →
   서버 바깥 catch가 **맨 404로 위장**해 라우트 미스로 오진. 함수명을 통일하고,
   `/api/` 예외는 console.error로 스택을 남기게 수정.
2. 형상 유사도에 코사인을 써서 서로 다른 제품도 100% → **정규화 유클리드(1-rms×2.2)**로 교체(변별력 확보).
3. 재질 검색이 한국어("스테인리스")와 저장 키(`stainless`)를 못 이음 → 재질 동의어 표 추가.
4. 자연어 파서가 비결정적(같은 질문이 text/maintenance로 갈림) → **키워드 보정 폴백**을 서버에 추가해
   0건 사고 방지. (검증 중 PowerShell이 한글 본문을 ISO-8859-1로 보내 서버 버그로 오진할 뻔함 —
   한글 POST 테스트는 `[Text.Encoding]::UTF8.GetBytes()` 필수.)

# (이전) v8 로봇 자산 컴파일러

## v8 — 파라메트릭 CAD → Robot Asset Compiler (알고리즘 전면 교체)
산출물의 정의가 바뀌었다: "보기 좋은 3D"가 아니라 **시뮬레이터가 실행할 수 있는 구조화 자산**.
`Unstructured Geometry → Parts → Assembly Graph → Kinematics → Physics → Executable Asset`
디자인·화면 구조는 유지하고 알고리즘 계층만 교체. **파라미터 슬라이더는 제거**, 편집 대상은 이제 조인트다.

- **js/robot.js (신규, 컴파일러 코어)** — 전부 결정론·실측. LLM은 이름만 짓는다(축·원점·가동범위·질량 접근 금지).
  · Stage 0 진단: 메시 수·연결요소·열린 경계(watertight)·삼각형·권장 파이프라인(cad-preserving / hierarchy-preserving / segment-first)
  · Stage 1 파트 분리: 기존 계층 우선 보존 → 없으면 **정점 용접 union-find 연결요소** → 완전 융합이면
    주축 단면 최소점(neck) 절단. `connected component = part`로 가정하지 않음
  · Stage 2 링크 실측: 부피·CoM·**공분산 기반 관성**(삼각형 적분, 정확), 주축(Jacobi 고유분해), 세장비, 회전대칭 편차(cv)
  · Stage 3 어셈블리 그래프: 접촉 판정(근접 밴드 0.6%·최근접 거리·containment) → 접촉 패치의 평면도/선형도 →
    루트 추론(부피 0.42 + 낮은 위치 0.30 + 연결도 0.28) → **최대 가중 신장트리**로 부모·자식 확정
  · Stage 4 조인트 추론: 회전대칭+동축 → revolute / 레일형(선형도>2.6) → prismatic / 넓은 평면 접촉 → fixed.
    **가동범위는 스윕 실측**(2°씩 회전하며 부모 침입 시작점까지, 전구간 무간섭이면 continuous로 승격).
    항목별 confidence + 판정 근거(evidence) 문자열 + requiresReview 플래그
  · Stage 5 충돌체: 시각/충돌 분리. sphere·cylinder·capsule·box 중 충전율 최소 → 1.55 초과면 **복합 박스 3~4개**.
    ConvexHull 애드온이 없어 convex decomposition 대신 compound primitive 채택(로봇 링크에 더 적합)
  · Stage 6 질량·관성: ρ×V(재질별 밀도 22종), 열린 메시는 status=ai_proposed로 강등·실측 입력 요구
  · Stage 7 검증: 단일 루트/순환/고아 · **Sylvester 판정으로 관성 양의 정부호** · 조인트 전구간 스윕 관통률 · 충돌체 위생
  · Stage 8 Asset Readiness: 8개 서브스코어 가중합 + 검토 필요 항목 목록
- **js/robot-export.js (신규)** — CIR 하나에서 4포맷. 프레임 규약: 모든 링크 프레임은 월드 축정렬 +
  자신을 붙이는 조인트 위치(루트는 원점) → 회전 합성 없이 체인이 정확해짐.
  · **USD(.usda)**: PhysicsArticulationRootAPI / RigidBodyAPI / MassAPI / Revolute·Prismatic·FixedJoint,
    임의 축은 `physics:axis="X"` + localRot 정공법, 각도는 도 단위 변환
  · **URDF**: link/inertial/visual(meshes/*.stl)/collision + joint origin·axis·limit
  · **MJCF**: 중첩 body 재귀 + fullinertia + geom
  · **ZIP 번들**: robot.usda + robot.urdf + robot.xml + asset.json(CIR) + meshes/link_N.stl + README (fflate)
  · 왕복 검증: URDF/MJCF DOMParser 파싱+참조 정합, USD 중괄호·바디 참조·articulation root, GLB 재로딩
- **UI (디자인 유지, 내용 교체)**: "파라미터 (mm)" → **조인트(Kinematics)** 블록(타입 드롭다운·가동 슬라이더·
  신뢰도 배지·판정 근거·검토 필요), 신규 **충돌체·질량** 블록, 신규 **검증 콘솔**(✓/⚠/✕),
  "시안 일치도" → **자산 준비도** + 8항목 브레이크다운, 스테이지 버튼 "파라미터 분석"→**구조 컴파일**,
  "시뮬레이션"→**조인트 스윕**(실측 범위를 왕복하며 관통 검사와 동일한 운동 재생)
- **파이프라인**: 프롬프트 → 시안 렌더 → image-to-3D → 구조 컴파일 / 이미지 → 동일 / 3D·CAD 업로드 → 즉시 컴파일.
  메시 서비스 불가 시에만 카탈로그 아키타입 폴백. `/api/semantics` 신설(이름·재질·온톨로지 전용).
- 온톨로지: base·frame·torso·link·motor·gearbox·actuator·transmission·bearing·gripper·finger·suction_cup·
  tool·camera·lidar·force_torque·imu·shell·panel·protective_cover

### v8 검증 결과 (실측)
**정답(ground truth) 리그 검증** — base 박스(120×20×80) + column 원통(r15 h60) + rail 박스(90×12×14)를
GLB로 내보낸 뒤 다시 컴파일해 정답과 비교. 6.8초:
| 항목 | 정답 | 컴파일러 | 판정 |
|---|---|---|---|
| 링크 수 / 루트 | 3 / base | 3 / base | ✓ |
| 계층 | base→column→rail | base→column, column→rail | ✓ |
| base→column 축·원점 | Y축, 접촉면 y=20 | (0,-1,0), y=20 | ✓ |
| 질량 | 0.518 / 0.114 / 0.041 kg | 동일 (해석해 일치) | ✓ |
| column→rail | 평면 접촉 | prismatic(선형도 2.8) 52% + 검토 필요 | 근거 제시 후 검토 요청 |
준비도 88/100 · 4포맷 왕복 전부 통과 · 조인트 스윕 애니메이션 동작.

**메시 클라우드 향수병 메시(452k tri) 컴파일**: 13.5초, 링크 2 / 조인트 1, 준비도 83/100,
ZIP 번들 9.76MB 생성, 시각 품질은 원본 메시 그대로 유지(파라메트릭 근사로 대체하지 않음).

**검증 과정에서 잡은 실제 버그 4건** (모두 수정):
1. 질량·관성 적분을 **서브샘플된 삼각형**에 돌려 값이 10× 틀림 → 전체 해상도 적분으로 교체
   (총질량 0.134kg → 1.444kg, 정답 리그에서 해석해와 일치 확인)
2. 수밀성(watertight) 판정도 서브샘플에서 계산해 항상 "열림"으로 오판 → 인덱스 버퍼 전체를 걷는
   `openEdgesOfObject`로 분리 (위상은 서브샘플 불가)
3. 표면 샘플이 삼각형 무게중심 1개씩이라 저폴리 박스에서 접촉면을 놓침(정답 리그 계층 오류) →
   **면적 가중 배리센트릭 샘플링**으로 교체, 준비도 73→88
4. 고정 임계 접촉 밴드가 실제 클리어런스를 못 견딤 → 최근접 거리 기반 **적응형 밴드**, 준비도 64→83

**온프레미스 메시 서비스 가동**: `/health` → `{ok:true, engine:"trellis-image-large", device:"NVIDIA H100 80GB HBM3"}`.
설치 중 걸린 문제 3건: tar 추출이 실행권한 제거(→`bash run.sh`), CUDA 확장이 빌드 격리로 torch 미검출
(→`--no-build-isolation`), **transformers 5.x가 torch 2.4에 없는 `DTensor`를 import**(→transformers 4.44.2 핀).
serve.py는 `formats=["mesh"]`만 요청해 가우시안 텍스처 베이크와 무거운 래스터라이저 의존을 제거
(우리 파이프라인은 자체 재질을 입히므로 텍스처가 필요 없음).
4번째 함정: 지연 로딩이 `ModuleNotFoundError: plyfile`을 삼켜서 **로컬이 조용히 메시 클라우드로 폴백**했다.
→ 기동 시 백그라운드 스레드로 가중치를 미리 로드하고 `/health`가 `{ready, loading, error}`를 보고,
server.mjs는 `ready===true`일 때만 로컬로 라우팅(핑 타임아웃 1.5s→4s: CUDA 초기화가 더 오래 걸림).
가중치 로드(약 4GB) 완료 전까지는 메시 클라우드 클라우드로 정상 폴백 — 무중단 우아한 강등.

# (이전) v7 점수 게이트 + 경로 라우터 + 온프레미스 메시

## v7 — 실패를 내보내지 않는 구조
- **점수 게이트**(generate 말미): 검수 점수 ≥80 그대로 · 60~79 제시+고품질 메시 안내 토스트 ·
  **<60 자동 메시 승격**(applyHiFiMesh("promoted"), mScore에 "N/100 → 메시 승격" 표기). 파라메트릭 시도는 라이브러리에 남음.
- **형상 사전 라우터**: MESH_FIRST_RX(마우스·게임패드·이어폰·신발·자동차·헬멧·피규어·동물 등 유기곡면류)가
  프롬프트에 걸리면 프로그램 저작을 시도조차 않고 시안 렌더 → 메시 직행(applyHiFiMesh("routed")).
  메시 서비스 실패 시 기존 파이프라인으로 자동 복귀. 카테고리 아키타입 매칭이 항상 우선.
- **3D 업로드 전면 자동 파라미터화**: GLB/FBX/OBJ/STL도 업로드 즉시 runReverse (기존엔 STEP/IGES만).
  원본 메시는 버리지 않고 **"원본 메시" 토글 버튼(btnOrig, right:404)**으로 파라메트릭 트윈과 전환.
  슬라이더를 움직이면 자동으로 파라메트릭 뷰 복귀. 실측: 업로드→파라미터 9.0초, 원본 452k tri 보존.
- **온프레미스 메시 스택(mesh-svc/)**: TRELLIS(MIT) FastAPI 데몬 :8348 (serve.py/install.sh/run.sh).
  server.mjs /api/mesh3d가 로컬(:8348/health 1.5s 핑) 우선 → 메시 클라우드 폴백. cfg.localMesh.url 오버라이드.
  라이선스 확정: TRELLIS·TRELLIS.2(MIT, H100 3~17초) / 메시 클라우드SG(MIT) / PartCrafter(MIT, 파트 단위 생성)
  / CAD-Recode(포인트→CadQuery 코드) 채택 가능. **Hunyuan3D는 한국 제외 조항으로 사용 불가.**
  GPU 서버 tmux trellis-install에서 설치 진행(H100×8 유휴, 디스크 1.8T 여유).

# (이전) v6 결정론 역설계 + 메시 클라우드 하이브리드

## v6 — 실측 우선 역설계 (CAD-Recode/Point2CAD 패러다임)
근거 리서치: CAD-Recode(ICCV 2025, arxiv 2412.14042), Point2CAD(CVPR 2024, arxiv 2312.04962) —
"기하를 먼저 측정/분할하고, 파라메트릭 코드는 측정값에 맞춘다. LLM이 좌표를 눈대중하게 하지 않는다."
- **js/reverse.js 결정론 합성**: analyzeParts(파트별 **24슬라이스** p90 반경 + 축별 반폭 hx/hz) → RDP decimate
  → buildDraftProgram
  · **원형도(circularity) = mean(min(hx,hz)/r)** — 원형 1.0 / 정사각 0.71 / 평판 0.4.
    ≥0.9면 lathe(측정 프로파일이 곧 프로그램), 미만이면 **loft(실측 라운드-직사각 단면 적층)**, 변화 6% 미만이면 roundedBox.
    이 지표가 기존 median/max 반경 지표의 오판(커버 패널→물병)을 제거.
  · **넥 분할(neckSplit)**: 상단 단면이 최대폭 72% 아래로 좁아지면 body/cap 두 파트로 자동 분할(캡은 어깨 높이에서
    직립 칼라로 시작) → 융합 메시 1개가 편집 가능한 조립체가 되고 cap:unscrew 시뮬레이션이 붙음.
  · 프로파일 양 끝을 실제 bbox로 고정 → 높이 오차 7%→0.3%.
  · 모든 수치는 `totalH*frac`/`mainW*frac` 수식. LLM은 /api/label(flash)로 **이름·재질·소견만** 작성.
- **runReverse 재작성**: LLM 프로그램 작성 경로 제거, 결정론 draft→validate→라벨→스왑.
  실측: STEP 커버 패널 65.0×166.0×26.6mm 정확 재현 10.7초 · 메시 클라우드 향수병 메시 61.6×142.6×91.0 → 60.6×143.0×89.0
  (높이 +0.3%, 폭 -1.6%, 깊이 -2.2%) 2파트(loft 본체 + lathe 캡) 1,520삼각형 14.0초.
- **메시 클라우드 하이브리드 "고품질 메시" 버튼(btnHiFi)**: 시안 이미지 → /api/mesh3d(image-to-model v2, 73초/13MB GLB)
  → GLTF 로드·이전 모델 높이로 스케일·제목/시안/사양 유지 → 역설계 버튼으로 파라미터화(자동 실행하지 않음:
  고품질 메시 자체가 산출물이고, 파라메트릭 근사는 사용자가 필요할 때 선택).
- **설계 도면 → 3D**: 레퍼런스 모달 첫 샘플 "설계 도면"(assets/samples/drawing-part.png, WEB-CAD fixture).
  재현/레퍼런스 두 모드와 인벤토리 프롬프트 모두에 **도면 전사 규칙**(Ø=지름, R, t, ± 무시, 투상도별 3축 대응) +
  **단위 판정 규칙**(표제란 우선, ".19/1.19" 형식이나 전체 5 미만이면 인치로 보고 ×25.4).
  검증 중 실제로 인치 도면을 mm로 오전사(1.2×46×1.0mm)하는 것을 확인 → validateProgram에
  "최대 8mm 미만 / 최소 0.8mm 미만" 판정을 추가해 기존 1회 자동 수리 루프가 단위 오류를 되잡도록 함.
- config.local.json `mesh-cloud.apiKey` (크레딧 63,730). /generated/ 정적 서빙.
- 랜딩(KO/EN)에 "설계 도면·스케치 입력", "3D·CAD 업로드 → 실측 파라미터화" 카드 추가.
- **로그인 차단 버그 수정(중요)**: `crypto.subtle`은 보안 컨텍스트에서만 존재해서 http://61.107.200.148:8347
  같은 평문 오리진에서는 로그인이 "확인 중…"에서 멈췄다. js/auth.js에 순수 JS SHA-256 폴백을 넣고
  (node crypto와 해시 일치 검증) login.html에 try/catch를 추가 → 배포 주소에서 Test7 로그인 성공 확인.
- 배포 스모크(http://61.107.200.148:8347): 랜딩/로그인/앱 200, config.local.json 403,
  카테고리 생성 15.6초·4파트·68.7×172×68.7, STEP 자동 역설계 9.1초·65.0×166.0×26.6 정확,
  내보내기 검증 5종(GLB/STL/OBJ/STEP/DXF) 전부 치수 일치.

## v5.3
- 검수 루프 반복형: 최대 2라운드, pass/score≥80/점수 미개선 시 조기 종료. 각 라운드 [검수 N차 반영] brief 표기.
- 검수자 디테일 상향 의무: mesh 1개 파트에 2차 형상 추가, lathe 프로파일 ≥5점, 수정본 mesh 수 증가 요구.
  배치 수정은 본체 파라미터 수식으로만. 빌드 통계(파트별 mesh 수)를 검수 입력에 포함.

## v5.2
- **비전 검수 루프**: 프리폼 생성 직후 클라이언트가 3D 렌더 캡처 → `/api/review`(설계 AI Pro 비전)가
  시안·렌더를 나란히 보고 0~100 채점(실루엣30/파트30/비례20/재질20) + 불일치 목록 + 수정 프로그램 반환
  → verdict revise면 검증 후 교체, brief에 [검수 반영] 표기. 점수는 모델 패널 "시안 일치도"에 표시, 텔레메트리 review 로깅.
- 정밀도 체크리스트(설계 규칙 6-1): mesh≥파트×2, 전 모서리 라운드, 벽두께≥1.5mm, 분리선 단차, 대칭 짝수, 접점 수식 강제.
- evalExpr 파라미터 대소문자 관용, 역설계 검증 실패 1회 재시도(오류 되먹임) — STEP 자동 역설계 E2E 성공.
- **임시 공개 주소 http://61.107.200.148:8347** (외부 200 확인). 정식 도메인은 Route 53 레코드 대기.

## v5
- **설계 두뇌 = 설계 AI** (config.local.json `primary`): plan=primary-3.1-pro-preview, text=3.6-flash, image=3.1-flash-image.
  `callLLM(tier)` 통합 진입점, 폴백 LLM는 자동 폴백. 상태: /api/status → provider:"primary".
- **프롬프트 심층 분석**: /api/design이 buildRequirements(flash)로 요구사항 시트(명시/추론/렌더프롬프트) 생성 →
  이미지 프롬프트에 반영 + 클라이언트가 /api/plan에 requirements로 전달 → 인벤토리·프로그램의 하드 제약.
- **완성도 게이트**: 인벤토리 파트 수 대비 프로그램 파트 70% 미만이면 누락 목록과 함께 1회 재작성.
- **CAD 업로드**: GLB/FBX/OBJ/STL/STEP/IGES/BREP (vendor/occt WASM 7.4MB, 브라우저 테셀레이션 425ms 실측).
  STEP/IGES는 업로드 즉시 자동 runReverse() → js/reverse.js가 결정론 실측(치수·회전체지수·대칭·반경프로파일·파트) →
  /api/reverse가 실측 재현 파라메트릭 프로그램 작성(치수 창작 금지 ±5%, sims 판정 포함) → 슬라이더 편집.
- 조립 보정: program.js closeAssemblyGaps() — 최대 파트 기준 3축 최단 접촉 스냅.
- DNS 여전히 미등록 (Route 53에 cad A레코드 필요 — 가비아 레코드 무효).

## v4.2: 하이브리드 라우팅 (품질 전략 확정)
- 생성 요청 → `matchArchetypeScored` 키워드 점수 ≥3 이면 **카테고리 템플릿**(아키타입 8종, 조립 품질 보장, ~7초, 시안 생략)
- 미매칭 또는 이미지 첨부 → **프리폼 파이프라인**(시안 렌더→디테일 인벤토리→DSL 프로그램, 40~60초, 목업 품질)
- 프리폼 파라미터는 3~8개로 제한(형상 완성도 우선). 파라메트릭 편집은 두 경로 모두 유지 (실패 원인은 조립이지 파라미터가 아님 — 실측 결론)
- 품질 확장 = 아키타입 추가가 정공법 (Glock 데모도 "전용 어댑터" 사용 명시)

## v4.1: img2threejs 디테일 인벤토리 단계
`/api/plan` 프리폼 경로가 3콜 체인으로 확장: 시안 이미지 → **buildInventory()**((폴백 모델) 비전이
파트별 정체성 특징·비례 목록 작성) → 그 사양서가 프로그램 작성 프롬프트에 주입("여기 없는 요소 창작 금지").
사양서는 응답 `spec`으로 내려가 우측 패널 "AI 확장 설계 사양" 접이식에 표시, 라이브러리에도 저장.

> 세션이 끊겨도 이 파일부터 읽고 이어서 진행합니다.

## v4: img2threejs 방식 프리폼 생성 (기본 경로)
파이프라인: 프롬프트 → `/api/design`(이미지 모델, 구형 폴백)으로 **디자인 시안 렌더 생성**
→ 그 이미지를 `/api/plan`((폴백 모델), PLAN_MODEL)의 비전 입력으로 → **기하 DSL 프로그램**(파라미터 스키마 포함) 작성
→ 브라우저 `js/program.js` 인터프리터가 안전하게 빌드(임의 코드 실행 없음) → 실패 시 1회 AI 수리 → 그래도 실패면 아키타입 폴백.
사용자가 이미지를 직접 첨부하면 시안 생성은 생략.

- DSL 명세: `js/program-spec.js` (서버·클라이언트 공유, three 의존성 없음)
- 프리미티브: box/roundedBox/plate/cylinder/chamferCyl/sphere/capsule/torus/cone/gear/bolt/rib/lathe(끝 자동 닫힘)/loft/gem/**handle**(세로 C고리, 회전 무시, x=본체 반경)
- 파라미터 편집·프롬프트 편집(`/api/edit` kind:"program", 전체 프로그램 재작성)·라이브러리(kind+program 저장)·시뮬레이션(program.sims) 모두 유지
- 커버리지 배너 유지 (프리폼이라 out_of_scope는 유기 형상 정도)

## 검증 상태
- 머그(손잡이): C고리 방향·부착 수정 완료(rotateZ(-arc/2), x=bodyD/2). 이미지 우선 파이프라인 최종 캡처 확인 중 (shots/i1_mug_design.jpg, i2_mug_3d.jpg)
- 자동차: (폴백 모델)에서 차체 로프트+바퀴 생성되나 조립 어색 → 바퀴 배치 예시 개선함, 재검 필요
- 남은 것: 최종 QA(머그·조명·시계), 운영 배포(scp+tmux 재기동), PROGRESS/메모리 갱신

## 실행
로컬: launch.json "vringon-cad" (node server.mjs 8347) · 운영: ssh plushgpu, tmux `vringon`
계정 Test1~20 / test1234 · 키: config.local.json (planModel 미설정 시 (폴백 모델) 기본)

## v14 — 상세 빌드 시트 + 수치 에디터 (2026-07-31)
설계도를 threejs 빌드 시트 형태로 상세화하고, 생성 후에도 각 수치를 직접 고칠 수 있게 했다.

### 빌드 시트 에디터 (`js/app.js` buildSheetUI / scheduleSheetRebuild)
- 파트마다 lathe 프로파일 점을 (반경, 높이) 쌍으로 펼쳐서 편집. 점 삽입(+, 중점 보간)·삭제(×, 최소 3점)
- loft는 단면 {w,d,y,r}, 그 외 프리미티브는 args, 모든 메시에 x/y/z 배치 필드
- 260ms 디바운스 후 rebuild → 실루엣 일치도 재계산 → 라이브러리 저장
- 컴파일된 자산도 계속 편집 가능. 수정하면 CIR을 버리고 구조 컴파일 버튼이 다시 나온다
- 버그: 노출 조건이 `state.kind === "program"`이었는데 컴파일이 kind를 "asset"으로 바꾼 직후라 항상 거짓.
  편집 대상은 모드가 아니라 program의 존재 여부이므로 `state.program`으로 교체

### 치수 게이트 세 축 비교 (`js/program.js` validateProgram)
- 이전에는 최대 치수 하나만 비교해서, 높이만 맞으면 폭이 1.94배여도 통과했다
- 정렬한 세 축을 각각 비교하고, 비율이 2배 근처면 "lathe의 첫 값은 반경" 이라고 원인을 지목한다

### 실측 전 세우기 (`js/measure.js` orientUpright)
- image-to-3D 출력의 자세는 매번 다르다. 같은 병이 한 번은 서서, 다음엔 누워서 나왔다
- 누운 상태로 Y축을 따라 자르면 단면이 길쭉해서 회전체를 "판형"으로 분류하고, 일치도가 26.7%까지 떨어졌다
- 세 축 각각의 단면 원형도를 재서 가장 둥근 축을 회전축으로 잡고 세운다.
  원형 축이 없는 브래킷·판재는 돌리지 않는다(돌리면 추측이 된다)

### 반경/지름 명시 (`js/measure.js` measurementText, `server.mjs`)
- 실측 보고서에 "r은 반경, 지름은 2r"과 "최대 지름 = 전체 폭" 검산식을 넣었다

### 실측 검증 (300ml 화장품 용기, 넓은 캡)
| 항목 | v13 | v14 수정 전 | v14 |
|---|---|---|---|
| 형상 일치도 | 52% | 26.7% (누운 메시) | **82.3%** (정면 72.6 · 측면 84.1 · 평면 90.3) |
| 준비도 | 70/100 | - | **84/100** |
| 프로파일 | 16점 | - | 16점 + 5점 |
- 에디터 왕복: 본체 첫 반경 60.5 → 96.8 수정 시 모델이 193.6×140.6×193.6으로 재생성되고
  일치도가 82.3% → 72.2%로 정확히 나빠졌다. 되돌리면 82.3% 복귀

## v15 — 내측 설계 + 원본 메시 탭 + 다시점 고품질 메시 (2026-07-31)

### 내부 실측 (`js/measure.js` measureInterior)
겉면만 재던 것을 안쪽까지 잰다. 축에서 바깥으로 레이를 쏘면
속이 찬 지점에서 출발한 레이는 표면을 한 번 통과하고, 공동에서 출발한 레이는 내벽·외벽을 차례로 통과한다.
**히트 횟수가 공동 여부를 판정**하고 첫 두 히트 거리가 보어 반경과 벽 두께를 그대로 준다. 12방향 중앙값.
얻는 값: 높이별 보어 반경, 벽 두께 min/중앙/max, 개구부 위치(상/하/밀폐), 실측 용적(ml).

- **THREE.Raycaster를 쓰면 탭이 얼어붙는다.** 레이 하나가 메시의 모든 삼각형을 검사해서
  384레이 × 수십만 삼각형이 메인 스레드를 멈춘다. 실제로 브라우저가 30초 넘게 응답하지 않았다.
  레이가 전부 수평이므로 **삼각형을 높이 슬랩 48개로 버킷팅**하고 자체 Möller-Trumbore로 교차한다.
  백페이스 컬링은 하지 않는다(공동에서 나온 레이는 외벽을 반드시 뒤에서 친다).
- **외곽 프로파일의 슬라이스 목록을 물려받으면 안 된다.** 그 목록은 표면 샘플에서 나와서
  샘플이 적게 떨어진 구간이 통째로 빠진다(lathe 검증에서 1.6mm 다음이 20.3mm였다).
  공동 바닥이 17mm 높게 잡히고 용적의 20%가 사라졌다. 레이캐스트는 샘플이 필요 없으므로
  높이 사다리 64단을 스스로 만든다.
- **정답 대조 검증**(외경30·보어26·높이100·바닥3 셸): 보어 26.0 정확, 벽 4/4/4 정확,
  개구부 top 정확, 바닥 3.9(사다리 간격 1.6mm 이내), **용적 204.1ml vs 이론 205.9ml = 오차 0.9%**.
  속이 찬 원기둥은 null 반환(오검출 없음). 72ms.
- 설계도 규칙 4-1: 공동이 보고된 파트는 lathe 프로파일을 한 붓에 셸로 그린다.
  바닥 중심 → 외벽 위로 → 림 바깥 → 림 안쪽(보어) → 내벽 아래로 → 공동 바닥 → 축.
  LatheGeometry가 그대로 셸을 만들어 주므로 엔진 변경이 필요 없다.
- `validateProgram`에 재료 부피 비교 추가: 실측 대비 2.2배 넘으면 "속을 파지 않았습니다"로 되돌린다.
  치수만 보면 겉이 맞아서 통과하고, 질량으로만 드러난다(용기 본체가 1.94kg으로 나왔던 원인).

### 원본 메시 탭 (`js/app.js`)
코드 경로가 시작되면 재구성 메시를 refForm으로 빼두면서 그냥 버렸다 → 실측의 출처를 볼 방법이 없었다.
이제 `state.origMesh = state.refForm`으로 넘기고 상단 원본 메시 버튼을 노출한다.
`adoptCompiledScene`은 재구성이 있으면 저작 모델로 덮어쓰지 않는다.

### 다시점 고품질 메시 (`js/app.js` captureViews + `server.mjs` mesh-cloudMultiview)
고품질 메시 버튼이 현재 형상을 정면·좌·후·우 4면으로 렌더해서 메시 클라우드 `multiview_to_model`에 보낸다.
단일 이미지는 뒷면을 지어내야 하고 그 추측을 이후 실측이 전부 물려받는다.
오브젝트만 있는 임시 씬(평면 배경, 그리드·바닥 없음)에서 렌더 타겟으로 뽑고 상하 반전해 PNG로 만든다.
`texture_quality: detailed`, `face_limit: 120000`, `pbr: true`. 폴링 창은 4분 → 8분.
메시 클라우드 업로드/폴링/다운로드는 `mesh-cloudUpload`/`mesh-cloudRun`/`mesh-cloudCollect`로 분리해 단일·다시점이 공유한다.

### 용량이 절대 크기를 정한다 (`js/app.js` capacityScale)
image-to-3D 출력은 정규화되어 나오므로 **절대 크기가 임의값**이고 비율만 믿을 수 있다.
이게 내부 설계와 정면 충돌했다: 실측 113.7×150mm 용기는 벽 2mm면 약 1380ml다.
300ml를 만들라는 지시를 받은 저작기는 벽을 9.34mm로 키우고 공동을 8mm 깊이로 줄여서 58.3ml를 만들었다.
프롬프트에 적힌 용량은 고객이 라벨에서 읽는 확정값이므로, 그 값이 크기를 정하고 실측 비율은 유지한다.
원통 근사 π(kR−t)²(kH−2t)=V 를 이분법으로 풀어 k를 구한다. 검산: k=0.614 → 69.8×92.1mm → 정확히 300.0ml.
오작동 방지: 용량이 명시되고 사양서가 공동을 선언했을 때만, k가 0.35~2.5이고 1에서 15% 이상 벗어날 때만 적용한다.
실루엣 IoU는 각 오브젝트를 자기 바운딩 박스로 프레이밍하므로 스케일이 바뀌어도 점수에 영향이 없다.

### 셸이 나오려면 세 곳이 동시에 맞아야 했다
① 실측(공동 검출) ② 설계도(제품군으로 내부 설계) ③ 형상 저작(program-spec 품질기준).
③의 규칙 3이 **"속이 찬 솔리드로 만든다"**여서 셸 지시와 정면으로 충돌했다.
얇은 판재를 막으려고 쓴 문장인데 통짜로 만들라는 지시로 읽혔다.
①②만 고쳤을 때 사양서에는 "내부는 비어 있어야 합니다, 벽 두께 2"가 정확히 적혔는데도 형상은 통짜로 나왔고,
사후 검사가 "속이 빈 제품인데 통짜로 만들어졌습니다"로 잡아냈다.
③까지 고치니 캡 질량 0.726kg → 0.078kg, 본체 1.686kg → 0.893kg으로 떨어졌다.
`validateProgram`에 내용적 게이트 추가: 사양 대비 ±30% 벗어나면 필요한 보어 반경 계산식과 함께 되돌린다.

## v16 — 프로파일 정밀화 + 슬라이더 편집 + 정밀 가공품 자산 (2026-07-31)

### 프로파일을 샘플링에서 레이캐스트로 (`js/measure.js` preciseProfile)
캡 프로파일이 17점인데 값이 37.6 / 37.6 / 37.6 / 37.5 / 37.5 / 37.5 / 37.5였다.
점은 많은데 전부 직선 벽에 찍혀 있고 형상이 꺾이는 곳에는 없었다.
원인은 표면 샘플을 32구간에 넣고 p90을 쓰는 방식이다. 샘플이 떨어진 곳에 점이 생기지 형상이 변하는 곳에 생기지 않는다.
- 240단 방사 레이캐스트로 **정확한 외경**을 재고(마지막 히트 = 최외곽면),
  Douglas-Peucker로 직선 위 점을 버리고, 남은 구간을 이름 붙은 특징으로 판독한다.
- 공차는 최대 치수의 0.35%(0.12~0.6mm)로 스케일에 따라간다.
- **정답 대조**: 챔퍼1×45 → r15 직선30 → 테이퍼 → r18 직선20 → 단차 → r10 직선25 축을 넣으면
  240점을 **8점**으로 줄이면서 "챔퍼 1.3mm×40° / 직선 벽 r15.0 29.6mm / 테이퍼 44.3° 15.1→18.0 /
  직선 벽 r18.0 19.8mm / 단차(축소) 18.0→10.0 / 직선 벽 r10.0 24.7mm"로 전부 복원했다. 60ms.
- 사양서에 좌표 나열 대신 **명명된 특징 + 수치**가 들어간다. 필렛은 원 3점 피팅으로 R값을 낸다.

### 빌드 시트 슬라이더 (`js/app.js` numField)
슬라이더가 기본이고 옆에 숫자 입력이 붙는다. 드래그하면 형상이 즉시 바뀌고 숫자가 따라오며,
숫자를 직접 치면 슬라이더 범위를 넘겨도 min/max를 늘려 받아들인다.
반경과 높이는 프로파일에서 뽑은 각각의 범위를 쓴다. 실측: 슬라이더 80 + 숫자 80 쌍, 48.8→53.8 동기 확인.

### 정밀 가공품 자산 18종 (`js/seed-precision.js`)
인덱스 커플링·하우징·웜/기어/실 샤프트·임펠러·밸브·스크롤·아버·매니폴드·지그 팔레트·
유니버설 조인트·스프로킷·프레스 액시스·캡링·ATC 암·화이트메탈 베어링.
51개 파트 중 43개를 **선삭 프로파일 [반경,높이]** 로 저작해서 챔퍼·단차·언더컷·실 홈이 캡션이 아니라 기하다.
`seed-run.js`에 lathe 프리미티브를 추가했다(기존은 box/cyl/tube뿐이라 자산이 뭉툭했다).
등록 후 GLB에서 되읽으면 저작 치수가 그대로 나온다: `[14,40] [17,43]` → "테이퍼 42.4° 14.0→17.0".
전체 78건 덮어쓰기 재등록, 실패 0건.

### 이번에 드러난 버그
- `/api/mesh3d`가 기본 1MB 본문 제한이라 4면 렌더(약 520KB×)가 잘려서 `{}`로 파싱됐다.
  잘림이 조용히 빈 요청처럼 보였다 → 기본 16MB로 올리고, 한도 초과는 던져서 이유를 남긴다.
- `library.html`의 카테고리 트리가 하드코딩 목록을 순회해서 새 카테고리를 통째로 빠뜨렸다
  (사이드바 합계 60, 실제 78). 데이터에서 카테고리를 뽑고 CATS는 순서만 정하도록 바꿨다.
- 자산 정렬이 최신순이라 먼저 등록한 정밀 부품이 맨 아래였다 → 정밀 가공품을 앞으로.
- **용량 게이트를 차단으로 걸었더니 결과가 나빠졌다.** 기하학적으로 멀쩡한 프로그램이 두 번 반려되면
  일반 카탈로그 형상으로 떨어지는데, 그게 실루엣 41%였고 반려된 프로그램은 66%였다.
  용량은 품질 수치이지 빌드 실패가 아니므로 조언으로 바꾸고 빌드 시트에서 조정하게 했다.
- 원격에서 `pgrep -f "node server.mjs"`는 **자기 ssh 커맨드 문자열까지 매치한다**. PID가 매번 바뀌어
  프로세스가 살아 있는 것처럼 보였지만 실제로는 죽어 있었다.

## v17 — 원본 메시 유지 구조화 + 자산 사양서 30종 + 썸네일 복구 (2026-07-31)

### 표현 전략 판정 (`js/threespec.js`)
파이프라인이 고품질 3D를 만든 뒤 **버리고** 프리미티브로 다시 모델링하고 있었다.
파라미터 슬라이더를 붙이려고 원본 메시를 lathe/box로 갈아치운 것이 "구현이 부족해진" 원인이다.
이제 파트마다 회전체·각형 프리미티브를 실제로 맞춰보고 잔차를 재서 결정한다.
오차 2% 이하 → PARAMETRIC, 5% 이하 → HYBRID, 그 외 → SOURCE_MESH(원본 유지).
- **정규화를 최대 치수로 하면 안 된다**: r14×210 축이 "각형 2.1%"로 나왔다. 잔차는 길이 대비 작고
  단면 대비 크며, 프리미티브가 틀리는 곳은 단면이다. 세 치수의 기하평균으로 정규화한다.
- **캡 제외 밴드도 파트 높이 기준**이어야 한다. 최대 치수 기준이면 납작한 디스크의 캡이 제외되지 않아
  회전체 오차가 49%로 잡힌다.
- 정답 대조 5종 전부 일치: 원기둥·디스크·구 → 회전체, 정육면체·판재 → 각형.

### ThreeJsSpecification
제품 설명문이 아니라 런타임 실행 사양. 소스 자산(포맷·단위·좌표계·바운딩박스·삼각형),
파트별 표현 전략과 프리미티브 적합도 실측치, 편집 가능 항목, 조인트, 검증 허용오차.
모든 값이 SourceStatus(MESH_MEASURED / INFERRED_FROM_DOMAIN 등)를 가진다.
의미 분석은 text 티어(Flash) → **plan 티어(설계 AI 3.1 Pro)** 로 올렸다.

### 자산 30종 실행 사양서 (`spec.html`, `js/spec-run.js`)
저장된 GLB를 직접 열어 사양서를 쓴다(이미지가 아니라 실제 3D를 본다는 원칙).
5개 카테고리에 라운드로빈으로 30건, 파트 224개. 결과: 원본 메시 108 / 파라메트릭 51 / 하이브리드 65.
자산 상세 화면에 "실행 사양서" 탭 추가.

### 이번에 고친 버그
- **썸네일이 비어 보이던 원인은 프레이밍이 아니라 NaN이었다.** `autoDetail(name, semantic, kind, size)`가
  lathe 파트에서 `size`로 **프로파일 배열**을 받는다. 내부 기본값이 `size[0]`을 숫자로 쓰므로 배열이 들어가
  NaN 정점이 생기고, NaN 파트 하나가 모델 전체 바운딩박스를 NaN으로 만들어 카메라가 무효화된다.
  명시적 detail을 준 파트(스프로킷·커플링)만 살아남은 이유가 이것. lathe는 [최대반경, 높이]를 파생해 넘긴다.
  NaN 지오메트리는 이제 콘솔에 남기고 맨 프리미티브로 폴백한다. 78건 중 빈 썸네일 0건.
- 카메라 프레이밍도 실제로 2.2배 가까웠다(FOV로 거리 계산하지 않고 대각선×0.62를 썼다). 함께 수정.
- **모든 생성을 구조 컴파일로 보내면 안 된다**: 180k 삼각형 메시에서 몇 분씩 걸리고 화면이 자산 모드로
  전환돼 "생성하면 자산 등록으로 넘어간다"로 보였다. 메시는 유지하되 사양서만 쓰고,
  구조 컴파일은 다시 버튼으로 되돌렸다. 조기 return 경로가 busy 해제를 놓쳐 버튼이 잠겨 있던 것도 수정.
- 사양서의 파트 삼각형 수가 **서브샘플 값**이었다(180k를 22.5k로 표기). 실제 버퍼에서 센다.
- GLB 루트 이름 "world"가 파트명으로 나오던 것 → 제품명으로 대체.
- TRELLIS가 마칭큐브 격자 그대로 90만~100만 면을 반환한다 → 서버에서 quadric decimation 18만 면.
  `simplify_quadric_decimation`은 `fast_simplification` 패키지가 필요하고, trimesh 4.x는 face_count를
  **키워드로** 받는다(위치 인자는 0~1 비율로 읽혀 예외).
- 원격에서 `pkill -f uvicorn`은 **공유 서버의 다른 사용자 서비스까지 매칭**한다. 포트로 PID를 특정할 것.

### STEP 내보내기 크기 가드
STEP은 삼각형마다 CARTESIAN_POINT·VERTEX·EDGE·ADVANCED_FACE를 쓴다(약 800바이트/삼각형).
원본 메시를 유지하는 구조로 바뀌면서 18만 삼각형 결과가 148MB STEP이 됐고 일반 CAD에서 열리지 않는다.
스트라이드로 삼각형을 솎으면 솔리드가 찢어지므로, 6만 삼각형을 넘으면 예상 크기를 알리고
폴리곤 최적화에서 감축 후 내보내도록 안내한다(진행 여부는 사용자 선택).
취소 경로가 try 뒤의 `btn.disabled = false`를 건너뛰어 버튼이 잠기던 것도 함께 처리.

### 자산 재시딩 순서 주의
`seed.html` 덮어쓰기 등록은 자산 레코드를 통째로 다시 쓰므로 `specification` 필드가 지워진다.
썸네일·형상을 다시 굽고 나서 `spec.html`을 돌려야 한다(반대 순서로 하면 사양서가 사라진다).

## v18 — 이미지 기반 사양서 + 메시 온디맨드 (2026-08-01)

### 생성 흐름을 뒤집었다
전: 프롬프트 → 시안 → **3D 생성(2~5분)** → 실측 → 사양서. 결과는 융합 바디 1개, 편집 불가.
후: 프롬프트 → 시안 → **이미지에서 사양서 분석(약 60초)**. 3D는 버튼을 눌러야 만든다.
- `/api/specify` (설계 AI 3.1 Pro): 이미지에서 4계층을 읽는다.
  물리 바디 / 의미 영역 / 엔지니어링 피처 / 편집 그룹.
- 원본 메시 버튼 → 온프레미스 TRELLIS (실측 42초). 고품질 메시 버튼 → 메시 클라우드(`engine:"mesh-cloud"`).
  서버가 engine을 존중하지 않으면 두 버튼이 같은 결과를 내므로 `wantsCloud` 분기를 넣었다.
- **실측: 사양서 61초, 메시 42초.** 이전 단일 경로 330~360초.

### 사양서 보강 (피드백 반영)
- **목표 치수와 실측 바운딩박스를 분리**한다. 한 필드에 섞으면 어느 쪽이 근거인지 알 수 없고
  편집 시 잘못된 비율로 리스케일된다. `targetDimensions`(USER_PROVIDED) / `measuredBounds`(MESH_MEASURED).
- **축 의미를 명시**한다(X=좌우 폭 등). width/height/depth 이름만으로는 메시 축과 안전하게 연결되지 않는다.
- **파트 1개가 아니라 바디 1개 + 의미 영역 n개**. 일체형 브래킷은 물리적으로 하나지만
  기능 구역은 여럿이다. 영역을 바디로 쪼개면 물리 모델이 틀린다.
- **홀·컷아웃·리브·필렛은 영역이 아니라 피처**로 간다. 볼트 6개는 CIRCULAR_HOLE_PATTERN 하나에
  count/pcd/diameter로.
- **검증 점수를 분리**한다. 원본 메시를 그대로 쓰면 형상 충실도는 당연히 100%이고 구조 이해도는
  아무것도 말해주지 않는다. 구조 이해도(영역·피처·편집그룹·편집가능 파라미터)를 따로 낸다.
- 좌우 대칭은 mirrorOf → MIRROR 제약조건으로.

### 되돌린 것
사양서로 프리미티브를 다시 조립해 3D를 만드는 경로를 넣었다가 제거했다.
이미지에서 추정한 수치로 BOX/CYLINDER를 쌓는 것은 여러 번 거부된 방식이고,
메시 생성기보다 나쁜 형상이 나온다. 사양서는 문서이고 3D는 메시 생성기가 만든다.

### 조명
three r162에는 `scene.environmentIntensity`가 없다(r163부터). 금속은 환경 반사가 거의 전부라
어두운 스튜디오에서 조명을 아무리 더해도 검게 나온다. 유일한 실효 조절점은 재질별
`envMapIntensity`(금속 2.4 → 3.4), 노출 1.28 → 1.62, 배경 0x0c0c10 → 0x15161c, 앰비언트 추가.

### 디스크
검증 중 `ENOSPC`로 메시 생성이 실패했다. 20TB 볼륨이 100% 찼고 `/data`가 5.7TB다(공유 인프라).
`generated/`의 재생성 가능한 임시 메시 33개를 정리해 451MB를 확보하고 통과시켰다.
근본 해결은 서버 소유자의 용량 확보다.

## v19 — Source-Mesh-Preserving 구조화·편집 아키텍처 (2026-08-01)

### 제거한 잘못된 로직
- **이미지 추정 수치로 프리미티브를 조립하던 경로**(`specFromAnalysis` → `compileSpec`).
  제품 카테고리에서 폭·높이·두께를 만들어 BOX/CYLINDER로 원본을 대체하는 방식이었다.
  명령서 1장 금지 사항 1~10에 해당한다.
- `INFERRED_FROM_DOMAIN` 출처의 파라미터. 이제 허용 출처는 네 가지뿐이고
  CATEGORY_FALLBACK / CATALOG_GUESS / GENERIC_DEFAULT / LLM_ASSUMED_DIMENSION은 스키마에 없다.
- 대상 없는 슬라이더. 파라미터는 target과 구현된 operation이 있을 때만 생성된다.

### 추가한 스키마 (`js/model-spec.js`)
네 계층을 분리한다. PhysicalBody(연결된 강체) / AssemblyPart(분리 근거가 있는 부품) /
SemanticRegion(바디 내부 기능 구역) / EngineeringFeature(검출된 홀·보어).
- `detectRegions`: 위치+법선 k-means. **단일 메시에서 복수 영역**이 나온다(요구사항 6).
  영역은 절대 별도 강체로 승격되지 않는다.
- `detectBores`: 슬랩별로 **오목한 면만 골라** 원 피팅. 처음엔 후보 전체를 한꺼번에 피팅해서
  바깥 벽(r 40~60)과 보어(r 10)가 섞여 실제 관통홀을 놓쳤다.
  법선이 단면 중심을 향하는지로 오목/볼록을 가르는 것이 핵심이었다.
- `rebuildEligibility`: 표면 커버리지 95% · 피처 재현율 95% · 실루엣 IoU 97% · 바운딩박스 1%.
  미통과 시 SOURCE_MESH_LOCKED 유지, 통과해도 사용자 승인 후 파생 자산으로만.

### 비파괴 Modifier (`js/modifier.js`)
원본 정점 버퍼를 `geometry.userData`에 보관하고 매번 거기서 다시 계산한다.
영역 편집은 박스 거리 기반 smoothstep 가중치로 경계를 부드럽게 유지한다.
`runOperation`은 연산별로 필요한 것만 건드린다 — GLOBAL_TRANSFORM은 Object3D만,
REGION_OFFSET/SCALE은 해당 영역 정점만. **파라미터 변경이 전체 재생성을 뜻하지 않는다.**
구현 불가한 HOLE_DIAMETER는 조용히 무시하지 않고 "국부 불리언 필요"로 명시하고 사양서에 기록한다.

### 정답 대조 검증 (120×10×80 판재 + Ø20 관통홀)
| 항목 | 결과 |
|---|---|
| 보어 검출 | **Ø19.99** (실제 Ø20), 신뢰도 0.96 |
| 홀 없는 동일 판재 | 검출 0건, "홀 직경 편집" 미지원으로 표시 |
| 단일 메시 → 의미 영역 | 4개 |
| 파라미터 출처 | 전부 MESH_MEASURED / FEATURE_DETECTED |
| 대상 없는 파라미터 | **0개** |
| 영역 돌출 6mm | 변형 409 정점 / **보존 611 정점** |
| 0으로 복원 | **드리프트 0** (비트 동일) |

### UI
빌드 시트를 교체했다. 표현 방식(원본 보호 ON) · 구조 트리(바디/영역/피처, 클릭 시 3D 강조) ·
지원 편집(대상·방식·근거·신뢰도 표시) · **지원하지 않는 편집(사유 명시)** · 파라메트릭 재구축(고급, 게이트 표시).

### 남은 제한
- 홀 직경 편집은 브라우저에 불리언 커널이 없어 사양서 기록까지만 한다.
- 조인트는 근거 부족 시 UNKNOWN 유지가 원칙이나 아직 검출기를 붙이지 않았다.
- 다중 뷰 Face ID 역투영(2D 세그멘테이션 융합)은 PHASE 2로 남았다.

## v20 — 이미지(폴백 LLM) → 사양서(설계 AI) → Three.js 코드 (2026-08-01)

### "어떤 프롬프트든 같은 결과" 조사 결과
`/api/design`과 `/api/specify`를 프롬프트별로 직접 호출해 비교했다. **둘 다 정상**이다.
- 이미지: 길이가 다름(598591 vs 616231) → 서로 다른 이미지
- 사양서: 시계/텀블러/풀리/용기가 각각 다른 영역·피처·치수로 나옴
같아 보였던 원인은 내가 넣었던 **이미지 추정 수치 → 프리미티브 조립** 경로였다.
어떤 제품이든 BOX·CYLINDER를 쌓으니 결과가 비슷해 보였다. 그 경로는 제거했다.

### 역할 분리
- **이미지 생성: 폴백 LLM** (`gpt-image-1`) 우선, 설계 AI는 폴백.
- **사양서 작성: 설계 AI 3.1 Pro.** 이미지는 깨끗한 제품 샷이면 되고 판독이 어려운 쪽이다.

### 사양서를 Three.js 코드 기준으로 다시 씀 (`SPECIFY_SYSTEM_V2`)
"이 문서만 읽고 코드를 짠다"를 전제로 한다. 영역마다 builder + mm 수치 + center(절대좌표) +
codeHint(코드 작성 지시 한두 문장) + material(three 표준) + repeat(개수·배치 규칙)을 요구한다.
FREEFORM 빌더를 추가해 프리미티브로 안 되는 형상을 솔직히 표시한다.
제품 정체성은 사용자 요청이 기준이고, 이름은 전부 한국어로 쓴다.

### 사양서 → 코드 (`js/spec-to-code.js`)
`generateThreeCode`가 읽을 수 있는 Three.js 소스를 쓰고 `buildFromAnalysis`가 같은 규칙으로 형상을 만든다.
보이는 것과 복사해 가는 것이 같아서, 사양서의 빈틈이 산문에 숨지 않고 틀린 형상으로 바로 드러난다.
빌드 시트를 코드 에디터로 교체했다(적용·복사). `runThreeCode`는 좁은 스코프에서 실행하고
실패하면 뷰포트를 비우지 않고 오류만 보고한다.

### QA에서 잡은 실제 버그
- **중복 const**: 홀이 2개인 영역에서 `const regionhubHole`이 두 번 선언돼 적용 시 문법 오류.
  피처 인덱스로 이름을 구분하게 고쳤다. (적용 버튼이 이 오류를 정확히 보고한 덕에 발견)
- **함수명 `buildV`**: 한글 제품명이 ASCII로 걸러지며 한 글자만 남았다. 접미사 3자 미만이면 buildModel.
- **영어 이름**: "V-Belt Pulley"로 나오던 것 → 규칙 11로 한국어 강제. 확인: "V벨트 풀리 · 동력 전달 부품,
  영역 중앙 보스/연결 웹/바깥 림".

### 실측 검증
| 항목 | 결과 |
|---|---|
| 프롬프트 3종 사양서 | 전부 다른 영역·빌더·피처 (풀리 REVOLVE+5홀, 용기 5영역 165mm) |
| 전체 생성 | 95초 · 영역 3 → 파트 6 · 코드 52줄 |
| 코드 파싱 | 3케이스 전부 OK, 반복 배치 스포크 5개 정확 |
| 코드 편집 왕복 | 프로파일 반경 ×1.4 → **180 → 252mm** (높이 50 불변) |
| 콘솔 오류 | 0건 |

### 남은 제한
- codeHint가 키홈 같은 비원형 컷을 지시해도 생성기는 원형 홀만 뚫는다. 힌트와 코드가 어긋난다.
- FREEFORM은 라운드 박스로 대체되며 codeHint로만 의도를 남긴다.

## v21 — 사양서 7개 항목 필수화 (2026-08-01)

기존 구조는 그대로 두고 설계 AI 사양서에 일곱 가지를 필수로 추가했다.

1. **정확한 단면 설계** — `outerProfile` / `innerProfile`을 **따로**, 직선 점이 아니라
   LINE·ARC(radius, sweep)·BEZIER(control1/2) 세그먼트로. 각 세그먼트에 continuity.
2. **곡률 규격** — `curvature`에 바닥 필렛 R, 어깨 R, 상단 R, 측벽 테이퍼 각, 최소 연속성(G1/G2).
3. **용량 검증** — `capacity`에 내부 유효 체적·정격 충전·충전선 높이·헤드스페이스·오버플로.
4. **파트 인터페이스** — `interfaces`에 목 외경/내경, 캡 내경, 체결 깊이, 간극,
   **닫힘 이음선 폭**(0.5~1.5mm 권장 범위를 벗어나면 표시), 체결 방식, 나사 피치.
5. **제조 디테일** — `manufacturing`에 공정, 최소 벽 두께, 드래프트 각, 파팅라인,
   바닥 리세스, 게이트 허용 영역, 공차 등급.
6. **디자인 디테일** — `designDetails`에 라벨 영역, 로고면, 인쇄 영역, 색상 대비, 바닥 링, 미세 단차.
7. **렌더링 사양** — `rendering`에 초점거리, 기준 시점, HDRI, 키라이트, 접지 그림자, 배경, 컬러 스페이스.

### 코드가 사양을 검산한다
`revolveVolume`이 내부 프로파일을 절두원뿔로 적분해 `capacity`와 대조하고, 15% 이상 벌어지면
생성 코드 주석과 사양서 양쪽에 경고를 남긴다. 실측에서 **사양 320mL vs 계산 264.3mL(83%)** 를 잡아냈다.
`tessellate`가 ARC를 현·반경에서 중심을 구해 실제 곡선으로 펴고, 내부 프로파일이 있으면
외부를 올라갔다 내부를 되짚어 내려오는 한 붓 폴리라인으로 **진짜 셸**을 만든다.
`setupView`로 카메라 초점거리·톤매핑·조명까지 코드에 나온다.

### innerProfile을 빼먹는 문제
프롬프트 규칙만으로는 계속 건너뛰었다. 두 단계로 강제했다.
- `specifyGaps`가 누락을 목록으로 만들고 **한 번 보완 요청**을 보낸다(그래도 안 되면 gaps로 반환).
- 그래도 부족해서 스키마 `required`에 `outerProfile`·`innerProfile`을 넣었다.
  속이 찬 파트는 `[]`로 명시하게 되므로, 용기가 비워두면 그 자체가 오류로 드러난다.
  적용 후 gaps 0건, 몸통 외부 6 / 내부 5 세그먼트.

### 실측
| 항목 | 결과 |
|---|---|
| gaps | 0건 |
| 코드 | 119줄, 파싱 OK, 메시 3 |
| 용량 검산 | 사양 320mL vs 계산 264.3mL → 불일치 경고 |
| 이음선 | 1mm (적정 범위 표시) |
| 제조 | 최소 벽 2mm · 드래프트 1.5° · ISO 2768-m |
| 렌더링 | 85mm · 쿼터뷰 · setupView 생성 |

## v22 — 관측 → 레시피 → 결정론 빌더 (CAD IR) (2026-08-02)

LLM이 형상을 코딩하던 구조를 버렸다. 이제 역할이 셋으로 갈린다.
이미지 → **관측**(설계 AI: 정규화 좌표 곡선 + 출처 + 신뢰도) → **레시피 선택**(코드: 필수 관측
충족 검사) → **결정론 빌더**(코드: 같은 IR이면 항상 같은 형상) → **재렌더링 검증**(원본 이미지
실루엣 IoU + 제한된 종횡비 보정).

### 새 모듈
- `js/recipes.js` — 기본 16 + 피처 7 + 조립 5 레시피. 각각 필수/선택 관측, 필수 파라미터,
  지오메트리 그래프, 검증기, 산출 등급을 계약으로 갖는다. `recipeReadiness`가 충족을 검사해
  미충족이면 **거부**한다(발명하지 않는다). 곡선 없는 자동차 도어 레시피가 거부되는 것을 확인.
- `js/ir-build.js` — IR 실행기. 실루엣 bbox → 목표 치수 매핑 프레임 하나를 모든 곡선이 공유.
  revolved_container는 내부 프로파일이 관측 안 되면(닫힌 용기에선 관측 불가능) 벽 두께
  **규칙으로 유도**해 셸을 만든다. 미충족 레시피는 시각 복제로 강등하고 사유를 남긴다.
- `/api/observe` — 관측 전용. 곡선은 visible_contour / inferred_from_shading /
  inferred_from_category 출처와 신뢰도(추론은 ≤0.6)를 반드시 단다.

### 산출 등급을 정직하게
Visual Replica / Editable Design Approximation / Manufacturing CAD 3단계.
단일 이미지 결과는 최대 2단계까지만. LOFT 곡면 빌더가 없는 레시피(도어·신발 등)는
자동으로 시각 복제 등급으로 표시된다.

### QA에서 잡은 결함 3건
1. **출처 사칭**: 이미지 없는 요청에서 곡선이 visible_contour 0.9를 주장 →
   서버 가드가 이미지 부재 시 관측 출처를 제품군 추론(≤0.55)으로 강등.
2. **필수/선택 관측 미구분**: repeat_features(스포크) 누락만으로 커버리지 89% 풀리가
   자유형으로 강등됐다. 피처성 관측을 optional로 분리 → 풀리가 100% 충족, 설계 근사 등급.
   용기의 inner_revolve_profile도 같은 이유로 optional(닫힌 용기에선 관측 불가).
3. **보정기가 사용자와 싸움**: 높이 편집 직후 재검증의 종횡비 보정이 X/Z를 반토막냈다
   (정면 촬영 풀리의 원형 실루엣에 측면 렌더를 맞추려 함). 보정 폭을 0.75~1.33으로 제한하고
   **사용자 편집 후에는 점수만 매기고 보정하지 않는다.**

### 실측 (V벨트 풀리, 외경 180mm)
| 항목 | 결과 |
|---|---|
| 레시피 선택 | rotational_mechanical 충족 100% (후보 4개 순위 표시) |
| 산출 | 85초 · 파트 2(본체+보어) · 설계 근사 등급 |
| 원본 이미지 실루엣 IoU | 78% (제한 보정 후; 무제한 보정 시절 58%) |
| 파라미터 편집 | 높이 180→270: Y 정확히 ×1.5, X/Z 불변 (재보정 안 함) |
| 결정론성 | 같은 IR 3회 빌드 → 비트 동일 치수 |
| 합성 IR 정답 대조 | 60×150×60 정확, 도어(곡선 없음) → 시각 복제 강등 |

---

## v13 — 4단계 스텝 데모, 설계 AI 3.1 Pro JSON 사양서

한 번 누르면 끝까지 달리던 생성을 네 단계로 끊었다. 사양서가 파는 물건인데
지나쳐 버리면 읽고, 반대하고, 고칠 자리가 없다.

| 단계 | 하는 일 | 멈추는 이유 |
|---|---|---|
| 1 디자인 데이터 | 프롬프트 → 시안 이미지 (이미지 업로드면 건너뜀) | 시안이 마음에 들어야 다음이 의미 있다 |
| 2 메시 · 텍스처 | 시안 → 3D 메시. 원본으로 보존 | 이후 SOURCE_MESH 지정의 근거 |
| 3 JSON 사양서 | 카테고리 라우팅 → Core 사양서 | **사용자가 JSON을 직접 고친다** |
| 4 Three.js CAD | 사양서의 geometry 블록만 읽고 형상 생성 | 하단 프롬프트로 사양을 고쳐 재생성 |

상단 중앙 스텝퍼가 진행을 보여주고 다음으로만 넘어간다. 건너뛰기는 막았다.

### 사양서 API를 둘로 쪼갠 이유
문과 항아리와 반지를 한 스키마로 받으면 매번 대부분이 비어서 돌아온다.
`/api/spec-route`가 카테고리와 레시피만 정하고, `/api/spec-json`이 그 계약에 맞춰 Core 사양서를 쓴다.
둘 다 설계 AI 3.1 Pro에 `thinking_level: high`, 온도는 모델 기본값(가이드 권고),
XML 태그로 구획한 System Instruction, responseSchema로 구조 강제.

### JSON이 곧 CAD 입력
`parts[].geometry`가 4단계가 실제로 실행하는 유일한 필드다.
builder · size_mm · center_mm · outer_profile · inner_profile · repeat.
프로파일은 LINE/ARC/BEZIER 세그먼트라 어깨 곡률이 코너로 뭉개지지 않는다.
JSON 패널에서 수치를 고치면 그대로 형상이 바뀐다. 설명만 있고 못 만드는 필드는 없다.

### 스키마가 보장하지 않는 것을 검사하는 후처리 (`validateSpec`)
Structured Outputs는 모양만 보장하지 말이 되는지는 보장하지 않는다.
- 없는 part_id를 가리키는 parent/material/relationship, 계층 순환
- 스케일 기준이 없는데 mm 절대치수를 쓴 dimension
- representation_strategy와 builder 불일치, 프로파일 없는 REVOLVE
- 회전 단면이 축(반경 0)에서 시작하지 않음 → 바닥이 뚫린다
- 내부 공동이 있는데 바깥 단면이 상단에서 닫힘 → 열리지 않는 통짜
- radius 없는 ARC, 현의 절반보다 작은 radius, sweep 없는 ARC
- MULTI_PART_ASSEMBLY인데 파트 1개
- 스키마 enum 밖의 representation_strategy·builder 값
오류는 필드 경로와 고칠 방법이 붙은 목록으로 모델에 되돌려 한 번 수리한다.
자유 서술 "고쳐줘"보다 이쪽이 실제로 고쳐진다.

### 메시와 CAD는 대체가 아니라 병존
처음엔 사양서가 자유곡면 파트에 SOURCE_MESH를 지정할 수 있게 뒀다. 결과가 나빴다.
2단계 메시는 파트 경계가 없는 덩어리 하나라 두 파트가 나눠 가질 수 없고, 모델은 계속
용기와 캡 양쪽에 같은 메시를 지정했다 — 한쪽은 빈 파트가 됐다. 규칙을 붙여 막으려 했지만
수리 라운드도 메시 보존을 고집했다.

그래서 사양서 어휘에서 SOURCE_MESH와 ORIGINAL_MESH를 아예 뺐다. 메시를 버리는 게 아니다.
메시는 장면에 그대로 남아 원본 메시로 볼 수 있고, 바운딩박스는 절대 스케일 기준이 된다.
4단계는 CAD를 만드는 자리고, 둘은 서로를 대신하는 대신 나란히 놓인다.
이 결정을 되돌리면 캡이 두 번 생기거나 빈 파트가 나온다.

### 출처 사칭 가드
이미지가 없는 요청인데 IMAGE_OBSERVED가 달려 오면 CATEGORY_DEFAULT로 강등하고
신뢰도를 0.5 이하로 낮춘다. 근거는 주장할 수 있으면 근거가 아니다.

### QA에서 잡은 결함
1. **EXTRUDE_2D를 회전시킴** — `buildGeometry`가 outerProfile이 있으면 무조건 선반을 돌려
   120×80×6mm L 브래킷이 240mm 원반이 됐다. builder를 명시적으로 존중하도록 고치고,
   임의 외곽선 압출과 구멍 뚫기를 구현했다. 이제 120×80×6mm.
2. **파트가 2.5mm 떠 있음** — 선반은 자기 프로파일 범위로 재중심화하는데 center_mm은
   size_mm 기준이라, 100mm로 선언된 파트의 프로파일이 95mm면 그만큼 떴다.
   조립이 신경 쓰는 건 바닥이므로 바닥을 맞추도록 바꿨다.
3. **유리가 불투명** — material_class GLASS인데 MeshStandardMaterial에 투명도가 전달되지
   않아 흰 플라스틱으로 보였다.
4. **내부 단면 방향을 모델에 맡김** — 셸은 바깥 위로 안쪽 아래로 잇는 폐곡선인데
   작성 방향이 반대면 벽이 접혔다. 어느 쪽 끝이 림에 닿는지로 코드가 정한다.
5. **스텝퍼와 모델 버튼이 같은 줄을 다툼** — 상단 우측 버튼 5개를 스텝퍼 아래로 내렸고,
   `다음` 버튼 라벨이 세로로 깨지던 것을 nowrap으로 고쳤다.
6. **size_mm을 고쳐도 회전체가 안 변함** — 프로파일이 형상을 정하므로 맞는 동작이지만
   편집이 무시된 것처럼 보였다. 프로파일 실제 높이와 size_mm이 어긋나면 빌드 기록에 적는다.

### 단계는 눌러야 진행한다
1단계 결과가 나오면 거기서 멈춘다. `다음`을 눌러야 그 다음 단계가 생성된다.
스텝퍼 아래에 다음에 무엇이 만들어지는지 한 줄로 적어 둬서, 어딘가에서 이미
돌고 있는 건지 기다릴 필요가 없게 했다. 건너뛰기는 막았다.

### 확인
- `tools/spec-build-check.mjs` — 저장한 사양서를 브라우저 없이 4단계 빌더로 컴파일한다.
  형상 회귀가 빈 화면이 아니라 숫자로 나온다.
- 300ml 유리 용기 + 알루미늄 캡: 파트 2개, GLASS/METAL USER_PROVIDED,
  용기 80×95×80 바닥 y=0, 캡 y 95..115에 적층, 2560 삼각형, 검증 오류 0.
- L 브래킷 120×80×6mm, M8 4공 PCD60: 외곽선 압출 + 구멍 4개, 510 삼각형, 검증 오류 0.

---

## v14 — 3단계로 재편, 단계마다 받을 수 있는 것이 다르다

4단계에서 디자인 시안과 메시를 한 단계로 합쳤다. 시안은 3D를 만들기 위한 중간물이지
따로 승인받을 산출물이 아니었다.

| 단계 | 들어오는 것 | 하는 일 | 받을 수 있는 것 |
|---|---|---|---|
| 1 3D 생성 | 프롬프트 | 시안을 그린 뒤 3D | 메시 (GLB · STL · OBJ) |
| | 이미지 | 바로 3D | 파트별 재질·텍스처 변경 |
| | 3D 파일 | 그대로 뷰어에 | |
| 2 JSON 사양서 | 1단계 결과 | 카테고리 라우팅 → Core 사양서 | JSON 직접 편집 + 하단 프롬프트 |
| 3 Three.js CAD | 사양서 | geometry 블록으로 형상 생성 | CAD (STEP · DXF) · 메시 · 선택 파트별 |

### 단계마다 다운로드가 다른 이유
1단계 결과는 삼각형 덩어리다. 거기에 STEP 버튼을 띄우면 파일이 약속하지 않는 것을
약속하게 된다. CAD 형식과 파트별 내보내기는 3단계에서 사양서로 솔리드를 만든 뒤에 열린다.
`applyStepUI()`가 단계에 따라 다운로드 그룹과 범위 선택을 켜고 끈다.

### 3단계 수정은 2단계로 돌아가지 않는다
하단 프롬프트는 언제나 사양서를 먼저 고친다. 메시를 직접 건드리면 사양서가 화면에 없는
것을 설명하게 되고, 내보내기와 CAD가 전부 사양서에서 나오므로 그게 곧 거짓말이 된다.
3단계에서는 그 수정이 뒤에서 일어나고 새 형상이 제자리에 나타난다. 스텝퍼는 움직이지 않는다.
2단계에서 같은 프롬프트를 쓰면 사양서만 고치고 멈춘다. 아직 형상이 없기 때문이다.

### 업로드한 3D
1단계의 산출물이 이미 있는 셈이므로 파이프라인이 1단계 완료 상태로 열린다.
예전처럼 자동으로 구조 컴파일에 들어가지 않는다. 그건 몇 분이 걸리고 작업창을
자산 모드로 바꿔 버려서, 버튼 뒤에 그대로 둔다.

### thinking_level이 조용히 무시되고 있었다
`generationConfig.thinkingLevel`은 존재하지 않는 필드다. 올바른 위치는
`generationConfig.thinkingConfig.thinkingLevel`이다. 잘못 보낸 요청은 400으로 거절됐고,
`callLLM`의 fallback이 그걸 삼키고 폴백 LLM (폴백 모델)로 넘어갔다. 로그가 없어서
설계 AI 3.1 Pro를 쓴다고 화면에 적혀 있는 동안 실제로는 (폴백 모델)가 사양서를 쓰고 있었다.
이제 fallback이 이유를 찍는다. 조용한 폴백은 폴백이 아니라 은폐다.

고친 뒤 같은 요청에서 프로파일 세그먼트가 3/2에서 4/4로 늘고 검증 오류가 0이 됐다.

### 출력 예산은 생각도 쓴다
설계 AI의 thinking 토큰은 maxOutputTokens에서 나간다. high thinking + 14000으로는
사양서가 배열 중간에서 잘려 파싱 불가 JSON으로 돌아왔다. 32000으로 올렸다.
하단 프롬프트의 수정은 이미 맞는 문서를 고치는 대화형 작업이라 low thinking에
90초 예산으로 따로 뒀다 — 35초에 끝난다. 사양서 생성은 240초.

### 타임아웃이 없었다
LLM 호출에 마감이 없어서, 응답이 멈추면 버튼이 "…" 상태로 영원히 남았다.
느린 기능이 아니라 고장 난 기능으로 읽힌다. 모든 호출에 마감을 붙였다.

### 회전축이 지평선 위에 묶여 있었다
`controls.maxPolarAngle = Math.PI * 0.495`. 카메라가 수평보다 아주 조금 위에서 멈춰서
제품 바닥면을 볼 수 없었다. 바닥 리세스, 게이트 자국, 바닥 두께가 전부 그 면에 있다.
제한을 없애 0~π 전 범위로 돌린다.

바닥에서 올려다보면 그리드와 그림자 평면이 눈과 부품 사이를 가로막는다.
카메라가 타깃보다 아래로 내려가면 둘 다 감춘다. 툴바의 그리드 토글은 위쪽 절반에서
그대로 유효하다 (`gridWanted`). `forceRender`도 같은 처리를 거치게 했다 —
애니메이션 루프를 건너뛰는 경로만 바닥이 남아 있으면 QA가 거짓말을 한다.

### 설계 AI 3.1 Pro 점검 도구
`node tools/primary-check.mjs`. 응답의 `modelVersion`을 그대로 찍으므로 실제로 어떤 모델이
답했는지 확인된다. thinkingConfig low/high, 구조화 출력, 그리고 예전에 조용히 실패하던
최상위 `thinkingLevel`을 회귀 가드로 함께 친다.

현재 결과: 3/4 통과 (4번째는 실패해야 정상). 응답 모델 primary-3.1-pro-preview,
thinking 토큰이 `thoughtsTokenCount`로 따로 보고된다 — 출력 예산을 함께 쓴다는 근거.

## v15 — 좌표 밀도가 형상 품질이다

사양서가 만들어 내던 단면은 직선 2~6개였다. 방향이 바뀌는 모든 지점이 90도로 각졌고,
캡은 직선 두 개짜리 맨 원통이었다. 재질을 아무리 손봐도 깡통은 깡통이다.

규칙 3-1~3-5를 좌표 중심으로 다시 썼다. 핵심은 세 가지다.
- 넣어야 할 곳과 통상값을 숫자로 준다 (바닥 R1.5~3, 어깨 R8~20, 림 모따기 C0.5~1, 드래프트 0.5~2도)
- 세그먼트 하한을 준다 (본체 8, 작은 파트 5)
- **좌표 예시를 통째로 준다.** 200ml 용기의 outer/inner 프로파일 13줄. 이게 제일 크게 먹혔다.

검증기도 붙였다. 지시만 하고 확인하지 않으면 지켜지지 않는다.
- `PROFILE_TOO_COARSE` — 세그먼트가 하한 미만
- `PROFILE_ALL_STRAIGHT` — ARC도 BEZIER도 하나 없음

수리 라운드를 2회로 늘렸다. 반경을 채워 넣으면 좌표가 바뀌므로 그 다음에 size_mm 정합
검사에 걸리는 일이 잦다. 개선이 멈추면 그 자리에서 끊고 더 나은 쪽을 남긴다.

같은 300ml 용기 요청 기준:

| | 이전 | 이후 |
|---|---|---|
| 본체 외곽 세그먼트 | 4 (전부 직선) | 8 (R3 바닥 · R8 어깨 · BEZIER 목 · R1.5 림) |
| 캡 외곽 세그먼트 | 2 (전부 직선) | 5 (R5 · R1.5) |
| 삼각형 | 3,584 | 14,848 |

## GitHub Pages 정적 데모

https://jhkim1543.github.io/vringon-cad/

1·2단계는 API 키와 서버가 필요해 정적 호스팅에 올릴 수 없다. 3단계는 순수 클라이언트
코드라 그대로 돈다. 그래서 서버에서 미리 쓴 사양서를 넣고 좌표를 직접 고치면 형상이
바뀌는 부분만 시연한다. 컴파일러(`js/spec-cad.js`)는 운영 데모와 같은 파일이라
따로 관리할 사본이 아니다.

## v16 — 공개 데모에 시작점과 라이브러리

정적 데모가 3단계 결과에 바로 떨어지고 처음으로 돌아갈 방법이 없었다. 제품이 아니라
결과물 한 장이었다.

- 왼쪽 컴포저(프롬프트 + 예시 칩 + 생성)와 빈 스테이지에서 시작한다.
- 스텝퍼가 1 → 2 → 3을 걸어간다. 1·2단계는 API 키가 필요해 서버에서 미리 실행한
  결과를 쓰고, **각 단계 옆에 "서버에서 미리 실행"인지 "이 브라우저에서 실행"인지 적는다.**
  준비된 결과가 라이브 결과인 척하게 두지 않는다.
- 라이브러리 화면. 카드 미리보기는 저장된 이미지가 아니라 **지금 이 브라우저에서 사양서를
  컴파일해 그린다.** 저장 이미지는 사양서가 바뀐 뒤에도 옛 형상을 계속 보여준다.
- 처음으로 버튼이 전부 되돌린다.

`tools/build-samples.mjs`가 샘플 사양서를 서버에 대고 미리 만든다. 하나에 1~4분씩 드는
유료 호출이라 이미 있는 것은 건너뛴다. `--force`로 다시 만든다. 6종을 넣었다.

### 새로 시작이 아무것도 안 하고 있었다
`btnNew`가 첫 줄에서 `$("refNote").textContent`를 읽었는데 그 엘리먼트는 오래전에
마크업에서 빠졌다. null 참조로 예외가 나고 그 뒤 전부가 실행되지 않았다. 프로젝트명도,
프롬프트 비우기도, 모델 정리도. 조용히 죽는 핸들러는 없는 기능보다 나쁘다.
겸사겸사 파이프라인 상태(스텝퍼·JSON 패널·프롬프트 바·다운로드 게이트)도 함께 되돌린다.

### 사양서가 선언한 원점을 컴파일러가 지키게 했다
파트가 바닥 아래로 85mm까지 가라앉았다. `center_mm`을 파트 중심으로 쓸지 밑면으로 쓸지가
사양서마다 갈렸기 때문이다. 파트마다 추측하는 대신, 완성된 조립체를 사양서가
`coordinate_system.origin`으로 선언한 위치에 맞춘다. 파트 사이 상대 위치는 그대로 둔다.

## v17 — 드론 특화 서비스로 전환

범용 제품 데모를 드론 설계 서비스로 좁혔다. 좁힌 만큼 사양서가 깊어진다.

### 분류가 입력보다 먼저다
Domain(용도 영역) × Mission(임무) × Platform(기체 형상)을 별도 축으로 두고
입력 전에 고른다. 하나의 "전투용/비전투용" 분류로는 실제 설계 차이(정찰 고정익과
살포 헥사의 파트 구조 차이)를 표현할 수 없다. 사용자가 고른 분류는 사양서의
classification에 그대로 고정되고, 모델이 바꾸면 서버가 되돌린다.

방산 도메인은 정찰·통신·수송만 받는다. 무장·표적·투하 관련 파트와 파라미터는
시스템 프롬프트 수준에서 차단했고, KINETIC 계열은 임무 목록에 넣지 않았다.

### 드론 사양서 스키마
기존 parts[].geometry 계약(그대로 Three.js가 되는 유일한 블록)은 유지하고
드론 블록을 더했다:
- classification — 5개 도메인 × 13개 임무 × 6개 플랫폼
- size_performance — MTOW·자체중량·탑재량·휠베이스. 형상으로 증명 못 하는 값은
  null + USER_REQUIRED. 이미지에서 모터 정격을 "추론"하면 그건 발명이다.
- parameters[] — 슬라이더가 될 값. affects에 움직일 part_id를 적는다.
- external_classifications — KR 조종자 증명 구분은 모델 의견이 아니라 시행규칙
  표이므로 MTOW에서 서버가 결정론적으로 계산한다.
- assurance.airworthiness_claim — 항상 false로 강제.

QUAD_X 실측: repeat CIRCULAR r225(=휠베이스 절반) 일치, 로터 Ø254 <
450×sin45°=318 겹침 없음, 점검 임무 짐벌·줌카메라 포함, 검증 오류 0.

### 사양서가 중앙에 온다
2단계가 끝나면 뷰포트 중앙에 사양서 시트가 열린다. 일반/JSON 탭은 같은 JSON의
두 표현이고 일반 탭은 결정론적 렌더링이라 두 탭이 다른 이야기를 할 수 없다.
(?) 버튼에 Three.js 사양서 작성법(빌더 종류·프로파일 좌표·repeat·파라미터 연쇄)을 넣었다.

### 내보내기 폴리곤 단계
하이/중간/로우(감축 0%/45%/72%)를 내보내기 시점에 클론에 적용한다. 뷰포트 모델은
다운로드에 절대 건드려지지 않는다. 알고리즘은 three.js SimplifyModifier(quadric
edge collapse) — 이미 vendor에 있는 검증된 것을 쓰지, 새 의존성을 들이지 않는다.
mergeVertices로 토폴로지를 붙인 뒤 감축해야 unindexed soup에서 무너지지 않는다.

지원 형식: GLB · STL · OBJ · STEP · DXF · USD · URDF · MJCF.
FBX는 three.js 생태계에 익스포터가 없어 제외 — 없는 것을 있다고 하지 않는다.

### 예시가 프롬프트에서 이미지로
프롬프트 칩을 빼고 드론 이미지 4종(점검 쿼드·농업 헥사·측량 고정익·수색 VTOL)을
넣었다. 이 서비스는 이미지를 먼저 읽으므로 예시도 이미지여야 한다. 누르면 이미지
첨부와 함께 분류 3축이 같이 설정된다 — 예시는 맥락째로 온다.

## v18 — 파트별 파라메트릭 편집 · 시뮬레이션 · 고정익 방향

### 파트가 나뉘는가, 파트별로 고칠 수 있는가 (분석 결과)
나뉜다 — 사양서의 파트가 각각 별도 메시로 컴파일되고 repeat 인스턴스는 regionId로
사양 파트를 공유한다. 고칠 수 없었다 — 편집 경로가 JSON 원문과 AI 프롬프트뿐이었다.
그래서 결정론 편집 계층(js/drone-params.js)을 만들었다:
- 드론 파라미터: 사양서 parameters[]가 그대로 슬라이더가 된다. 휠베이스를 돌리면
  암·모터·로터·프로펠러 가드 링이 함께 움직인다(가드가 빠져 로터만 밖으로 나가는
  버그를 QA에서 잡음). 값은 USER_PROVIDED로 기록된다.
- 파트 편집: 뷰포트나 파트 목록에서 파트를 고르면 W·H·D·중심높이(반복 파트는
  배치 반경·수까지) 필드가 열리고, 수정은 사양 JSON에 쓰인 뒤 재컴파일된다.
  치수를 바꾸면 프로파일 좌표도 같은 비율로 따라가 서버 정합 검사가 깨지지 않는다.
LLM 호출 없음 — 슬라이더가 한 일이 곧 JSON에 적힌 일이다.

### 4단계 시뮬레이션
로터 회전(로터/프로펠러 이름 매칭, 암·마운트·가드 제외), 각본형 이륙→호버→선회→착륙,
단일 강체 낙하 테스트(반발 계수 0.38), 돌풍 슬라이더(기울임+드리프트).
설계 검사(각 행이 수식을 표기):
- 로터 디스크 간격 (로컬 지오메트리 기준 — 회전 AABB √2 팽창 아티팩트 수정)
- 디스크 로딩 MTOW/(nπr²)
- 호버 파워: 운동량이론 P=n·T^1.5/√(2ρA)÷FM(0.7) — 1차 근사임을 행에 명시
- 배터리 Wh 입력 → 예상 호버 시간, 탑재 만재 시 파워 증가율
- 무게중심 vs 랜딩기어 지지면 (균일 밀도 근사)
6-DoF·SITL·CFD는 모터·공력 데이터가 필요해 다음 단계로 명시.

### 고정익이 세로 판이 되던 문제
EXTRUDE_2D가 정면(XY) 평면 고정이라 날개 평면형을 그릴 수 없었다. geometry.plane
(FRONT/TOP/SIDE)을 추가해 날개는 TOP(평면형, 두께 상하), 수직미익은 SIDE로 눕히고
압출 축도 평면을 따라간다. WING_NOT_TOP_PLANE·VSTAB_NOT_SIDE_PLANE 검증기 추가.
isr-wing이 995×1100(세로 판)에서 1800×274×1158(요청 날개폭 정합)로 재생성됐다.

### Pages 데모 UX
단계가 순식간에 넘어가 결과가 안 보인다는 지적: 1단계가 3D를 즉시 표시(실제 메시 클라우드
메시는 서버에 있고 여기서는 사양 형상 미리보기임을 라벨로 명시), 2단계는 JSON 패널로
스크롤+하이라이트, 단계마다 최소 표시 시간. 예시가 프롬프트 텍스트에서 드론 이미지
카드로, 설명 문단은 두 문장으로. 심독 CSS가 docs에 복사되지 않아 스텝퍼와 겹치던
것도 수정(공유 CSS는 고칠 때 docs/css로 같이 복사할 것).

## v19 — 단계형 진행 버튼 · 사양 심화 · 부품 라이브러리 · 고장 시뮬 · 3D 프린팅

### 진행 UX
스텝퍼의 "다음"을 없앴다. 진행은 우측 하단의 단일 버튼이고 그 라벨이 다음 단계의
이름이다: 메시 3D 생성 → 정밀 3D 설계 사양서 생성하기 → 코드 베이스 3D 생성 →
시뮬레이션 실행 → 3D 프린팅·다운로드. "다음"은 누르면 무엇이 생기고 얼마가 드는지
말해주지 않았다. 상단 스텝퍼는 현재 위치 표시와 뒤로 이동만 담당한다.
Pages 헤더의 공개 데모 태그·소스 버튼 제거.

### 사양서 품질 분석과 심화
분석 결과의 공백: 내부 부품(배터리·FC·ESC·GNSS)이 파트로 없었고, 파라미터가 2~3개,
파트 6~11개, 부품 실물 참조 없음. 고친 것:
- 플랫폼별 필수 파트 로스터 검증기(REQUIRED_PART_MISSING) — 멀티로터 8종·고정익 7종.
  빠지면 수리 라운드가 채운다. 내부 부품은 실제 위치(center_mm)에 배치 의무.
- PARAMS_TOO_FEW — 형상을 바꾸는 파라미터 최소 6개.
- 파트 상한 16→26. 바디는 셸 권장(분해 뷰에서 내부가 보여야 한다).
- 재생성 실측: 점검 쿼드가 9→12파트(하부쉘·덮개·배터리·FC 스택·GNSS 마스트·ESC 포함),
  파라미터 2→6개, 검증 0건.
함정: component_ref를 응답 스키마에 넣자 설계 AI가 "invalid argument"로 요청 자체를
거부했다(스키마 크기 한도). 모델이 쓸 필요 없는 클라이언트 전용 필드는 스키마에서
빼고 패치 후 서버가 재병합한다.

### 부품 라이브러리 (js/drone-catalog.js)
배터리 6종·모터 5종·FC/ESC 4종·센서·탑재 9종 — 공개 스펙 기반 근사 중량과 치수.
선택하면 맞는 파트의 치수·중량이 바뀌고, 맞는 파트가 없으면 새 파트로 추가된다
(바디 기준 휴리스틱 배치 후 파트 편집으로 조정). 배터리를 고르면 Wh가 시뮬레이션
배터리 입력에 자동 반영. 질량 합계는 "부분합"으로만 표기 — 프레임·배선 없이
전체 중량인 척하지 않는다.

### 시뮬레이션 확장 (연구 근거)
- 로터 1개 고장: 쿼드는 요 제어를 포기하고 회전 하강(문헌의 표준 대응 — Mueller &
  D'Andrea, ETH/Berkeley), 헥사·옥토는 추력 재분배로 성능 저하 유지(파워 ×(n/(n-1))^1.5),
  고정익은 활공. 설계 검사에 판정 행 + 시뮬레이션 재생 버튼.
- 탑재 중량 슬라이더: 디스크 로딩·호버 파워가 실시간 재계산.
- 자유낙하 시간 √(2h/g) 표기.

### 5단계 3D 프린팅 (js/print-estimate.js)
부피는 삼각형 서명 사면체 적분(진짜 값), 시간은 근사(라벨 명시): 침적 부피 =
솔리드 × (0.30 외벽 + 0.70 × 인필). 프린터 3종(Bambu X1C·Prusa MK4S·Ender-3 V3 SE)의
지속 유량 클래스와 재료 5종(PLA·PETG·ABS·ASA·TPU) 밀도·속도 계수. 빌드 볼륨 초과 시
필요 축소 배율 표시. 점검 쿼드 실측: 2989cm³ · PLA 1501g · 약 24시간 · 1/2.7 축소 필요.

### 분해·조립
Pages에 분해하기/조립하기 토글(중심 기준 방사형 오프셋) 추가. 앱은 기존 분해 버튼 유지.
내부 파트가 생겼으니 분해 뷰가 실제로 내부 구성을 보여준다.

### 라이브러리가 드론을 저장하지 않고 있었다
`saveToLibrary`가 `state.archetype || state.program` 없으면 조기 반환했다. 드론 실행은
둘 다 없어서(정체성이 사양서다) 사용자가 만든 드론이 페이지를 떠나면 사라졌다.
`pipe.spec`이 있으면 저장하고 `droneSpec`을 항목에 담는다. 복원은 같은 컴파일러로
사양서를 다시 빌드하므로 저장한 그 드론이 그대로 열린다.
연쇄로 나온 두 버그: (1) 명명 저장 후 libId가 남아 다음 실행의 자동 저장이 그 항목을
덮어써서 **모든 저장본이 다음 드론의 사양서를 들고 있었다**, (2) 새 사양서를 올려도
직전 제목이 따라와 자동 저장이 엉뚱한 이름을 달았다. 저장은 스냅샷이므로 저장 후
정체성을 끊고, 새 사양서는 제목·libId를 초기화한다.

### QA 하네스
`tools/qa-samples.mjs` — 번들된 사양서 전부를 컴파일→시뮬(호버·로터고장·낙하)→
파라미터→파트 필드→부품 배정→프린팅 추정까지 헤드리스로 통과시킨다. 5/5 통과.
라이브러리에 5종(교량 점검 쿼드·농업 살포 헥사·측량 고정익·배송 X8 옥토·방산 정찰 고정익)
저장 완료, 열기까지 확인.

## v20 — 6자유도 비행 물리

각본형 프리뷰를 실제 적분으로 대체했다. 추력 계수·관성 텐서·제어기가 주어지면 운동이
방정식에서 나오고, 로터 고장은 **결과**가 된다 — 실패하는 애니메이션이 아니라 실제로 실패한다.

모델: T = kT·ω², Q = kQ·ω², 1차 모터 지연, 축별 2차 항력, 파트 박스의 평행축 정리로
얻은 관성 텐서, 자세 P → 각속도 PD → 최소자승 배분 → 로터별 ω(최대치 포화).
블레이드 요소 이론·유입 지연·지면 효과는 없다. "제대로 된 이유로 숫자가 움직이는" 프리뷰다.

### 구현하며 잡은 물리 버그 넷 (전부 헤드리스로 재현·확인)
1. **모멘트 부호 반전.** r × F를 직관으로 적어 M.x = a.z·T, M.z = −a.x·T로 뒀는데
   실제 전개는 M = (−a.z·T, 0, a.x·T)다. 정지 호버에서는 보정이 0이라 멀쩡해 보이고,
   외란이 들어오는 순간 모든 자세 보정이 양의 되먹임이 되어 뒤집혔다.
2. **기울기 보상 폭주.** collective ÷ max(0.15, cos θ)라 80°에서 호버의 7배 추력을
   요구했다. 뒤집힘이 로켓이 됐다. 0.5로 바꿔 60° 넘으면 고도를 못 지킨다는 사실이
   숨겨지지 않게 했다.
3. **기저 추력의 모멘트.** 로터가 CG에 대칭이 아니면 균일 추력 자체가 모멘트를 만든다
   (짐벌은 앞, 배터리는 뒤). 이걸 빼주지 않아 제어기가 기울여서 트림했고 호버가 32°로 섰다.
4. **축 독립 배분.** 축마다 자기 arm² 합으로 나누는 방식은 양축 대칭일 때만 옳다.
   로터 하나 빠진 육각형은 대칭이 아니라 교차 결합을 놓쳐 헥사가 뒤집혔다.
   B·t = M의 최소노름해 t = Bᵀ(BBᵀ)⁻¹M로 바꿨다.

### 고장 시 축 우선순위
잔여 로터가 세 축을 동시에 못 낼 때 셋을 같이 줄이면, 애초에 불가능한 방위 요구 때문에
롤·피치까지 잃는다. 문헌의 답은 요를 버리고 자세를 지키는 것이라 요는 남는 여유만 받는다.
또 로터가 빠지면 차동 성분의 합이 0이 아니라 총추력을 갉아먹으므로 base를 총추력에 대해
다시 푼다.

### 결과 (로터 1개 정지, 10초)
| 기종 | 결과 | 요 권한 |
|---|---|---|
| 쿼드 (4) | 0.6초 추락 | 75%→상실 |
| 헥사 (6) | 고도 유지, 요 11°/s 표류 | 0% (요 포기) |
| 옥토 X8 (8) | 고도 유지 | 100% |
문헌과 일치한다 — 쿼드는 요 제어를 포기하는 것이 연구된 대응이고, 헥사는 과구동이지만
요가 제어 불가능해진다. 8m/s 돌풍에서 세 기종 모두 0.3m 이내로 위치를 유지한다.

설계 검사에 추력 여유(최대 ω / 호버 ω)²와 관성 텐서 대각 성분이 추가됐다 — 경험칙이 아니라
실제 계수와 질량에서 나온 값이다. 점검 쿼드 실측 1.28배(1.8배 미만이라 경고).

### 하이브리드 VTOL 사양서가 계속 실패한 이유 두 가지
1. **Node의 `server.requestTimeout` 기본 300초.** 클라이언트에는 맨 `fetch failed`로만
   보이고 서버 로그에는 아무것도 남지 않는다. 다른 기종은 300초 안에 끝나서 멀쩡했고
   가장 복잡한 VTOL만 정확히 301초에 죽었다. 15분으로 올렸다 — 실사용자에게도
   똑같이 터질 버그였다.
2. **출력 토큰 부족.** 고정익 + 리프트 붐 + 두 추진계 + 내부 부품을 담으니 32000을
   넘겨 응답이 44,685자에서 잘렸고 JSON 파싱이 실패했다. thinking 토큰도 같은 예산에서
   나간다. 사양서 티어를 56000으로 올렸다.

### VTOL 실패의 세 번째이자 진짜 마지막 원인
서버 타임아웃과 토큰 예산을 고친 뒤에도 실패가 남았다. 계측해 보니 한 번은 269초에
성공하고 다음은 **301초에 실패**했다 — Node의 fetch(undici)가 응답 헤더를 300초까지만
기다리고 그 이상은 맨 `fetch failed`로 뱉는다. 브라우저 fetch에는 없는 제한이라
실사용자는 겪지 않고 빌드 도구만 겪는다. `tools/build-samples.mjs`의 요청을
`node:http`로 바꿨다. VTOL이 437초에 15파트·검증 0건으로 완성됐다.

같은 증상("fetch failed")에 원인이 셋이었다: 서버 requestTimeout 300초, 출력 토큰
32000 초과로 인한 응답 절단, 클라이언트 headersTimeout 300초. 하나씩 계측해서 벗겨내야
했고, 로그에 아무것도 남기지 않는 실패는 계측 없이는 추측만 낳는다.

### 최종 샘플 6종 (헤드리스 QA 6/6)
| 샘플 | 파트 | 로터 | 로터 1개 고장 | 프린트 (X1C·PLA) |
|---|---|---|---|---|
| 교량 점검 쿼드 | 25 | 4 | 요 제어 상실 | 15.5h / 0.9kg |
| 농업 살포 헥사 | 38 | 6 | 유지 (파워 +31%) | 94h / 6.4kg |
| 배송 X8 옥토 | 29 | 8 | 유지 (파워 +22%) | 369h / 26kg |
| 수색구조 VTOL | 23 | 5 | 유지 (파워 +40%) | 295h / 21kg |
| 측량 고정익 | 11 | 1 | 활공 | 64h / 4.5kg |
| 방산 정찰 고정익 | 11 | 1 | 활공 | 397h / 28kg |

### 공개 데모 1단계가 실제 고품질 메시를 띄운다
사용자가 "이미지에서 mesh-cloud로 3D 생성하는 거 있는거야?"라고 물어서 확인해 보니, 온프렘은
진짜로 붙어 있었지만 공개 데모는 사양서를 컴파일한 형상을 띄워 놓고 안내 문구는 "서버에서
메시 클라우드로 만든 3D"라고 단언하고 있었다. 대역을 실제 출력으로 내보이는 셈이었다.

사진이 있는 샘플 4종을 온프렘 `/api/mesh3d`(engine:mesh-cloud)로 한 번씩 돌려 GLB를 받아
`docs/assets/meshes/`에 커밋했다. 57~79초, 3~12MB. `tools/build-meshes.mjs`는 이미 있는
파일을 건너뛰므로 재실행해도 재과금되지 않는다.

메시 클라우드 GLB는 단위가 없는 ~1.0 크기로 온다. 사양서가 선언한 최장 치수에 맞춰 스케일하지
않으면 카메라가 보이지도 않는 점에 맞춰진다. 그 최장 치수를 구할 때 `size_mm`이 배열이
아니라 `{w,h,d}` 객체라 `for...of`가 "object is not iterable"로 죽었다. 링 배치 파트는
자기 center에 반경이 안 적혀 있어서 wheelbase·wingspan을 하한으로 함께 쓴다.

결과로 단계별 서사가 선명해졌다.

| 단계 | 파트 | 삼각형 | 치수 mm |
|---|---|---|---|
| 1 고품질 메시 | 1 (덩어리) | 410,534 | 541 × 471 × 720 |
| 3 Three.js CAD | 25 | 23,640 | 700 × 185 × 700 |

메시는 파트 경계가 없는 한 덩어리고 CAD는 25개로 갈라진다. 이게 이 제품이 파는 차이다.

### CAD가 이미지와 딴판이던 원인 — 배치 컴파일러 버그 3개 + 장님 사양서
사용자가 "JSON으로 3D를 만들면 이미지와 너무 다르다"고 했다. sar-vtol을 헤드리스로
컴파일해 파트별 월드 좌표를 찍어보니 사양서가 아니라 컴파일러가 범인이었다.

1. **MIRROR_PAIR가 spacing_mm을 버렸다.** 자기 center.x(0)를 부호 반전만 해서
   리프트 붐 2개·착륙 스키드 2개가 전부 (0,·,·)에 포개져 동체 속으로 사라졌다.
2. **CIRCULAR 각도 규약이 반대였다.** 사양서의 start_angle_deg 35.26°는
   atan(424/600) — 붐 끝 (±424,±600)을 정확히 의도한 나침반 각(+z 기준)인데,
   컴파일러는 수학 각(+x 기준)으로 읽어 로터가 허공에 떴다. 게다가 count 4를
   원둘레 90° 간격으로 찍으면 정사각형만 나온다 — 직사각 배치(848×1200)의
   모서리는 원 위에서 90° 간격이 아니다. count 4는 (±r·sin a, ±r·cos a)
   양축 대칭으로 바꿨다. 정사각(45°)은 두 방식이 같아 기존 멀티콥터는 불변.
3. **CYLINDER가 plane을 무시했다.** 순항 프로펠러(plane FRONT)가 수평으로 누워
   다섯 번째 리프트 로터처럼 보였다. 축=plane 법선으로 고치니, 기존 샘플 3종의
   로터가 FRONT로 잘못 라벨돼 있던 것이 드러나 TOP으로 데이터 수정
   (컴파일러가 무시하던 시절엔 티가 안 났다). 검증기 LIFT_ROTOR_NOT_TOP 추가.

수평미익이 주익과 같은 z에 있어 묻히는 건 사양서 저작 오류 —
EMPENNAGE_INSIDE_WING 검증기를 추가했다.

수정 후 sar-vtol: 붐 ±424, 로터 4개가 정확히 붐 끝 (±424,±600), 스키드 ±424,
추진 프로펠러 수직(400×400×15). QA 6/6 유지.

### 유사성의 상한: 사양서가 이미지를 본 적이 없다
서버는 imageB64를 받게 돼 있고 워크스페이스도 보내는데, 라이브러리 샘플 6종을
만든 배치 빌더만 {prompt, drone}으로 불렀다. 게다가 라이브 경로도 meshInfo
(삼각형 수 + 바운딩박스 숫자 3개)뿐 — 모델이 1단계 3D의 형상을 본 적이 없다.

- 서버: /api/spec-json이 views{front,left,back,right}를 받아 "이 렌더와 어긋나는
  사양서는 틀린 사양서다"라는 지시와 함께 설계 AI에 넣는다.
- 워크스페이스: pipeStep2가 captureViews(state.pipeMesh) 4면 렌더를 전송
  (메시 클라우드 멀티뷰용으로 있던 기계를 재사용).
- 배치 빌더: 샘플 사진을 imageB64로 전송, id 인자로 특정 샘플만 --force 재생성.
- 프롬프트에 배치 규약 문서화(나침반 각, MIRROR_PAIR spacing, CYLINDER 축).

### 프로펠러가 원반이던 문제 — 디스크 데이터에서 블레이드를 만든다
사용자 지적: "프로펠러도 그냥 원으로 표현하는데 이미지 속 프로펠러랑 다르다."
디스크는 프롬프트가 강제한 것이었다("로터는 얇은 CYLINDER"). 지름은 디스크 로딩·
간섭 검사·호버 파워가 다 쓰는 진짜 데이터라 유지하되, 렌더는 같은 치수에서
허브+N엽 블레이드를 만든다(spec-to-code propellerGeometry — 블레이드 하나를
베지어로 그리고 제어점 회전으로 N개 복제, 단일 ExtrudeGeometry). 엽수는 파트
이름의 "N엽/N-blade"에서 읽고 기본 2엽. 프롬프트가 이미지의 블레이드 수를 세어
이름에 넣게 한다. 다운로드 코드에도 같은 propellerGeometry 헬퍼를 내보낸다.

로터 회전축도 고쳤다: 디스크는 축이 틀려도 안 보였지만 블레이드는 보인다.
순항 프로펠러(blade가 XY면)는 z축 회전이어야 해서, 각 로터의 로컬 지오메트리에서
가장 얇은 축을 찾아 그 축으로 돌린다(drone-sim spinAxis).

부수 효과로 프린트 추정이 정직해졌다: 통짜 원반이 아니라 블레이드 부피라
옥토 369→276h, 헥사 94→55h.

### 사진 근거 재생성 첫 결과 (sar-vtol, 302초)
V미익이 꼬리(z=-600)로, 짐벌·탐조등이 기수로, 순항 모터가 후방 푸셔로 —
전부 사진과 일치. 랜딩기어 지지대 같은 사진에만 있는 파트도 생겼다. 18파트.

### 딥리서치 → 설계 검사·프롬프트·6-DoF 반영 (v22)
드론 안의 깊이를 문헌으로 채웠다. 수집한 예비설계 관례와 반영 위치:

| 지식 | 출처 계열 | 반영 |
|---|---|---|
| 로터 간섭: s/D ≥ 1.2에서 손실 수 % 이내, ≥1.5 둔감 | RMIT 간섭 논문, Lei&Cheng 헥사 간격 연구 | 간격 검사에 s/D 등급 |
| 디스크 로딩: 촬영 5~12 / 농업·물류 10~25 kg/m², P/W ∝ √DL | Krossblade, eVTOL 음향 트레이드 연구 | 로딩 검사 등급화 |
| 배터리 질량분율 25~40%, 체공 피크는 ~2/3이나 구조가 먼저 무너짐 | flying-batteries(Berkeley), 사이징 논문 | battery_fraction 검사 신설 |
| 팁 속도 M0.7 미만, 소음 ∝ V^5~6 | 프로펠러 설계 관례, 저소음 프롭 연구 | tip_speed 검사 + 6-DoF rpm 상한 |
| 전복각 ≥ 25° (턴오버 기준) | 랜딩기어 설계 교과서·AIAA | tipover 검사 신설 |
| 미익 체적계수 V_H 0.4~0.7, V_V 0.02~0.05, 팔 2.5~3.5×MAC | Raymer/Scholz 미익 사이징 | V_H·V_V 검사 신설 (V미익 65/35 배분) |
| 날개 하중 5~15 kg/m², 실속 √(2W/ρS·CLmax), AR 6~12 | 소형 UAV 설계 관례 | wing_loading·aspect_ratio 신설 |
| T/W ≥ 2.0 (호버 스로틀 50%) | 멀티로터 사이징 논문들 | 기존 추력 여유 검사 유지 + 프롬프트 사이징 규칙 |

같은 수치를 DRONE_SPEC_SYSTEM 1-2 사이징 규칙으로 넣어 생성 시점부터 지키게 했다.
검사와 생성이 같은 기준을 봐야 "생성→검사 빨강→수정" 루프가 수렴한다.

6-DoF 보정: rpm 상한이 지름과 무관하게 9000이라 26인치 농업 로터가 M0.87로 돌았다.
팁 속도 206m/s(M0.6) 상한과 9000rpm 중 낮은 쪽을 쓰니 추력 여유도 현실화됐다
(헥사 11.7→5.6×). 실측 판정: 쿼드 추력 여유 1.27×(부족 — 카탈로그에서 모터 상향 필요),
map-wing V_H 0.14(미익 과소), sar-vtol 날개 하중 51.7(주익 과소) — 전부 진짜 신호다.

### 디테일 인벤토리 이식 (드론 경로)
범용 경로의 검증된 패턴을 드론 경로로 옮겼다. 이미지가 있으면 사양서 전에 저비용
인벤토리 호출이 돌고(임무별 부품 체크리스트를 후보로 제시, "이미지에서 실제로
식별되는 것만 present"), 사양서는 인벤토리 전 항목을 파트로 실현해야 하며,
DETAIL_ITEM_MISSING 검증이 누락을 수리 라운드로 돌려보낸다. 파트 상한 26→30,
토큰 예산 56k→60k.

농업 헥사 첫 결과: 13종 → 28종 (접이 힌지·퀵릴리즈·케이블 커버·모터 포드·허브 캡·
배터리 슬레드·주입구 캡·호스·노즐 3계열). 458초, 검증 0건.

같이 드러난 컴파일러 결함 둘:
1. CYLINDER가 h를 무조건 길이로 읽어, "지름 40 길이 650" 카본 튜브 암(w40 h40 d650,
   FRONT)이 지름 650 원반으로 서 버렸다. size_mm은 월드 박스이므로 축 방향 치수가
   길이다 — plane별로 지름·길이를 갈라 읽게 수정. 스키드·노즐 붐도 같이 정상화
   (기체 높이 1325→685, 프린트 89→14.7kg).
2. "프로펠러 허브 캡"이 로터로 인식돼 헥사가 12로터로 계산됐다. 허브·캡·스피너를
   로터 정규식에서 제외하고, 프롬프트에 명명 규칙을 적었다.

### 인벤토리 경로 샘플 재생성 결과
| 샘플 | 이전 | 이후 | 비고 |
|---|---|---|---|
| 점검 쿼드 | 12종 | 18종 | 허브 캡·가드·짐벌 축 분리 등 |
| 농업 헥사 | 13종 | 28종 | 힌지·펌프계·노즐 3계열 |
| 측량 고정익 | 11종 | 14종 | 피토관·카메라 창 등 |
| 수색구조 VTOL | 18종 유지 | — | 재생성분(12종·검증 6건)이 퇴행해 이전본 복원 |

VTOL 퇴행은 인벤토리가 5~10항목만 잡히는 편차에 수리 루프가 수렴 실패한 경우다.
편차가 있으면 "새 결과가 이전보다 나은가"를 비교해 나쁘면 버리는 게 맞다 —
build-samples가 무조건 덮어쓰므로 복원은 git에서 했다.

파라미터 회귀도 하나: 모델이 쓴 affects에 가드·허브 캡이 빠져 휠베이스 링 결합이
막혔다. 링 결합은 물리 제약이라 affects보다 우선하게 수정. 반경 0(중심에서 뻗는
막대)은 링이 아니므로 반경을 건드리지 않는다.

### "여전히 다르다"의 진짜 원인 — 암이 접선 방향에 링 위에 떠 있었다
배포는 정상이었다(로컬·온프렘 해시 동일, Pages 서빙 확인). 그런데 컴파일된 기하를
측정하니 암에 별개의 버그가 둘 있었다. 파트가 13→28종으로 늘면서 드러났다.

1. **90° 틀어짐.** placements()는 파트의 로컬 +x를 반경 방향에 맞춘다. 그런데
   plane FRONT 실린더는 장축이 z라, 카본 튜브 암이 바깥이 아니라 **옆으로** 뻗었다
   (반경 정렬 0.00 = 완전 접선). delivery-octo는 암이 EXTRUDE_2D 장축 x라 우연히
   멀쩡했고, 그래서 여태 안 잡혔다.
2. **링 위에 중심이 놓임.** 프롬프트는 "암 *끝*의 radius_mm이 모터와 일치"라고
   요구하는데 컴파일러는 암 *중심*을 반경에 놓았다. agri-hexa 암(길이 650, r=700)이
   반경 375~1025를 덮어, 바디(반경 175)와 200mm 떨어진 채 모터를 325mm 지나쳤다.
   inspect-quad는 radius_mm=0이라 암 4개가 전부 중심에 뭉쳐 있었다.

spec-cad에 normalizeRadialStruts()를 넣어 컴파일 시점에 교정한다. 로드 판별은
수평 두 축(w,d) 비 ≥ 3 — 로터 디스크는 양쪽이 다 넓어 자동 제외되고, 실제로
ESC·케이블커버·허브캡 전부 올바르게 빠졌다. 결과:

| 샘플 | 이전 반경범위 | 이후 | 정렬 |
|---|---|---|---|
| agri-hexa 암 | 375~1025 (접선) | 50~700 | 1.00 방사형 |
| inspect-quad 암 | 중심에 뭉침 | 75~225 | 1.00 방사형 |
| delivery-octo 암 | 375~825 | 150~600 | 1.00 방사형 |

프롬프트와 컴파일러가 계약을 다르게 해석하면 검증기도 QA도 못 잡는다 —
QA는 컴파일·시뮬·파라미터만 보고 "어느 방향을 향하는지"는 안 봤다.

### 캐시 무효화와 접촉(연결성) 검증
**캐시.** Pages가 JS를 max-age=600으로 주고 모듈 지정자에 버전이 없어, 배포 후 10분간
브라우저가 옛 빌드를 돈다. 가설이 아니라 실제로 내 검증 스크립트가 CDN 캐시를 읽고
"수정이 반영 안 됨"이라고 잘못 보고했다. `tools/stamp-version.mjs`가 js/·docs/specs/
내용 해시를 구해 docs/index.html과 docs/js/*.js의 상대 import에 `?v=`를 찍고,
런타임 fetch(사양서·메시)는 BUILD 상수를 붙인다. 해시는 항상 원본(js/)에서 계산해
스탬프된 사본을 다시 해시하지 않는다 — 안 그러면 매 실행마다 값이 바뀐다.

**연결성.** "각 파트가 뭔가에 닿는가"로는 부족하다. 살포 탱크는 자기 주입구 캡과
호스에 닿아 있으므로 통과하지만, 그 덩어리 전체가 기체에서 떠 있었다. 그래서
접촉 그래프의 **연결 성분**을 센다. 서버(사양서 좌표, 수리 루프용)와 브라우저
(실제 컴파일 기하, 사용자 표시용) 양쪽에 넣었다.

로드(암·붐)는 회전된 채로 하나의 축정렬 박스에 넣으면 링 끝에 뭉쳐 없는 간극을
만든다. 반경 방향으로 5등분 샘플 박스 체인으로 근사했다.

**정규화 위치를 서버로 옮겼다.** normalizeRadialStruts를 컴파일 시점(spec-cad)에만
두니 사양서 JSON은 여전히 radius 700을 말하고 검증기는 그걸 읽어 암이 떠 있다고
판정했다. 같은 계약을 두 곳이 다르게 보면 수리 루프가 수렴하지 않는다.
server.mjs normalizeStrutRadius()가 사양서 자체를 고쳐 JSON·검증기·뷰어가 한 이야기를
한다. 컴파일 쪽은 멱등이라 그대로 두고 옛 사양서의 안전망으로 남긴다.

첫 실측: agri-hexa 5덩어리(살포 붐+노즐 / 탱크+캡 / 호스 2개가 각각 분리),
delivery-octo 3덩어리(GNSS 마스트, 화물 베이). 나머지 4종은 한 덩어리.

### 인벤토리 편차 보정 + TUBE 축 규약
농업 헥사 재생성이 16파트로 줄어 원인을 찾으니, 연결성 검증이 아니라 **인벤토리
편차**였다 — 같은 사진에서 13항목/9항목으로 갈렸고 사양서가 28/16파트로 증폭됐다.
9항목 생성은 수리 루프를 아예 안 탔다(검증 0건). 12항목 미만이면 "놓치기 쉬운 것
(체결류·케이블 경로·분리선·힌지·캡·안테나)"을 지목해 재조사하고 두 결과를 합친다.
9→12항목, 16→19파트, 연결 OK. 세 후보 대조 후 C 채택:

| 후보 | 파트 | 연결성 |
|---|---|---|
| A (인벤토리 13) | 28 | 5덩어리 분리 |
| B (인벤토리 9) | 16 | 한 덩어리 |
| C (재조사 12) | **19** | **한 덩어리** |

TUBE에도 CYLINDER와 같은 축 규약 버그가 남아 있었다. `max(w,d)`를 지름으로 읽어
35mm 카본 튜브 암이 지름 700mm 원반이 됐다 — 암 6개만 38.8L. plane 법선을 축으로
읽게 고쳐 프린트 추정 102→78kg. 남은 과다분은 배터리 슬레드(250×100×200 솔리드)
같은 사양서 쪽 치수 문제로 별건이다.

### v23 — 좌표를 지어내지 말고 메시에서 측정한다
사용자 지적: 배치를 네 번 고쳤는데도 형태가 안 닮는다. 원인 분석 결과 지금까지 고친
것은 전부 **배치** 계층이었고, 남은 격차는 **표현력** 한계였다.

측량 고정익 동체는 `EXTRUDE_2D plane=SIDE w140 h120 d760` — 측면 실루엣은 ARC·BEZIER
8세그먼트로 훌륭한데, 그 단면을 140mm 두께로 그대로 밀어낸다. 옆에서 유선형, 앞에서
판자다. 문헌도 같은 벽을 보고한다(Text2CAD-Bench: 현재 데이터셋은 sketch-extrude
위주이고 loft·sweep·fillet 미지원, 실패 유형에 "스윕·로프트를 더 단순한 sketch-extrude로
대체" 명시). 어휘 통계도 같은 말을 한다: CYLINDER 35 · EXTRUDE_2D 23 · BOX 17,
LOFT/SWEEP/FILLET 0, rotation 필드 0.

**해법: LLM을 좌표의 출처에서 내린다.** 1단계 메시를 파트의 선언 박스로 마스킹해
축 방향으로 슬라이싱하고, 각 station의 폭·높이·충만도를 측정해 사양서에 쓴다.
분할 API를 사지 않는다 — 사양서가 이미 파트 박스를 갖고 있고 그게 마스크다. 비용 $0.

측량 고정익 동체, 측정 결과 (선언은 h120 고정이었다):
```
 19%  w 43  h  9      56%  w126  h 97      94%  w 31  h  7
 31%  w 74  h 36      69%  w101  h 72
 44%  w115  h 80      81%  w 62  h 31
```

구현 중 잡은 설계 오류 둘:
1. 처음엔 로프트 축을 `plane` 법선으로 잡았다. plane은 압출 단면이 놓이는 곳을
   말할 뿐이라, 동체를 길이가 아니라 **폭 방향으로** 잘라 무의미한 값이 나왔다.
2. 최장축으로 바꿨더니 세로형 탱크(400×302×400)가 옆으로 누웠다. 최종 규칙은
   **가장 비슷한 두 축이 단면, 나머지가 축** — 동체는 (126,97) 짝이라 d가 축,
   탱크는 (400,400) 짝이라 h가 축. 규칙은 mesh-loft.loftAxis 한 곳에만 있고
   컴파일러가 그것을 import한다(계약 이원화가 이번 세션 버그의 주범이었다).

단면 형상은 충만도(fill)로 표현한다. 각 station에서 16개 각도 빈의 최대 반경으로
외곽 다각형을 만들고 그 면적을 바운딩 사각형과 비교한다 — 타원 0.785, 사각형 1.0.
컴파일러는 그 값에 맞는 지수의 초타원 링을 쌓아 스키닝한다.

적용 결과: 로프트 6파트(동체 2, 캐노피 2, 하부 셸 1, 살포 탱크 1). 렌더 치수가
선언 치수와 정확히 일치. QA 6/6. 단면이 균일하거나 측정에 실패하면 기존 빌더를
그대로 두고 그 사실을 보고한다.

### v24 — 전 파트 좌표 측정
v23은 기계는 맞았지만 적용이 5%(94파트 중 5)뿐이었다. 이름 필터(동체·셸·탱크만),
반복 파트 제외, 정렬 실패가 원인이었다. 셋 다 걷어냈다.

- **이름 필터 제거.** 이름이 아니라 "그 박스 안에 메시가 있는가"로 판정한다.
- **반복 파트 포함.** 링의 첫 인스턴스만 재서 나머지에 적용한다(암 6개는 암 하나가
  여섯 번이다). CIRCULAR·MIRROR_PAIR 배치 좌표를 재현해 박스를 제자리에 놓는다.
- **정렬을 축별 피팅으로.** 균일 축척 하나로 맞추면 나머지 두 축이 어긋나 대부분의
  셸이 "측정 실패"였다. 생성 메시는 측정이 아니라 재구성이라 비례가 몇 % 흔들린다.
  축마다 독립으로 맞추니 실패가 사라졌다.
- **박스 여유 단계 확대(0.12 → 0.4 → 0.9).** 얇은 미익·로터 디스크는 배치가 조금만
  어긋나도 빈 박스가 나와 내부 파트로 오판됐다. 비율 가드(0.4~2.5배)가 넓은 그물이
  이웃을 삼키는 것을 막는다.

측정률 5% → **77%(69파트 중 53)**. 측정 내용은 치수·중심 보정 + 단면이 변하면 로프트.
남은 16개는 전부 실제 내부 부품(배터리·비행제어·ESC·짐벌 내부)이라 겉면 재구성으로는
원리적으로 볼 수 없다 — 이건 사양서 값을 유지하고 그렇다고 보고한다.

### v25 — 표현 어휘 확장: 파트 회전과 날개 단면
v24로 좌표의 77%가 실측이 됐지만 담을 그릇이 없었다. 어휘 통계의 `rotation 필드 0`이
그대로였고, 날개는 평면형을 일정 두께로 밀어낸 판자였다. 빌더를 늘리는 대신 기존
빌더에 **방향과 단면**을 붙였다.

**`geometry.rotation_deg {x,y,z}`** — 파트 자체 기울기. 상반각·틸트로터·경사 랜딩기어가
열린다. 핵심은 합성 순서다: 파트 회전은 파트 자신의 프레임에서 일어나므로 배치 요(yaw)
**보다 먼저** 적용해야 한다. 반대로 합성하면 링 위의 암 여섯 개가 전부 같은 나침반
방향으로 기운다 — 바깥으로 벌어지는 게 아니라. 오일러 3축이 아니라 쿼터니언으로
합성해 순서를 기본값에 맡기지 않고 코드에 적었다(`instanceXform`).

MIRROR_PAIR는 **대칭면이 힌지**다. 각 copy의 자기 중심을 축으로 돌리면 팁은 올라가고
루트는 동체 밖으로 빠진다. 대칭면 기준으로 스윙해야 그게 상반각이다. 반사는 회전의
y·z 성분을 뒤집고 x는 그대로 두는 변환이라, 좌우가 같은 쪽이 아니라 서로 반대로 벌어진다.

**`geometry.airfoil {thickness_pct, camber_pct, twist_deg}`** — plane TOP의 EXTRUDE_2D
에만 붙는다. 평면형(위에서 본 외곽선)은 사양서가 잘 쓰는 유일한 것이라 그대로 두고,
단면만 NACA 4자리로 **생성**해 루트→팁으로 로프트한다. 최대 캠버 위치(넷째 자리)는
0.4로 고정했다 — 이 급의 익형은 거의 전부 거기 있고(2412·4412·2415), 사양서 작성자가
넷째 숫자를 찍게 하는 비용이 얻는 범위보다 크다. 두께식 마지막 계수는 교과서의
−0.1015가 아니라 **−0.1036**이다. 뒷전이 정확히 닫혀야 스키닝이 막히고, 안 닫히면
등 뒤에 틈이 난 셸 두 장으로 프린트된다.

두께는 **최대 코드 × thickness_pct**로 정해진다. size_mm.h는 입력이 아니라 맞춰야 하는
값이 되므로, 어긋나면 빌드 기록과 검증기가 둘 다 말한다(회전체에서 프로파일이 높이를
정하는 것과 같은 관계다).

**측정 패스가 이 둘을 덮어쓰지 않게 막았다.** `refineFromMesh`가 쓰는 것이 정확히
이 두 필드가 담는 것이라, 그대로 두면 측정이 정제가 아니라 삭제였다. 회전한 파트의
size_mm은 기울기 **전** 자기 박스인데 측정값은 월드 축 범위라 항상 더 크다 — 측정할
때마다 부풀고 기울기가 오차를 곱한다. 에어포일 파트는 builder가 LOFT로 뒤집히면서
생성 단면이 측정 박스 8개로 대체된다. 단면을 넣어 없애려던 바로 그 판자다.
건너뛴 파트는 `authored`로 보고한다 — 조용히 건너뛰면 나중에 판자로 발견된다.

**설계상 이번에 제외한 것: 파트 간 블렌드(필렛/융합).**
- 범위가 다르다. rotation·airfoil은 파트 하나를 만드는 방법을 바꾸는 **국소** 변경이라
  `buildGeometry` 안에서 끝난다. 블렌드는 두 파트의 경계를 함께 푸는 문제라 파트를
  독립적으로 만들고 배치하는 지금의 파이프라인 구조 자체를 건드린다.
- 필요한 기계가 없다. 삼각 메시 두 개를 매끄럽게 잇는 것은 불리언 합집합 + 경계 곡선
  추출 + 가변 반경 필렛이다. 이 저장소는 Three.js BufferGeometry만 쓰고 CSG 커널이
  없다. 커널을 들이는 것은 이번 작업 전체보다 크다.
- 이미 있는 길이 있다. 이 저장소의 규약은 **필렛·챔퍼는 파트가 아니라 프로파일 곡선**
  이다(v1에서 정한 것). 파트 안쪽 모서리는 outer_profile의 ARC/BEZIER로 이미 표현되고,
  검증기가 `PROFILE_ALL_STRAIGHT`로 강제한다. 남는 것은 파트 **사이**의 융합뿐인데,
  그건 조립체를 하나의 솔리드로 보는 관점이라 분해 뷰·부품표·프린트 견적이 전부
  파트 단위인 지금 설계와 정면으로 어긋난다.
- 그래서 하려면 별도 작업이다: CSG 커널 도입 → 관계(relationships)에 FUSED 타입 추가
  → 프린트/BOM이 융합체를 어떻게 셀지 결정. 세 개가 다 정해지기 전에는 반쪽이 된다.

**1차 LLM 스키마 한도 (이번에 드러난 진짜 문제).** 필드를 더한 뒤 온프렘에서 재생성해
보니 1차 LLM이 `"Request contains an invalid argument."`로 요청 전체를 거부하고 폴백이
대신 답했다 — 10파트짜리 표본이 3파트로 내려앉았다. 그런데 `git show HEAD:server.mjs`의
스키마도 **똑같이 거부**됐다. 즉 이 필드들이 원인이 아니라, 드론 스키마는 이미 한도를
넘어 있었고 그동안의 표본이 전부 폴백 산물이었다. 증상이 오류가 아니라 "조용히 더 나쁜
사양서"라서 안 보였다.

엔드포인트에 직접 물어 확인한 값: 드론 스키마는 비용 424에서 거부, 196에서 수락.
평평한 스키마는 250 수락 / 300 거부. 깊이는 10까지 무관했다. enum 멤버가 비용에
들어가고 가장 싸게 포기할 수 있는 것이라, 한도를 넘을 때만 enum을 description으로
접는다(`primarySchemaFor`). 어휘는 여전히 모델에 전달되고, `validateSpec`은 원래부터
어휘를 서버에서 다시 검사한다(Structured Outputs는 형태를 제약할 뿐 어휘를 제약하지
않는다). 작은 스키마는 하드 enum을 그대로 쓴다. 접고도 한도를 넘으면 경고를 찍는다 —
다음 사람이 필드를 더할 때 자기 필드가 범인으로 몰리기 전에 여유를 알 수 있도록.

고친 뒤 isr-wing 재생성: **12파트, 검증 2건, 292초, 1차 LLM이 답함.** 1차 LLM은
주익에 `{12, 2, -2}`, 수평미익에 `{10, 0, 0}`, 메인 랜딩기어에 `rotation_deg.z 15`를
스스로 넣었고 size_mm.h도 코드×두께비(260×12% = 31)에 맞췄다.

계약 회귀 테스트를 `tools/qa-vocab.mjs`로 분리했다. qa-samples는 컴파일·비행·프린트를
보지만 **파트가 어느 쪽을 보는지**는 못 본다. 축 규약은 이 저장소가 네 번 깨뜨린
지점이고 매번 컴파일은 됐다. 43개 검사 — 루트 단면이 NACA와 0.000mm 일치, 수밀
(열린 에지 0), 거울상 정점 588/588, 링 인스턴스 편차 0.0000, 측정 패스가 두 필드를
건드리지 않음, 기존 5개 사양서 정점 바이트 동일.

### v26 — 좌표를 최적화가 정한다

여기까지 사양서의 모든 숫자는 언어 모델이 사진을 읽고 쓴 것이었고, `tools/similarity.mjs`
는 그것이 얼마나 빗나갔는지 **말만** 했다. `tools/fit-spec.mjs`가 그 말을 목적함수로
바꾼다: 4면 실루엣 IoU가 오르지 않을 때까지 파트 치수를 탐색한다. 생성은 없다 — 들어간
사양서가 그대로 나오고, 안의 밀리미터만 다르다.

자유 파라미터는 파트별 size_mm·center_mm, loft_sections의 w/h/fill, repeat 반경·간격,
그리고 v25의 rotation_deg. 알고리즘은 노브별 적응 스텝을 가진 좌표 하강이다. 최적이라서가
아니라 **모든 이동이 귀속되기 때문**이다 — 채택된 스텝마다 파트·필드·델타가 남아서
사양서 diff가 블랙박스 출력이 아니라 결정 목록으로 읽힌다.

#### 고정한 것 세 가지 (이게 도구의 정직성이다)
1. **USER_PROVIDED 치수.** 노브를 클램프하는 게 아니라 아예 만들지 않는다. `dimensions[]`
   와 size_mm의 연결은 이름이 아니라 값으로 찾는다 — map-wing의 wingspan 1200이 size_mm.w
   와 같으니 그 축만 고정되고 코드는 자유롭게 둔다. 길이가 아닌 값(10L 탱크)이나 어느 축도
   안 맞는 값이면 축을 모른다는 뜻이라 그 파트 전체를 고정한다. 추측이 이 규칙이 막으려는
   바로 그 실패다.
2. **회전 정렬.** similarity는 좌표 규약을 되돌리려고 참조를 90° 돌릴 수 있는데, 여기서는
   손대지 않은 사양서로 한 번 재고 **얼려버린다**. 자기 프레임을 다시 고를 수 있는
   최적화기는 파트를 하나도 안 움직이고 점수를 올릴 수 있다.
3. **설계 검사·검증 규칙.** 후보마다 컴파일 → `drone-sim.js` checks → `validateSpec`.
   기준선을 먼저 잡으므로 샘플에 이미 있던 불평은 막지도, 늘어나지도 않는다.

#### 판정만 보는 가드는 부족했다
첫 판 가드는 "통과였던 검사가 실패로 바뀌면 기각"이었다. 그걸로 sar-vtol이 조립체를
**3덩어리에서 5덩어리로** 쪼개고 로터 간격을 **47mm에서 12mm로** 줄이면서 통과했다 —
두 행 모두 이미 실패거나 미판정이라 판정이 움직이지 않았기 때문이다. 그래서
`js/drone-sim.js`의 두 행이 숫자를 함께 내보내게 하고(`n`), 크기로 비교한다. 한국어
문자열에서 숫자를 파싱하는 것은 깨질 파서를 하나 더 만드는 일이다.

**추력 여유는 왜 가드하지 않는가.** 가드한 둘은 CAD의 기하학적 사실이다 — 파트가 닿거나
안 닿거나, 디스크가 겹치거나 안 겹치거나. 추력 여유는 파트 부피에서 추정한 질량에서 나온
값이라, 사양서의 파트가 너무 작았다는 걸 최적화가 찾아내면 그 값이 나빠지는 것은
**모델이 기체에 대해 더 정직해졌다는 뜻**이다. 얼리면 과소 사양서를 영원히 인정하지
못하게 된다. 그래서 가드하지 않고 매 실행마다 값 변화를 인쇄한다.

#### 한 번 훑는 것으로는 부족했다
노브마다 기각할 때 스텝을 반으로 줄이면 탐색 전체가 끝나기 한참 전에 그 노브가
은퇴한다. 혼자서는 못 움직이던 축이 이웃 파트가 옮겨간 뒤에는 움직일 수 있기 때문이다.
그대로 뒀더니 **자기 출력물에 도구를 한 번 더 돌리면 0.5~3.4pp가 더 나왔다** — 한 번의
실행이 고정점이 아니라는 뜻이고, 멈춰도 되는 곳은 고정점뿐이다. 그래서 진전이 없는 패스가
실행을 끝내지 않는다: 모든 노브가 처음 스텝을 되돌려 받고 지금 자리에서 다시 시작한다.
연속 두 번 헛돌면 끝난다. 재개 후 다시 돌리면 4종 중 3종이 **채택 0건**이다.

#### 결과 (참조: 1단계 메시, 격자 128)
| 샘플 | IoU 전 | 후 | 경계 전 | 후 | 종횡비 전 | 후 | 조립 연결성 |
|---|---|---|---|---|---|---|---|
| inspect-quad | 32.4% | **55.3%** | 5.5% | 3.6% | 82.6% | 38.3% | 한 덩어리 → 한 덩어리 |
| agri-hexa | 30.9% | **58.1%** | 3.4% | 2.1% | 10.3% | 6.0% | 10덩어리 → **4덩어리** |
| map-wing | 33.6% | **69.0%** | 2.8% | 0.9% | 12.1% | 9.1% | 4덩어리 → **한 덩어리** |
| sar-vtol | 40.5% | **64.3%** | 2.1% | 1.5% | 16.5% | 12.8% | 3덩어리 → 3덩어리 |

최적화한 것은 IoU뿐인데 경계 거리와 종횡비 오차도 같이 내려갔다. 조립 연결성은 가드가
악화만 막았는데 두 샘플이 스스로 좋아졌다 — 실루엣을 맞추다 보니 파트들이 제자리를 찾았다.
로터 간격도 agri-hexa가 s/D 1.59 → 1.93으로 올랐다.

#### 반증 시험
- **자기 자신을 참조로.** 사양서의 자기 실루엣을 참조로 주면 6종 × (기본·`--rotate-all`)
  전부 IoU 1.000000, 채택 0건, 바이트 동일. 맞출 것이 없으면 아무것도 만들어내지 않는다.
- **격자 교차.** 128에서 맞추고 256에서 재면 53 · 54 · 64 · 60 — 이득이 2~4pp만 줄어든다.
  격자 인공물이 아니다.
- **다른 참조.** 사진에서 만든 실루엣(fit이 한 번도 보지 않은 참조)으로도
  32.0→51 · 38.3→55 · 33.8→65 · 35.8→46. 독립 참조가 같은 방향을 가리킨다.
- **결정성.** 독립한 두 실행의 사양서 바이트가 동일하고, 배포본과도 동일하다.

#### 이 도구가 내는 가장 쓸모 있는 출력
한계에 걸린 노브다. 탐색이 더 가고 싶었는데 범위가 막았다는 뜻이고, 범위가 "이 치수가
틀렸다"와 "이건 다른 파트다"를 가른다. agri-hexa의 캐노피는 340→544(상한)에서 멈췄는데
중앙 바디가 397이다 — 캐노피가 바디보다 커지려 한다. 사양서가 그만큼 틀렸거나, 참조에
있는 부피를 담당할 파트가 사양서에 없다는 뜻이다. **크기 조절로는 후자를 답할 수 없다.**

이게 사실이라는 증거가 있다: 범위는 **호출마다 입력 사양서를 기준으로** 잡히므로 다시
돌리면 누적된다. agri-hexa를 한 번 더 돌리면 움직이는 것이 정확히 지난번 한계에 걸렸던
노브들뿐이고(캐노피 340 → 544 → 870), 나머지 3종은 채택 0건이다. 한계에 걸린 노브는
끝없이 커지고 싶어한다 — 없는 파트를 이웃이 대신 채우고 있다는 서명이다. 그러니 그
표를 보고 `--scale`을 올리지 말고 사양서에 파트를 넣어야 한다.

#### `--ref views` 경로에 5~13% 바닥이 있다
자기 마스크를 PNG로 쓰고 다시 읽으면 IoU가 1.0이 아니라 87~95%로 나온다. 셀 크기가 1
이하일 때 `masksFromImages`가 최근접 표본을 쓰는데, 이게 얇은 구조의 테두리 행·열을
복제해 잉크를 3~14% 부풀린다(isr-wing 정면 593→675픽셀). 그래서 `--ref views` 수치는
`--ref mesh`보다 체계적으로 낮고, 얇은 기체일수록 더 낮다. 이번 적합은 메시 참조로 했고
사진 참조는 검증에만 썼다.

`server.mjs`의 `validateSpec`은 `spec-validate.mjs`로 분리했다. 생성기와 최적화기가 같은
규칙을 봐야 하고, 규칙을 복사했다면 한쪽만 고치는 날까지만 맞았을 것이다.

### v27 — 통합 검증과 배포: 세션 시작점 대비 실측

v23~v26을 한 커밋으로 묶기 전에, 이 세션이 실제로 무엇을 옮겼는지 **하나의 축**으로
재봤다. 앞의 각 절이 인용한 "전"은 저마다 자기 직전 상태였고, 그것들을 이어 붙인 값은
측정이 아니라 산수다.

**기준선을 어떻게 잡았는가.** 세션 시작 커밋 `8281dab`을 별도 워크트리로 꺼내고,
거기에 `tools/similarity.mjs`·`tools/image-io.mjs`·`js/mesh-loft.js`만 얹어 같은 자를
들이댔다. `8281dab`에는 `js/mesh-loft.js`가 아예 없어 컴파일러가 그것을 부르지 않으므로,
얹어도 그 시점 기하는 한 바이트도 달라지지 않는다 — 참조 GLB를 읽는 리더로만 쓰인다.
중간 지점 `b476c2c`(v24)에는 `spec-to-code.js`가 `loftAxis`를 부르므로, 그 함수가
현재본과 **동일함을 먼저 확인한 뒤에** 얹었다. 자는 세 지점에서 같아야 하고 재는 대상만
달라야 한다.

교차 확인: `b476c2c` 워크트리의 값이 v25 절이 기록한 32.4 · 30.9 · 33.6 · 40.5과
소수점까지 일치한다. 다른 폴더·다른 트리에서 같은 수가 나온다는 뜻이다.

| 샘플 | 8281dab (시작) | b476c2c (v24) | 현재 (v26) | 시작 대비 |
|---|---|---|---|---|
| inspect-quad | 31.4% | 32.4% | **55.3%** | +23.8pp |
| agri-hexa | 28.3% | 30.9% | **58.1%** | +29.9pp |
| map-wing | 41.1% | 33.6% | **69.0%** | +27.9pp |
| sar-vtol | 28.3% | 40.5% | **64.3%** | +36.0pp |
| **평균** | **32.3%** | 34.3% | **61.7%** | **+29.4pp** |

경계 거리는 평균 3.5% → 2.0%, 종횡비 오차는 inspect-quad 75.6 → 38.3%,
agri-hexa 15.3 → 6.0%, sar-vtol 16.2 → 12.8%.

**map-wing이 v24에서 41.1 → 33.6으로 떨어진 것을 지운다.** 전 파트 좌표 측정이 이
샘플에서는 손해였다. 파트 14개짜리 고정익은 측정할 표면이 적고, 측정된 박스가 날개의
얇은 실루엣을 정면에서 부풀린다(그 시점 정면 IoU 24 → 19%). v26의 적합이 그것을 되돌리고
더 갔지만, 되돌린 만큼은 이득이 아니라 회수다. v24를 "5% → 77%"로 요약한 것은 **적용률**
이고 정확도가 아니었다 — 두 수를 같은 문장에 놓지 말아야 한다.

**99%에는 닿지 않는다. 61.7%다.** 남은 38pp가 어디 있는지는 세 절에 걸쳐 이미
적혀 있고, 크기 조절로 닿는 곳이 아니다. inspect-quad는 참조에 있는 하부 랜딩기어
덩어리가 사양서에 없고(Y 비율 CAD 0.559 대 참조 0.661, 독립 참조인 사진도 0.687로 합의),
agri-hexa는 캐노피 노브가 상한에 붙어 이웃 파트가 없는 부피를 대신 채우고 있으며,
map-wing 정면 55%는 얇은 판을 정면에서 보는 뷰라 몇 밀리미터가 IoU를 무너뜨린다.
셋 다 **사양서에 파트가 빠졌다**는 같은 진단이고, 그건 치수 탐색이 아니라 어휘·인식
단계의 일이다. 지금 표를 더 올리려고 `--scale`을 열면 없는 파트를 이웃이 대신 부풀린
값이 점수로 들어온다.

배포 확인: `qa-samples` 6/6, `qa-vocab` 42/42, 빌드 스탬프 `d78ba17d`(재실행 0개 = 수렴),
`docs/js/` 34개 파일이 `js/`와 버전 쿼리만 빼고 바이트 동일, 공급사명 검사 0건(미추적 파일 포함).

### v29 — 내부 부품이 실루엣을 훔치고 있었다 + 부품 교체가 3분의 1만 도착했다

세 결함을 고쳤다. 둘은 서로 다른 사람이 붙었고, 셋째는 그 둘이 만나는 자리에서 생긴다.

#### 결함 1 — 측정이 내부 파트를 기체로 바꿔놓았다

`refineFromMesh`는 파트의 선언 박스 안에 있는 정점을 모아 그 파트의 치수로 되쓴다.
배터리·비행제어기·ESC는 겉면 재구성에 **아예 없다**. 그런데 박스는 비면 넓혀서 다시
찾으므로(여유 0.12 → 0.4 → 0.9) 언젠가 반드시 무언가를 찾는다 — 머리 위를 지나가는
동체 셸이다. 그것이 파트의 치수로 기록되고, 다음 실행은 **그 기록을 중심으로** 다시
넓힌다. 점검 쿼드의 비행 배터리 팩은 선언 60×30×100 → 145 → **232mm**가 됐다.
배터리 이름표를 단 동체 조각이다.

방어 셋을 넣었다(`js/mesh-loft.js`).
- **가려진 파트는 측정하지 않는다.** 휴리스틱이 아니라 사양서 자신의 선언을 쓴다 —
  오염된 파트는 전부 `visibility: "OCCLUDED"`였다. 겉면 재구성으로 볼 수 없다고
  사양서가 이미 적어둔 파트를 겉면 재구성으로 재려 한 것이 사고의 전부였다.
- **로프트 단면에도 비율 가드.** `[0.4, 2.5]` 창은 원래 치수 갱신 루프에만 걸려 있었고,
  로프트 경로가 같은 두 필드를 무조건 덮어썼다. 가드가 막은 값이 옆문으로 들어왔다.
  기준은 **원래 선언 치수**다 — 측정값을 측정값과 견주면 가드가 무력해진다.
- **그물을 넓힐 때는 일치를 요구한다.** 넓힌 그물(0.4·0.9)부터, 잡힌 점의 범위가
  선언 박스와 축별 구간 IoU로 얼마나 겹치는지를 본다. 문턱은 맞춘 상수가 아니라
  유도된 값이다: 박스를 가득 채운 포획은 정확히 `1/(1+pad)`를 기록하므로, 그 이하면
  파트가 아니라 그물을 잰 것이다.

되돌린 값은 `js/drone-catalog.js`의 실측 카탈로그를 1차 근거로 썼다(6S 5000 팩
140×48×42, F7 스택 37×37×16 등). 복원은 `tools/refine-specs.mjs --reset-internal`이며
**복원만 하고 재측정하지 않는다.** 복원 후 전체 재측정을 해보니 IoU가 54.4/58.1/68.8/61.4
→ 49/55/65/52로 무너졌다. 배포된 사양서는 생 측정 출력이 아니라 그 뒤 `fit-spec.mjs`
실루엣 피팅을 거친 결과인데, 새 측정 패스가 그것을 덮기 때문이다.

#### 결함 1의 결과: IoU가 올라간 게 아니라 내려갔다

| 샘플 | 기준선 | 현재 | Δ |
|---|---|---|---|
| inspect-quad | 54.40% | **52.44%** | −1.96 |
| agri-hexa | 58.07% | **57.35%** | −0.72 |
| map-wing | 68.83% | **68.61%** | −0.22 |
| sar-vtol | 61.38% | **61.38%** | ±0.00 |

**오염된 내부 파트는 실루엣을 갉아먹은 게 아니라 훔치고 있었다.** 점검 쿼드 배터리는
`ablate-parts` 기준 전용 266화소 · 획득 150 · 낭비 117로 순이득이었다 — 232mm짜리 조각이
기체 뒤아래로 삐져나가 CAD가 모델링하지 않은 참조 기하를 우연히 덮었다. 고친 뒤
전용 5 · 획득 3 · ΔIoU 2.01 → **0.05**. 손실 −1.96pp가 이 한 파트의 가짜 기여와 거의
정확히 같다. agri-hexa 배터리 슬레드도 ΔIoU 1.30 → 0.64. 미피복 참조 화소가
inspect-quad 26.7% → **31.4%**로 늘었다 — 원래 있던 구멍(CAD에 없는 저후방 기하)이
드러난 것이다. 배터리가 셸 안에 제대로 들어가면 실루엣 기여도는 0이 정상이고, 그 위는
전부 가짜 점수다. **2pp를 잃은 게 아니라, 2pp가 처음부터 없었다는 것을 알게 됐다.**

v28은 "내부 파트가 실루엣을 오염시킨다 — 아니다"로 이 가설을 기각했었다. 그때 확인한
짐벌·카메라·ESC·FC는 정말로 전용 화소 0이었다. **배터리를 보지 않았다.** 오염된 파트는
하나뿐이었고 하필 그것이 800화소를 칠하고 있었다.

#### 결함 2 — 부품을 갈아 끼우면 세 치수 중 하나만 도착했다

`applyComponent`가 `size_mm`만 갈고 옛 파트의 `loft_sections`를 남겼다. LOFT 빌더는
**두 단면 치수를 전부 loft_sections에서** 읽고 size_mm에서는 축 길이만 가져간다.
6S 5000 팩(140×48×42)으로 바꾸면 렌더는 **140 × 12.3 × 125.8**이 나왔다 — 길이만
바뀌고 단면은 이전 파트 그대로다. 짐벌은 33×16×32 → 33×90×32.

카탈로그 형상이 있으면 `builder`·`loft_sections`를 새로 쓰고 옛 `outer_profile`·
`inner_profile`을 지운다. 형상이 없는 항목이라도 **최소한 낡은 loft_sections는 지우고**
LOFT였던 빌더를 ROUNDED_BOX로 내려 세 치수가 모두 반영되게 한다.

#### 결함 3 — 카탈로그가 치수만 알고 생김새를 몰랐다

24개 항목 전부에 `shape: { builder, sections[] }`를 넣었다. 단면은 밀리미터가 아니라
**박스 대비 비율**이고 `loftSectionsFor()`가 최대 단면을 박스에 정규화하므로, 렌더 박스는
항상 `dims_mm`와 일치한다. 로프트 축은 카탈로그에 적지 않고 빌더가 쓰는 `loftAxis()`로
유도한다 — 두 쪽이 어긋날 수 없다. 배터리는 수축포장 끝단이 접힌 모서리 죽은 직육면체
(21700 원형 셀은 fill 0.86, 12S 하드케이스는 0.97), 모터는 마운트 베이스+스테이터
웨이스트+벨+축, FC는 2층 보드+스탠드오프, 짐벌은 구+요크+마운트 플레이트를 **한 파트 한
로프트**로 낸다. 파트를 쪼개지 않은 이유는 스키마가 파트당 geometry 하나이고, 덩어리마다
파트를 만들면 `massBudget()`이 `component_ref.mass_g`를 파트 단위로 더해 한 부품의 질량이
중복 계상되기 때문이다.

#### 결함 4(통합) — 같은 세 필드를 카탈로그와 측정이 서로 덮어쓴다

두 수정이 만나는 자리에 답이 정해지지 않은 질문이 남았다. `size_mm`·`builder`·
`loft_sections`는 **카탈로그 교체가 쓰는 세 필드이자 측정 패스가 쓰는 세 필드**다.
나중에 도는 쪽이 이긴다는 뜻이고, 그건 규칙이 아니라 사고다.

**사용자가 고른 부품이 이긴다.** 카탈로그 항목은 실재하는 부품의 실측 치수와 형상을
들고 온다. 측정은 재구성 표면에서 그것을 **추정**할 뿐이다. 명시적 선택과 추론 사이에서는
선택이 이긴다 — 그러지 않으면 교체 뒤에 측정이 한 번만 더 돌아도 사용자가 고른 부품이
이름만 남기고 조용히 다른 물건이 된다. `refineFromMesh`가 `component_ref.catalog_id`를
가진 파트를 회전·에어포일 파트와 같은 자리에서 건너뛴다(보고에는 `(부품)`으로 남는다).
파트가 **어디에** 놓이는지는 카탈로그가 다투지 않으므로 위치도 손대지 않는다.

반대 방향은 이미 옳다 — 측정이 만든 LOFT 파트에 카탈로그를 얹으면 옛 단면이 지워진다
(결함 2 수정). `tools/qa-vocab.mjs`에 양방향 계약을 못 박았다.

#### 검증

- `qa-samples` **6/6**, `qa-vocab` 전부 통과(교체 우선순위 3건 추가).
- **카탈로그 교체 140/140 치수 일치** — 샘플 6종 × 카탈로그 24종 중 대상 파트가 있는
  140개 조합 전부, 렌더된 파트의 **로컬** 정점 박스가 카탈로그 3치수와 오차 2% 이내.
  (월드 박스는 링에 45° 배치된 모터에서 √2배로 부풀어 보인다. Three의 "박스의 박스"
  계산 특성이며 교체 전후 동일하다.)
- `similarity.mjs` 4종 위 표대로. **개선 없음. −1.96 / −0.72 / −0.22 / 0.00이다.**
- `ablate-parts`로 내부 파트가 실루엣에서 빠진 것을 확인: 점검 쿼드의 ESC·FC·짐벌
  베이스·하방 카메라 전부 ΔIoU +0.00, 배터리 +0.05.
- 재측정을 걸어도 되돌아오지 않는다(`refine-specs --dry`): 오염됐던 파트가 전부
  `(가려짐)`으로 기각된다.
- 공급사명 검사 0건, `docs/js/` 34개 파일이 `js/`와 버전 쿼리만 빼고 바이트 동일.

#### 남은 한계 (손대지 않았다)

- **`PARTIALLY_VISIBLE` 내부 파트는 아직 측정된다.** agri-hexa 배터리 슬레드는
  재측정하면 250×100×200 → 246×61×225로 움직인다. 비율 가드 안이라 한 번에 무너지지는
  않지만, 실행을 반복하면 각 실행의 출력이 다음 실행의 선언이 되어 누적된다
  (200 → 225 → 255 → 289 → 327).
- **반복 측정 누적은 이 도구의 원래 성질이고 이번에 생긴 것이 아니다.** 기준선 코드로
  같은 실험을 하면 점검 쿼드 분리선이 5회에 10배로 부푸는 것까지 동일하다. `refine-specs`는
  한 번 돌리고 `fit-spec`으로 마무리하는 도구지, 수렴하는 도구가 아니다.
- **map-wing 배터리는 아직 실루엣에 +1.66pp 기여한다.** 치수는 카탈로그값(48×42×140)으로
  정상이고 낭비 화소도 0이지만, 그 자리의 동체가 참조보다 얇아 배터리 상자가 빈 곳을
  메우고 있다. 사양서에 없는 파트를 이웃이 대신 채우는 v26 이래의 같은 진단이다.
- **크기 슬라이더는 LOFT 파트에서 로프트 축 방향으로만 듣는다.** `js/drone-params.js`의
  `applyPartField()`가 `outer_profile`만 축척하고 `loft_sections`를 건드리지 않는다.
  기존 결함이고 배포된 샘플의 LOFT 파트 다수가 이미 그 상태다. 교체된 파트가 LOFT가
  되면서 적용 대상이 늘었을 뿐, 새로 생긴 것은 아니다. (→ v32 결함 B에서 해소)
- **코드 패널은 LOFT를 `BoxGeometry`로 방출한다.** 교체 전 LOFT 파트도 같았다. 이제
  그 상자가 최소한 올바른 세 치수를 갖는다.
- **잃은 2pp는 되찾을 수 없다 — 되찾으면 안 된다.** 그 자리를 덮으려면 참조에 있고
  사양서에 없는 저후방 기하를 실제 파트로 설계해야 한다. 배터리를 다시 부풀리는 것은
  점수를 올리고 모델을 망가뜨리는 일이다.

## v30 — 6분을 기다리는 사람에게 서버의 진짜 단계를 보여주고, 샘플을 10종으로 늘린다

두 갈래 작업을 한 커밋에 담았다. 하나는 **생성 진행 표시**, 하나는 **신규 드론 4종**이다.

### 진행 표시 — 붙잡지 않는 구조로 바꿨다

2단계 생성은 온프렘 실측으로 **232~434초**다. 그동안 화면은 `(1~3분)`이라고만 적혀 있었고,
그 뒤로는 아무 소식이 없었다. 더 나쁜 것은 구조였다 — 클라이언트가 응답 하나를 6분 동안
붙잡고 있었고, 1바이트도 흐르지 않는 커넥션은 `UND_ERR_HEADERS_TIMEOUT`(300초)에 걸렸다.
`tools/build-samples.mjs`는 이 함정을 이미 우회해 두고 있었는데, 계측 스크립트가 그대로 밟아
드러났다.

- `server.mjs`: 진행 기록 레지스트리(TTL 30분)와 `jobOpen/jobStage/jobDone/jobSnapshot`.
  `handleSpecJson`을 래퍼로 쪼개고 `runSpecJson(body, job)`을 신설했다. `jobId` + `async:true`면
  **즉시 작업 번호만 돌려주고** 백그라운드로 실행한다(`runSpecJob`이 모든 실패를 기록으로 흡수).
  단계 마커는 `inventory → inventory_recheck → spec → validate → repair(회차·지적건수) → finish`,
  드론·일반 두 경로 모두에 넣었다. `GET /api/spec-progress?id=`, `GET /api/spec-result?id=`.
  **`jobId` 없는 호출은 기존 동기 동작 그대로**라 `build-samples.mjs`는 영향받지 않는다.
- `js/app.js`: `requestSpecJson()`이 POST(120초, 업로드만) → 1초 폴링 → 결과 수령으로 바뀌었다.
  무한 대기가 사라졌고 상한은 20분, 폴링 20회 연속 실패면 "연결이 끊겼습니다 (작업 번호 …)".
  진행바는 단계 밴드 안에서만 지수적으로 기어가 다음 단계를 침범하지 않는다. 실패 문구는
  7초 유지한다(`holdGenFailure`) — 6분 기다린 사람에게 토스트 3.4초는 모자란다.
- 문구 `(1~3분)` → `(보통 4~7분)`. 어차피 1초 뒤 서버의 실제 단계로 교체된다.

정적 데모(`docs/index.html`)는 2단계가 `sleep(900)` + 미리 만든 사양서라 1초도 안 걸린다.
문구를 돌리면 깜빡이기만 해서 **건드리지 않았다.**

**단축은 하지 않기로 했다.** 비중은 인벤토리 19~24초(5~9%) · 사양서 118~153초(28~51%) ·
수정 85~282초(32~65%) · 검증/정리 0.0초다. 인벤토리를 병렬화해도 2~4%인데 인벤토리가 커지면
`inventoryGaps` 지적이 늘어 가장 비싼 수정 라운드가 길어진다. 수정 라운드는 가장 큰 조각이지만
품질을 버는 조각이다 — 2라운드를 다 돈 두 실행 중 하나가 6건→2건→**0건**으로 유일하게 완전
수렴했다. 다만 **5회 중 3회는 첫 수정 라운드가 지적을 줄이지 못해 `after.length >= errors.length`
가드에 걸려 결과가 통째로 버려졌다**(85~141초 소모). 돌려보기 전엔 알 수 없어 지금 자르면 품질이
떨어지지만, 진짜 단축 여지는 "어떤 error_code일 때 수정이 실효가 있는가"에 있다.

### 신규 4종 — 분류 3축이 겹치지 않게 골랐다

| id | 도메인 | 임무 | 기체형상 |
|---|---|---|---|
| `cage-inspect` | 민간·산업 | 실내·밀폐공간 점검 | CAGED_MULTIROTOR |
| `fpv-racer` | 취미·레저 | FPV 레이싱 | QUAD_X |
| `fire-octo` | 공공안전 | 중량물 운반(방재) | OCTO_X8 |
| `relay-hexa` | 방산 | 통신 중계 | HEXA |

임무는 넷 다 기존 6종에 없던 것이고, `CAGED_MULTIROTOR`는 택소노미에서 한 번도 안 쓰인
기체형상이었다. 택소노미에 틸트로터가 없어 후보에서 뺐다 — 넣으려면 `server.mjs`의 플랫폼
enum과 레시피를 건드려야 한다.

유사도(참조=1단계 메시, 뷰별 IoU): cage-inspect 60/62/60/62 **61%** · fire-octo 42/54/42/54
**48%** · fpv-racer 36/50/36/50 **43%** · relay-hexa 29/24/29/24 **26%**.
참조=사진 실루엣: fpv-racer **59%** · cage-inspect **52%** · fire-octo **46%** · relay-hexa **25%**.

**정면≡후면, 좌측≡우측이 같은 값으로 나오는 것은 필연이다** — 실루엣은 깊이가 없어 반대편
외곽은 정확한 거울상이고 CAD도 메시도 외곽이 대칭이다. 네 장이 진짜 독립인 것은 손으로 그린
시트뿐이고, 거기서 유일하게 갈린 게 fpv-racer 후면(53 vs 정면 60)이다.

절제 실험으로 원인을 확정했다.
- **fpv-racer 정면·후면 36%**: `part_009 통신 안테나`(80mm, x=−25°) 하나 때문이다. 빼고 재면
  43/43/43/43으로 균일해지고 세로 과잉이 +40%→−8%로 사라진다. 그리고 `refine-specs`는 회전값
  있는 파트를 측정하지 않으므로 **이 오차는 파이프라인 안에서 고쳐지지 않는다.**
- **relay-hexa 25~26%(최하)**: 테더 탓이 아니었다(참조에서만 빼면 12%, 양쪽에서 빼면 20%로 더
  나쁘다). 사양서가 안테나 마스트(105mm)와 랜딩기어(220mm)를 사진의 절반으로 적었다. 로터 스팬은
  입력 치수에 묶여 정확한데 실루엣 정규화가 최대 치수 기준이라 세로 오차가 네 뷰를 동시에 깎는다.
  **형상이 아니라 치수가 틀렸다**는 증거로 `fit-spec --ref views --rounds 1 --dry`가
  24.92% → **36.87%(+11.95pp)**를 낸다(파일에는 쓰지 않았다).
- **cage-inspect 경계 7.3%(최악)**: 사양서가 케이지를 두께 5mm 구형 셸로 추상화했다. 참조는 로드
  격자, CAD는 꽉 찬 원판이라 IoU 상한이 로드 면적비로 눌린다. **비율은 열 종 중 가장 정확하다
  (종횡비 1.8%, 축오차 ±3%).** 표현이 다를 뿐이다.

### 검증

- `qa-samples` **10/10**, `qa-vocab` 전부 통과. 로터 인식 4/4/8/6 정확.
- 회귀 기준선 유지: `similarity.mjs inspect-quad agri-hexa map-wing sar-vtol` → **52 / 57 / 69 / 61**.
- 공급사명 검사 **0건**(추적 파일 + 신규 미추적 파일 모두). `docs/js/` 34개가 `js/`와 버전 쿼리만
  빼고 바이트 동일. 스탬프 `396db0bf`(재실행 0파일 = 안정).
- 브라우저에서 정적 데모로 네 종 모두 라이브러리 → 메시 → CAD → 시뮬 → 프린팅 통과, 콘솔 에러 0건.

### 바이너리에 공급사명이 남아 있었다 — 커밋 직전에 잡았다

텍스트 검사는 계속 0건이었는데 **신규 GLB 4개와 PNG 4장이 파일 안에 공급사명을 싣고 있었다.**
GLB는 `asset.generator`와 material·mesh·node 이름에 5회씩, PNG는 25~29KB짜리 C2PA 출처 매니페스트
(`caBX` 청크)에 들어 있었다. 에디터에 보이지 않아 `git grep`이 잡지 못한다 — 기존 메시 4개가 이미
`https://vmesh3d.ai`로 세척돼 있었으니 관례는 있었으나 **강제하는 도구가 없어서 새 자산이 그대로
통과했다.**

`tools/scrub-assets.mjs`를 만들어 게이트로 세웠다.
- **GLB는 같은 길이 치환**이다. GLB는 헤더에 JSON 청크 길이를 적고 4바이트 경계로 패딩하므로 길이가
  바뀌면 두 길이와 패딩을 다시 써야 한다. 치환 전후가 똑같이 5바이트라 오프셋이 하나도 움직이지 않아
  컨테이너가 깨질 수 없다. 실제로 **네 파일 모두 바이트 크기가 그대로**여서
  `docs/assets/meshes/index.json`의 `bytes` 8종이 전부 유효하다.
- **PNG는 메타데이터 청크만 통째로 뺀다.** IHDR·IDAT·IEND는 건드리지 않으므로 화소가 동일하고,
  그 사진에서 이미 뽑은 실루엣·유사도 수치가 그대로 유효하다. 공급사명이 든 청크만 지운다
  (무관한 tEXt 설명은 남의 내용이라 지우지 않는다).

세척 뒤 BIN 청크 SHA와 IHDR+IDAT SHA가 **8개 전부 세척 전과 동일**함을 확인했고, `qa-samples` 10/10과
회귀 기준선 52/57/69/61도 그대로다. 검사 모드(`--check`)는 남은 게 있으면 exit 1이다.
PNG는 매니페스트가 빠져 4장 합계 5.16MB → 5.06MB가 됐다.

`docs/assets/views/`의 렌더는 처음부터 깨끗했다. 온프렘 `mesh-svc/serve.py`도 주석에 공급사명이
남은 옛 판이라 이번 배포에서 교체됐다(저장소 판은 이미 "메시 클라우드"로 고쳐져 있었다).
`mesh-svc/TRELLIS/`의 검출은 서드파티 라이브러리(미추적)라 손대지 않았고, `config.local.json`과
`server.log`는 gitignore 대상이다.

### 남은 한계 (손대지 않았다)

- **비교가 신규 4종에 불리하다.** 기존 4종의 52/57/69/61은 `fit-spec`을 거친 뒤의 숫자고, 신규
  4종은 6단계만 돌렸다(측정은 `--dry`). relay-hexa 한 종만 1라운드 돌려도 +11.95pp였다. 억지로
  숫자를 맞추지 않으려고 파일에는 쓰지 않았다.
- **`refine-specs`가 이 4종에서 순이득이 아니다.** mesh 평균 43→45인데 views 평균은 **49→46**이다.
  메시에서 단면을 재는 게 항상 좋지는 않다.
- **`refine-specs`가 검증 오류를 만든다.** `size_mm`·`loft_sections`만 고치고 `outer_profile`을
  두어 `PROFILE_SIZE_MISMATCH`·`BUILDER_STRATEGY_MISMATCH`가 생긴다(fpv-racer 0→5건,
  relay-hexa 0→2건). 기존 4종도 같은 상태라 새 회귀는 아니지만 3단계 노트에 그대로 노출된다.
- **`build-views`의 solidity 하한 0.45가 개방 구조 기체를 배제한다.** cage-inspect는 사진 경로에서
  3회 연속 기각(0.39~0.41)돼 메시로 폴백했다. 케이지 드론의 실루엣은 원래 뚫려 있는 게 맞다.
- **조립 연결성은 두 검사가 서로 다른 답을 준다.** fire-octo는 서버 validator 2덩어리 / 시뮬 6덩어리,
  relay-hexa는 1 / 3이다. 기존 `isr-wing`도 1 / 3으로 엇갈린다. 별개 이슈다.
- **사양서 오독**: fire-octo는 소화 노즐을 "전방 돌출 튜브", 호스 릴을 "케이블 릴 페이로드"로 적어
  형상은 잡았으나 임무 탑재체로 인식하지 못했다. relay-hexa는 테더를 `part_011`·`part_018` 두 파트로
  이중 기재했다.
- **저장소가 46.2MB 늘었다** (GLB 40.9MB + PNG 5.2MB). 기존 메시 총량 33MB의 두 배가 더해져
  `docs/`가 92MB가 됐다. GitHub Pages 상한 1GB에는 여유가 있지만 샘플당 6~14MB라 증가는 선형이다.
  **신규 사진이 PNG(장당 1.2~1.35MB)인 것이 기존 JPEG(장당 0.02MB) 관례와 어긋난다** — 재인코딩하면
  5MB를 거의 없앨 수 있으나, 그 사진에서 메시·실루엣·유사도 수치가 이미 나왔으므로 이번에는
  건드리지 않았다.
- **온프렘 앱의 칩 목록은 그대로다.** `js/app.js`의 `DRONE_SAMPLES`와 `assets/samples/drones/`에는
  신규 4종을 넣지 않았다. 요청 범위 밖이라 신규 4종은 `docs/` 정적 데모의 라이브러리에만 나타난다.

## v31 — 공급사명을 생성 경로에서 끊고, 사양서 단계가 왜 오래 걸리는지 실측한다

두 갈래 작업을 통합 검증해 함께 올린다. 하나는 **런타임이 만들어 내는 파일**에 공급사명이 남던
것을 근원에서 막는 일이고, 다른 하나는 **2단계(사양서)가 3~8분 걸리는 것이 오류인지** 실측으로
답하는 일이다. 둘 다 `server.mjs`를 건드렸으나 겹치는 구간이 없다(전자는 import·메시 기록·`json()`
직렬화, 후자는 `inventoryGaps()`·진단 로그).

### 텍스트 검사가 0건인데 화면에는 공급사명이 떠 있었다

v30에서 커밋된 바이너리를 세척했는데도 데모의 파트 목록에 공급사명이 그대로 나왔다. 이유는
**세척이 커밋된 파일만 대상으로 했기 때문이다.** 두 메시 엔진 모두 클라우드가 준 바이트를
`generated/`에 그대로 썼으므로, 실행할 때마다 브랜드가 박힌 파일이 새로 태어났다. 저장소를
아무리 grep해도 0건이 나오는 게 당연했다 — 브라우저가 받는 파일은 저장소에 없었다.

`glb-sanitize.mjs`를 새로 두고 **두 엔진을 `writeGeneratedMesh()` 한 함수로 모았다.** 나중에 세
번째 엔진을 붙이는 사람이 세척을 잊어도 이 문을 지나지 않을 수 없게 하려는 것이다. glTF JSON
청크를 파싱해 다시 쓰고 청크·파일 길이와 4바이트 패딩을 재계산한다. BIN 청크는 오프셋이
의미를 지니므로 **같은 길이 치환만** 한다.

파트 이름은 `part_1`, `part_2` … 로 바꾼다. `js/app.js`의 `markParts()`가 이미 쓰는 서수 관례라
두 경로가 어긋나지 않고, ASCII인 것도 의도다 — `exportDXF`/`exportOBJ`/`exportSTEP`이 통과시키는
`sanitize()`가 비ASCII를 `_`로 바꾸므로 한글 이름은 레이어명에서 `___ 1`이 된다.

GLB 말고 **한 군데가 더 있었다.** 핸들러가 전부 `catch (e) => { error: e.message }`로 끝나는데,
상류의 DNS·TLS 실패는 자기가 닿지 못한 호스트 이름을 메시지에 적는다. 아무도 그 문자열을 일부러
쓰지 않으므로 핸들러 코드를 읽어서는 절대 찾을 수 없다. 응답을 직렬화하는 단 하나의 지점인
`json()`에 세척을 걸었다. 토큰이 실제로 있을 때만 돌고, base64·data URI 구간은 건드리지 않는다 —
우연한 일치를 지우자고 이미지를 깨뜨리는 쪽이 더 나쁜 거래다.

### 재발을 막는 도구를 함께 둔다

`tools/qa-vendor-scan.mjs`는 두 짝이고 둘 다 적발 시 exit 1이다.

- 인자 없음 → **디스크의 모든 파일, 바이너리 포함.** 텍스트 grep이 GLB 안을 못 본 것이 1차 실패였다.
- `--server <base> [url …]` → **돌아가는 서버가 내주는 모든 바이트.** 페이지가 스스로 참조하는
  `src`/`href`를 따라가고, 헤더도 함께 본다. 커밋된 파일만 보는 검사가 2차 실패였으므로 이 짝이 없으면
  같은 사고가 다시 난다.

제외 항목은 결과와 **함께 출력한다.** 보이지 않는 제외야말로 검사가 초록불인데 사고가 나는 경로다.

### 사양서 단계는 오류가 아니라 구조다 — 완주 8건 실측

온프렘에서 2단계만 8건 완주시켰다(사진 재사용, 메시 생성 없음). **실패·타임아웃·폴백 전환 0건.**
평균 356초, 실측 구간 3:21~8:15.

시간 구성(8건 평균): 인벤토리 11.5s · 재조사 12.6s · 사양서 129s · **수정 205s(라운드 1.6회)**.
수정 라운드가 전체의 **57%**다. 수정 1회가 최초 작성과 맞먹는 이유는 구조 그대로다 — 원본 메시지
전체 + 이전 사양서 24,000자를 싣고 **처음부터 다시 쓰는** 호출이기 때문이다.

라운드 13회 중 **5회(38%)가 통째로 폐기**됐고 그 값이 523초, 전체 시간의 18%다. 원인은 두 겹이다.
첫째, **모든 라운드가 새 오류를 만든다** — 라운드 후 잔여 오류 K는 13회 모두 0이 아니었다. 둘째,
**가드가 오류 "개수"만 본다.** `DETAIL_ITEM_MISSING`과 `ASSEMBLY_DISCONNECTED`는 여러 결함을 객체
하나로 묶으므로, 파트가 11→18로 늘고 세부 가중치가 8→3으로 준 결과가 "개수 3→3"이라는 이유로
버려진다. 폐기된 후보를 회수해 채점해보니 **IoU 27% vs 채택본 22%** 였다 — 버린 쪽이 나았다.

### 고친 것 하나 — `inventoryGaps()` 오탐

`DETAIL_ITEM_MISSING`만 유독 안 고쳐지는 이유를 파고들었더니 **지적 자체가 틀렸다.** 3개 실행의
지적 11건 중 **8건이 이미 존재하는 파트**였다. 원인 두 가지다.

- **`norm()`이 `·`를 지운 뒤에 `split(/[·/]/)`을 했다.** 분리자가 이미 삭제돼 split이 한 번도
  동작하지 않았다. `"캐노피·셸"`이 `"캐노피셸"` 한 덩어리로 매칭돼, 파트 이름이 정확히 `"캐노피"`인
  사양서에 대해 "없는 부품"으로 보고된다.
- **프롬프트가 준 면제를 검증기가 안 지켰다.** 프롬프트는 LED·파스너 같은 극세 요소를 인접 파트에
  통합해도 된다고 허용해놓고, 검증기는 그것을 누락으로 지적했다. 허락한 일을 하고 벌점을 받는 구조다.

정규화 **전에** 분리해 대안 이름 중 하나라도 맞으면 존재로 인정하고, 프롬프트가 명시한 면제 대상을
지적에서 뺐다. 이 변경은 **지적을 제거만 하고 추가하지 않는다**(단조). 수집한 사양서 6개로 구·신
매처를 오프라인 재생해 **11건 → 7건, 새로 생긴 지적 0건**을 확인했다 — 오류 개수를 늘릴 수 없으므로
라운드를 더 유발할 수도 없다.

사후 실행이 2건뿐이라 **시간·품질 개선은 주장하지 않는다.** 확실한 것은 오프라인 재생으로 증명된
단조성과, 지적 목록에서 면제 대상이 설계대로 사라진 것뿐이다.

### 단축은 하지 않았다 — 표본이 모자란다

가장 값어치 있는 절감은 **수정 라운드의 사고 수준·사진 재전송 축소**(전체의 57%)인데 전혀 계측되지
않았고 품질 위험이 실재한다. "잔여 오류 ≤2면 라운드 생략"은 기제가 명확하나 경계 관측이 2건뿐이다.
**추측으로 자르지 않았다.** 대신 계측에 필요한 로그 전용 진단(`[repair-diag]`, `[inv-diag]`,
`codeTally`/`granularWeight`)을 남겼다 — 동작은 바뀌지 않는다.

권고 하나: 시드 목록의 `기체 상판/하판 분리선`은 부품이 아니라 **분할선**이다. 8/8 실행에서 누락
지적으로 등장하고 끝까지 해소되지 않는다. 시드 목록은 수정 루프 밖이라 이번에 손대지 않았다.

### 검증 — 공급사명 3층위

| 층위 | 대상 | 결과 |
|---|---|---|
| 소스 | `git grep -il`(추적 + 미추적) | **0건** |
| 커밋 바이너리 | 커밋 내용 그대로의 격리 워크트리 259파일 / 105MB (GLB 8·PNG 포함) | **0건** |
| 런타임 | 온프렘 디스크 295파일 / 274MB · `generated/` 168MB · 서버가 내준 26 URL(페이지·스크립트·`/api`·GLB·헤더) | **0건** |

- `qa-samples` **10/10** · `qa-vocab` **전부 통과** · 회귀 기준선 **52 / 57 / 69 / 61** 유지.
- 검사기가 헛돌지 않음을 확인했다: 수정 전 라이브 URL에 걸면 `적발 … (1종, 12120096B)`이 나온다.
- 라이브 생성 1회(61초)로 끝단 확인 — 서버 로그 `[mesh-scrub] … (gltf) 4건`, HTTP로 받은 바이트
  **0건**, `asset.generator: "VRINGON CAD"`, 파트 목록 이름 **`part_1`**. 삼각형 410,412개와 네 맵
  (`map`·`normalMap`·`roughnessMap`·`metalnessMap`), 경계 상자 모두 보존.

### 남은 한계 (손대지 않았다)

- **커밋된 샘플 8종의 파트 목록은 아직 `vmesh_node_<uuid>`다.** 공급사명은 없으나 읽을 수 있는
  이름도 아니다. `--rename-opaque`를 적용해 기하 무영향(기준선 동일, qa-samples 10/10)임을 측정한
  뒤 **원래 바이트로 되돌렸다.** 커밋된 배포 바이너리를 다시 쓰는 일이라 판단을 남긴다:
  `node tools/scrub-assets.mjs --rename-opaque`. 이후 `build-meshes.mjs` 실행은 자동으로 고친다.
- **서버 기동 배너가 모델명을 `server.log`에 찍는다.** 서버 전용이라 브라우저에 닿지 않고
  `config.local.json`은 설계상 같은 이름을 담는다. 출력에 이름이 남는 유일한 자리다.
- **`docs/index.html`의 빌드 스탬프가 커밋된 소스와 어긋난다(선재).** HEAD의 스탬프는 `396db0bf`인데
  커밋된 `js/`·`docs/specs/` 블롭으로 `stamp-version`을 다시 계산하면 `05234a97`이 나온다(줄바꿈
  변환과 무관하게 재현된다). 이번 커밋은 브라우저에 나가는 바이트를 하나도 바꾸지 않으므로 캐시를
  깰 이유가 없어 **스탬프를 건드리지 않았다.** `js/`를 바꾸는 다음 커밋이 함께 바로잡는 것이 맞다.
- **`qa-vocab`의 실패 3건은 이 커밋과 무관하다.** 작업 트리에 있던 **다른 작업자의 미커밋
  `js/mesh-loft.js`**(회전 파트 측정 작업)에서 나온다. 커밋 내용만 담은 격리 워크트리에서는
  `qa-vocab`이 전부 통과한다 — 앞선 보고가 이를 "기존 실패"로 잘못 귀속했다.

## v32 — 슬라이더가 세 축을 다 듣고, 회전 파트가 측정에 들어오고, 잘못 쓰인 치수 넷을 되돌린다 (2026-08-16)

네 결함을 한 커밋에 담았다. 둘은 코드(B·C), 둘은 배포 사양서의 잘못 쓰인 치수(A·D)다. `server.mjs`·`glb-sanitize.mjs`·
`tools/build-meshes.mjs`·`tools/scrub-assets.mjs`·`tools/qa-vendor-scan.mjs`·`README.md`는 v31 워크플로 소유라
건드리지 않았다(그쪽은 781ca3c로 이미 커밋·배포됨).

### 결함 B — 크기 슬라이더가 LOFT 파트에서 로프트 축 방향으로만 들었다 (`js/drone-params.js`)

LOFT 파트는 `size_mm`에서 축 길이만 읽고 단면 두 방향은 `loft_sections`의 `w_mm/h_mm`에서 읽는다. 그래서
`size_mm` 편집만으로는 축 외 두 방향이 그림에서 전혀 안 움직였다(v29 "남은 한계"에 적어 둔 그 결함).

- `import { loftAxis } from "./mesh-loft.js"` — 축 정의는 그 한 곳만 쓴다.
- 새 헬퍼 `syncLoftSections(g, before)`: 편집 전 `size_mm` 스냅샷으로 축(a0)과 편집 후 축(a1)을 구한 뒤,
  축이 같으면 단면 방향(`[w,h,d]`에서 축을 뺀 순서: `w_mm`↔첫째, `h_mm`↔둘째)을 `now/before` 비율로,
  축이 바뀌면(근사 정육면체 파트에서 슬라이더로 쉽게 일어난다 — 케이지 412×392×395의 H×1.2) 옮길 프로파일이
  없으므로 단면 최대값을 새 단면 치수에 맞춰 **`size_mm`=월드박스** 계약을 지킨다. 0/음수·섹션 없음(isr-wing
  part_001은 LOFT지만 sections null)·`w/h` 별칭 키 안전.
- `applyPartField`의 `size_mm.*` 편집과 `applyParameter`의 LOFT를 만지는 7분기(wheelbase의 암, rotor_diameter,
  arm_length, wingspan, gear height, body w/h/l, affects 균일 스케일) 전부에 같은 처리. `scaleProfile`은 그대로.

전후(헤드리스, 파트 로컬 정점 박스, W/H/D 각각 ×1.2): LOFT 34파트 × 3축 = 102 편집에서 **이전 68 실패**(파트당
축 외 2방향 무반응, 케이지는 축이 뒤집혀 다른 축까지 0.951배 이동) → **이후 1건**. 그 1건은 inspect-quad
`part_lower_shell` — 배포 데이터 자체가 섹션 h_mm 최대 158.1 > size_mm.h 102.8인 박스 불일치라 W×1.2에서
축이 d→h로 넘어가며 렌더가 정확히 size_mm로 스냅된 것이고 슬라이더 결함이 아니다(relay-hexa part_013,
sar-vtol 전방 스트럿도 같은 종류였으나 A·D에서 교정). 파라미터 경로는 10개 사양서 전 파라미터 ×1.2에서 LOFT
20건 중 **13건이 size_mm를 안 따랐다** → **0건**. 30단계 드래그 == 단일 점프(8.5e-14), 편집→되돌리기 원복
(1.4e-14), 비-LOFT 동작 불변.

### 결함 C — 회전 파트가 좌표 측정에서 통째로 빠졌다 (`js/mesh-loft.js`, `tools/refine-specs.mjs`)

`refineFromMesh`가 `rotation_deg`가 있는 파트를 건너뛰었다. 이유는 정당했다 — 기울어진 솔리드의 월드축 폭은
회전 전 박스(`size_mm`)보다 항상 크고, 돌릴 때마다 부풀었다. 그런데 그 대가로 fpv-racer의 안테나처럼 메시가
명백히 반박하는 파트가 작가값 그대로 실루엣 높이를 끌고 갔다.

- 파트마다 "프레임"으로 메시를 읽게 일반화. 회전 없는 파트는 옛 경로 그대로(산술식 동일). 회전 파트는
  `partFrame(g)`→{pos,R}로 프레임을 만들고 `localFrame()`이 전 메시 점을 축별 fit으로 mm 변환한 뒤 Rᵀ로 파트
  로컬 좌표계에 놓는다(비등방 fit 뒤에 회전이라 순서 고정). 그 뒤 같은 루프: PADS 확대, n≥24, `boxAgreement
  ≤ 1/(1+pad)`, 비율 0.4~2.5, 단면 측정, NEVER_LOFT — 가드 전부 그대로.
- 프레임 합성은 컴파일러 `instanceXform`과 동일: Euler XYZ(Rx·Ry·Rz) → MIRROR_PAIR는 x=0 힌지, CIRCULAR는 배치
  요를 뒤에 곱함. **회전각 자체는 측정하지 않는다** — 작가값 유지. 싱글톤 중심 보정은 로컬 축별 가드 통과
  성분만 R로 되돌려 월드 중심에 더한다(기울어진 안테나가 길이 방향으로 낮게 잡히면 기울기 방향으로 미끄러진다).
  로그 표기 `(회전·치수)`/`(회전·N단면)`, `refine-specs`는 회전 파트마다 `↻ 이름 회전(x,y,z)° · before → after`.
- 프레임 정확성: 사양서를 자기 CAD로 컴파일해 인스턴스 정점을 프레임에 넣으면 선언 크기가 정확히 재현된다 —
  싱글톤(fire-octo 안테나 10×180×10, 월드박스라면 134×99×99), MIRROR_PAIR 힌지(relay 다리 16×220×16, pos
  75.2,139.6,0 = 컴파일러), CIRCULAR 6링+틸트(46×40×46).
- 회전 없는 파트: 8샘플 전 파트 before/after 덤프가 **완전 동일**(치수·중심·로프트·리스트·yaw·scale).
- `tools/qa-vocab.mjs` 6절이 옛 계약("회전 파트는 건너뛴다")을 단언하고 있어 새 계약으로 바꿨다: map-wing 동체에
  z 8°를 주면 회전각 불변, 자기 좌표계로 171×122×570(무회전 171×121×570, 월드박스면 186×144)로 읽히고
  `measured`에 `유선형 동체(회전·8단면)`, 측정 수 동일(10). 옛 코드로 돌리면 세 단언이 실패한다.

측정 결과 자체는 채택 판단이 따로 필요했다(`--dry`):
- **fpv-racer 안테나** 4×80×4@(0,95,−35) rot(−25,0,0) → 6×80×5@(0,83,−29). pad 0.12 박스 41점이 전부 로컬
  y≈−13 — 안테나 선이 캐노피/배터리 윗면을 뚫는 지점의 표면 조각이지 안테나가 아니다(길이 판독 1.1은 비율가드로
  기각). 메시의 진짜 안테나는 굵기 ~2.5·높이 33~45mm로 60mm 뒤(|z|≈80~100)에 있어 확대망 z 반폭 8mm로 못 잡는다.
  사진(`fpv-racer.png`)은 배터리 팩 뒤끝의 ~80mm 휩 안테나가 뒤로 기운 모습 = 현재 사양서와 일치. **채택하지 않았다.**
  적용하면 메시 43.12→46.70이지만 views 59.23→55.59, 그리고 사진과 어긋난다.
- **fpv-racer FPV 카메라** 18×18×22 rot(30,0,0) → 20×20×22: 캐노피 코 표면, 저위험. 파일 미변경(fpv-racer는 이번에 안 씀).
- **relay-hexa 다리** 16×220×16 rot(0,0,−20) → LOFT 18×249×21(5단면): 지름은 메시와 부합, 길이 증분은 동체 셸
  관통이라 5단면 로프트는 근거 약함 → D에서 사진 기준으로 다시 정함.
- **fire-octo 안테나** 10×180×10 rot(45,0,45): 이제 시도되지만 pad0.12 n=13, 부합도 0.62/0.26 → `내부(메시 없음)`
  정상 기각(변화 없음).

### 결함 A — 측정이 이웃을 읽어 쓴 치수 둘 (`tools/refine-specs.mjs` RESET_TABLE, 사양서 2종)

`resetInternal`이 선택 필드 `plane/profile/rotation/spacing`을 쓰도록 4줄 확장, 근거 종류 `사진` 추가. 전부
`node tools/refine-specs.mjs --reset-internal <id>`로 적용(재실행 바이트 동일 = 멱등).

- **A-1 map-wing 추진 모터** 27×5×79 LOFT(5단면) → **CYLINDER 35×35×40 @(0,145,−160)**. 5mm 높이 모터는 없다 —
  박스가 프롭과 마운트를 읽었다. 근거: 카탈로그 mot_2814(2814 900KV, 9~11인치) 35×35×40, 최초 사양서 선언과
  동일. 푸셔라 축 길이 40이 d, plane FRONT 유지. 재측정 판정 `내부(메시 없음)`으로 기각(재팽창 안 함).
  유사도(메시) 68.61 → **68.24**(측면 82.5→81.8): 예전 5mm 슬리버는 실루엣에 안 보였는데 진짜 모터가 동체 등
  위로 서고, 참조 메시는 모터 덩어리를 z≈−255·y≈118(마운트 뒤끝)에 둔다. 위치까지 메시대로 옮기면 68.81로
  회복되지만 지시가 치수라 위치는 선언값 유지. **기준선 69 → 68.**
- **A-2 sar-vtol 랜딩기어 지지대** 전방 LOFT 26.6×46×40 @(61,109,250)·후방 461×161.6×40 @(0,116.8,−241.5) →
  둘 다 **EXTRUDE_2D 550×260×50** 아치, 전방 @(0,160,190)·후방 @(0,160,−185). 프로파일은 최초 커밋의 작가
  아치(다리 6%·크라운 13%)를 박스에 맞춰 스케일하는 `archProfile()` — 프로파일·size_mm 불일치 원천 차단.
  근거: 원본 사진 `sar-vtol.jpg`(스키드 중앙에서 동체 밑, 짐벌 뒤·주익 뿌리 앞전까지 바깥으로 벌어지는 곡선
  블레이드 지지대 좌우 한 쌍) + 메시(그 스테이션 z≈150~260, 스키드 윗면 y≈20→동체 밑 240, 다리 폭 30~40).
  폭 550 = 스키드 간격 520+다리, 높이 260 = 스키드 윗면 36(5.7 매입)→동체 밑 272(+18 매입).
  후방은 사양서 선언 "전방과 동일, 후방 배치"를 유지해 동체 중심 대칭·스키드 끝(−215) 위 −185. **사진에는
  후방 아치가 없다** — 절제 −1.43(낭비 50) → −0.95(낭비 30)로 줄었을 뿐 남는다. 두 아치를 z 225/175에 붙여
  사진의 한 블레이드로 두면 메시 61.8·views 46.4로 더 좋지만 두 파트가 한 부재의 앞·뒤 절반이 되는 셈이라
  채택하지 않았다.
  결과: 연결성 **3덩어리 → 1**, 검증 8 → 6건(전방 BUILDER/PROFILE 불일치 해소), 절제 전방 −0.10 → **+1.07**.
  유사도 메시 61.38 → 61.20(정면 60.5→58.8: 참조 다리는 스키드 간격 ~640, CAD 스키드 520이라 아치 다리가 안쪽 /
  측면 62.3→63.6), views 43.86 → **45.41**. 기준선 61 유지.

### 결함 D — relay-hexa 마스트·다리가 사진의 절반 (사양서 1종)

v30이 "형상이 아니라 치수가 틀렸다"고 진단해 둔 그 샘플. 측정 → 피팅 순서로 했으나 피팅은 기각하고 사진 기반
교정을 채택했다.

1. **측정 적용**(C 회전 측정 포함) 11파트 중 물리적으로 맞는 것만 유지 — 캐노피 71→82, 분리선 두께 10→15,
   페이로드 181×100×137@203 → 204×100×156@163, 모듈·기둥 지름. 기각(RESET_TABLE `사양서 선언` 복원): part_003
   하단 셸(REVOLVE→8단면 LOFT: 셸 아래 페이로드·테더를 읽음), part_011 테더 스텁(y 50→−1), part_012 하부 기둥
   (118→94로 오히려 짧아짐), part_018 테더 전원선(738@271, 마스트 위로 나옴), 다리 5단면 LOFT. 측정은 메시를
   사양서 y 범위에 축별로 끼워 맞추므로 **전체 높이 부족은 원리적으로 못 본다**(fit y 0.00135 vs x 0.00068).
2. **피팅 기각**: `fit-spec --ref views`가 전부 프레이밍 아티팩트를 쫓는다. v30의 24.92→36.87(1라운드)을 재현하니
   +11.95 중 **7.6pp가 part_018 테더선 이동**(center.y 186→133 +5.84, h 878→948 +1.76)이고 마스트·다리 노브는
   0.5pp 미만. 원인: 참조 4뷰는 테더가 프레임 바닥까지 그려져 세로(≈1900)로 정규화되고 CAD는 가로(1268)로
   정규화돼, 마스트·다리를 맞게 키울수록 표준 IoU는 떨어진다.
3. **채택**(사진 `relay-hexa.png` + 4뷰 실루엣, 로터 스팬 679px=1280): 하부 기둥 118→**200**, 모듈 2개 → 50 정육면체
   (사양서 '큐브형', 사진 ≈50), 상단 기둥 105→**300**(모듈 관통, 캐노피 위 마스트 289→508mm), 다리 16×220 rot z −20
   sp160 → 18×**340** rot z **+25** sp380(기존 부호는 발이 안으로 모이는 V자였고 스키드와 117mm 떨어져 스키드가
   별도 덩어리였다; 이제 뿌리 ±100·발끝 ±244·수직 308), 스키드 y 64→−52 sp 310→488. USER_PROVIDED 휠베이스 900
   불변 — fit-spec이 이를 볼 수 있도록 part_005(모터 링 450)에 `wheelbase=900mm USER_PROVIDED` dimensions 추가.
4. 결과: 메시 28.9/23.9/28.9/23.9=**26.40 → 34.4/33.5/34.4/33.5=33.93**(ΔY −31%→−12%). 표준 views 24.92 → **14.62**
   (위 아티팩트: 테더를 양쪽에서 뺀 진단은 20.04 → 32.67, CAD 테더를 참조처럼 페이로드 밑에 늘어뜨린 가정은
   15.72 → 42.54). 검사: 연결성 3→**1덩어리**, 전복각 16°(실패)→**25°(통과)**, 지지반경 155→244.

### 검증

- `qa-samples` **10/10** · `qa-vocab` **전부 통과**(6절 3건을 새 계약으로 재작성).
- 유사도 10종(참조 메시 / 사진 실루엣, 평균 IoU %). delivery-octo·isr-wing은 1단계 메시·실루엣이 등록돼 있지
  않아 대상 밖(qa-samples로만 검증). 회전 파트 없는 4종과 사양서 미변경 2종은 **바이트 동일**.

  | 샘플 | 메시 v31 → v32 | 실루엣 v31 → v32 | 비고 |
  |---|---|---|---|
  | inspect-quad | 52.44 → 52.44 | 46.91 → 46.91 | 동일 |
  | agri-hexa | 57.35 → 57.35 | 55.81 → 55.81 | 동일 |
  | map-wing | 68.61 → **68.24** | 64.68 → 64.31 | A-1 모터 실물 크기(위치는 선언값) |
  | sar-vtol | 61.38 → 61.20 | 43.86 → **45.41** | A-2 아치, 연결성 3→1 |
  | cage-inspect | 60.99 → 60.99 | 52.36 → 52.36 | 동일 |
  | fpv-racer | 43.12 → 43.12 | 59.23 → 59.23 | 측정값이 표면 조각이라 채택 안 함(위) |
  | fire-octo | 47.79 → 47.79 | 45.69 → 45.69 | 동일(안테나 측정 정상 기각) |
  | relay-hexa | 26.40 → **33.93** | 24.92 → 14.62 | D, views는 테더 프레이밍 아티팩트 |

  회귀 기준선은 **52 / 57 / 68 / 61**, 메시 참조 **61 / 43 / 48 / 34**로 갱신.
- 공급사명: `git grep -il`(추적 파일) **0건**, 변경 파일 0건. `qa-vendor-scan` 17건은 전부 미추적 서드파티
  (`VRINGON_회전체 데모/pipeline/.venv`).
- `docs/js/` 34개가 `js/`와 버전 쿼리만 빼고 바이트 동일. 스탬프 `396db0bf` → **`9ac70d99`**(재실행 0파일 = 안정)
  — v31이 남긴 "커밋된 소스와 어긋난 스탬프"가 이 커밋에서 바로잡혔다.
- `tools/fit-spec.mjs`의 회전 노브 주석이 "refineFromMesh가 건너뛴다"를 이유로 들고 있어 사실에 맞게 고쳤다
  (동작 불변: 작가가 기울인 파트에만 노브).

### 남은 한계 (손대지 않았다)

- **로프트 축이 박스에서 파생되는 설계** 탓에 근사 정육면체 파트를 슬라이더로 지나가면 로프트가 다른 축으로
  재적층되는 형상 변화 자체는 남는다(박스는 정확히 유지). `mesh-loft.js` `loftAxis` 소관.
- **inspect-quad `part_lower_shell`** 같은 배포 데이터의 섹션 > size_mm 불일치는 슬라이더가 닿는 순간 size_mm로
  스냅된다. 데이터 정리 대상.
- **fpv-racer ΔY +40%는 그대로다.** 메시의 안테나(33~45mm, 60mm 뒤)와 사진(~80mm 휩, 팩 뒤끝)이 서로 다르고
  사양서는 사진을 따른다. 측정으로는 안 고쳐지고(위), 안테나 제거 시 42.88/32.88, 메시 위치로 옮기면 메시 55.4·
  views 41.1 — 어느 쪽 참조를 믿을지의 문제라 남긴다.
- **relay-hexa 참조 4뷰의 테더 프레이밍**: 실루엣 비교에서 테더 제외 또는 뷰 재생성 검토. part_011·part_018 이중
  테더(878 로드가 마스트까지 관통, 스키드 아래 253) 미수정.
- 같은 종류로 보이는 미지시 항목: map-wing 프로펠러 82×4×260 @x=89 plane TOP(선언 180×4×180 @(0,145,−185));
  sar-vtol 스키드 간격 520 vs 참조 ~640·위치.
- `partSpots/specBounds`는 회전 파트를 여전히 무회전 박스로 잡아 fit이 조금 어긋난다(fpv 상단 135 vs 131, relay
  다리 하단 33 vs 51); `firstInstanceCenter`가 CIRCULAR 로드를 radius_mm(팁)에 두는 반면 spec-cad는 ringR−len/2에
  앉힌다(기존).
- 회전 파트도 측정하므로 `refine-specs`의 반복 누적 성질(v29)은 회전 파트에도 똑같이 적용된다 — 한 번 돌리고
  판단하는 도구다.
